---
# Roadmap-level Map of Content (MOC). Frontmatter join-key contract (PRSG-002).
# Generated compatibility view. The canonical reviewed map lives under
# docs/ai/knowledge/projects/<roadmap-slug>/roadmap.md. Never hand-edit an
# instantiated roadmap-MOC; regenerate it through the knowledge runner.
#
# up: MUST be a quoted relative markdown link — "[text](relative/path.md)", NEVER a
#     [[wikilink]]. The () target is load-bearing: the stale-index lint parses the
#     relative []() target and resolves it. A top-level roadmap map may have no parent;
#     when it does, point up: at it with a relative []() link.
up: ""                       # relative []() link to the parent map, if any; NEVER a [[wikilink]]
related: []                  # list of relative []() links; carried, unenforced in v1
status: ""                   # carried, unenforced in v1
rank:                        # carried, unenforced in v1
spec_id: ""                  # the roadmap identity — namespace-matches the containing directory
structureVersion: 1          # keep in sync with the lint scripts' hardcoded literal
type: "speckit-legacy-project-map"
x-speckit-authority: "generated-compatibility"
---

# Roadmap — Map of Content

> **Generated compatibility view.** The canonical reviewed roadmap map lives
> under `docs/ai/knowledge/projects/`. A source change requires reviewed
> same-path supersession; projection-only drift uses rebuild. Do not edit this
> file.

## Epics (generated from canonical knowledge)

This compatibility content is emitted from the canonical project map during a
knowledge rebuild.

<!-- GENERATED:INDEX:START (do not edit; regenerated by generate-spec-index) -->
<!-- GENERATED:INDEX:END -->
