{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "[ -d .specify ] && ! command -v specify >/dev/null 2>&1 && echo 'speckit-pro: WARNING — SpecKit CLI not found. Install: uv tool install specify-cli --from git+https://github.com/github/spec-kit.git'; true",
            "statusMessage": "Checking SpecKit CLI availability (only inside .specify projects)"
          }
        ]
      }
    ]
  }
}
