interface:
  display_name: "SpecKit Setup"
  short_description: "Prepare a spec branch and workflow for autopilot"
  default_prompt: "Set up a SPEC-ID from the technical roadmap for SpecKit autopilot"

policy:
  allow_implicit_invocation: false
