{
  "fixture_id": "03-checklist-output",
  "purpose": "Verify checklist-executor's output is parseable by the orchestrator (cross-agent format check).",
  "must_dispatch_to": ["speckit-pro:checklist-executor"],
  "must_not_have_forbidden_spawns": true,
  "response_assertions": [
    {
      "subagent_type": "speckit-pro:checklist-executor",
      "must_contain_section_keywords": ["domain", "gap"],
      "rationale": "Checklist-executor must report which domain it ran and reference gaps (found / remediated / remaining) so the orchestrator can decide whether to advance the next gate."
    }
  ]
}
