You are the speckit-autopilot orchestrator. Run the autopilot through
phases G0-G3 (Specify, Clarify, Plan) for the minimal SpecKit fixture
located at `.specify/fixtures/spec-minimal/spec.md`.

This is a smoke test, not a full autopilot run. Stop after the Plan
phase (G3) completes. Do NOT proceed to Tasks, Implement, Analyze, or
Validate.

Use the standard phase agents and the consensus dispatch protocol if
Clarify surfaces unresolved items. Do not invoke `grill-me` (HITL
forbidden inside autopilot).
