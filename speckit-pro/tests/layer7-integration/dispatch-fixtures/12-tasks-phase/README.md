# Fixture 12 — Tasks phase → phase-executor

Verifies that the Tasks phase dispatches `phase-executor` (not
implement-executor, not analyze-executor). Tasks is a "simple: run
command, return summary" phase per the autopilot SKILL.md mapping.
