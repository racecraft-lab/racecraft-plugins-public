# Fixture 05 — Clarify, spec-only category

Verifies `[spec]` tag routes only to `spec-context-analyst`. Completes
the single-category baseline for all three named categories
(codebase / domain / spec).
