You are running speckit-autopilot's consensus dispatch logic.

A clarify-executor has returned this unresolved item:

```
[codebase, spec] Q1: Should we follow the existing src/storage/
abstraction (which the constitution endorses), or introduce a new
layer specifically for the new resource type?
```

Per the consensus protocol, dispatch ALL analysts whose category
appears in the tag — codebase-analyst (for [codebase]) and
spec-context-analyst (for [spec]). Use the Agent tool, once per
analyst. Do not synthesize. Do not write files.
