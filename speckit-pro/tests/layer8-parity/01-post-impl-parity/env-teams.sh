#!/usr/bin/env bash
# Path A (Agent Teams) — set CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
# Requires Claude Code >= 2.1.32; the autopilot's Step 0.6 probe
# verifies the version at runtime.

export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
