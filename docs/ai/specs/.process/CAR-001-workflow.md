# SpecKit Workflow: CAR-001 — Candidate Route Baseline and Role Contracts

**Template Version**: 1.0.0
**Created**: 2026-07-13
**Purpose**: Executable workflow guide for the CAR-001 research spike. Copy-paste the prompts below into your AI coding agent (or run via `/speckit-pro:speckit-autopilot`).

---

## How to Use This Template

1. This workflow was generated by `/speckit-pro:speckit-scaffold-spec CAR-001` from
   `docs/ai/specs/claude-agent-routing-technical-roadmap.md` and the grill-me
   design concept doc.
2. All placeholders are populated; prompts below are ready for autonomous
   execution.
3. **Track progress** using the status table below.

---

## Design Concept

This workflow file was enriched from a Grill Me interview run during
`/speckit-pro:speckit-scaffold-spec`. The full Q&A log, Goals, Non-goals, and Open
Questions live at:

```text
docs/ai/specs/.process/CAR-001-design-concept.md
```

Re-read it before each phase if you need to disambiguate a prompt. The
Specify and Clarify Prompts below were populated from that interview,
so the design concept doc is the source of truth for any decision
captured during scoping.

> **Note:** Grill Me is human-in-the-loop only. It is **not** part of
> the autopilot loop. Once the workflow file is populated and autopilot
> begins, clarifications happen via `/speckit-clarify` and the
> consensus protocol — never via grill-me.

---

## Scope Budget and Split Decision (setup gate record)

The reviewability setup gate ran against the technical roadmap on 2026-07-13
and returned `status: warn, pass: true` with `reviewable_loc: 395`,
`production_files: 2`, `total_files: 15`, and the warning
`primary surfaces 5 exceeds warn threshold 1`. That surface count is a
roadmap-wide artifact (the gate scans all 11 CAR specs' surfaces); CAR-001
itself is a docs/process research spike. The shared estimator, run with the
spike flag and CAR-001's own signals (1 user story, 3 files, 7 scope bullets,
modify), returned `{estimated_loc: 0, suggested_slices: 1, status: ok}`.

**Split decision:** proceed as a single spike slice, sized by timebox (one
autopilot run — design concept Q9), not LOC. No split is warranted; gaps
remaining at the end of the run become explicit no-go items or capability
questions in the CAR-002 handoff instead of extending the box.

---

## Workflow Overview

| Phase | Command | Status | Notes |
|-------|---------|--------|-------|
| Specify | `/speckit-specify` | ⏳ Pending | |
| Clarify | `/speckit-clarify` | ⏳ Pending | Optional but recommended |
| Plan | `/speckit-plan` | ⏳ Pending | |
| Checklist | `/speckit-checklist` | ⏳ Pending | Run for each domain |
| Tasks | `/speckit-tasks` | ⏳ Pending | |
| Analyze | `/speckit-analyze` | ⏳ Pending | |
| Implement | `/speckit-implement` | ⏳ Pending | |

**Status Legend:** ⏳ Pending | 🔄 In Progress | ✅ Complete | ⚠️ Blocked

### Phase Gates (SpecKit Best Practice)

Each phase requires **human review and approval** before proceeding:

| Gate | Checkpoint | Approval Criteria |
|------|------------|-------------------|
| G1 | After Specify | All user stories clear, no `[NEEDS CLARIFICATION]` markers remain |
| G2 | After Clarify | Ambiguities resolved, decisions documented |
| G3 | After Plan | Architecture approved, constitution gates pass, dependencies identified |
| G4 | After Checklist | All `[Gap]` markers addressed |
| G5 | After Tasks | Task coverage verified, dependencies ordered |
| G6 | After Analyze | No `CRITICAL` issues, `WARNING` items reviewed |
| G7 | After Each Implementation Phase | Verification passes, manual review complete |

---

## Prerequisites

### Constitution Validation

**Before starting any workflow phase**, verify alignment with the project constitution (`.specify/memory/constitution.md`):

| Principle | Requirement | Verification |
|-----------|-------------|--------------|
| II. Cross-Platform Runtime & Script Safety | No new Bash/jq; artifacts are Markdown + JSON only | Review: deliverables are `docs/ai/research/*.md` + `*.json` |
| IV. Test Coverage Before Merge | Docs-only spike must not regress the suite | `python3 tests/speckit-pro/run-all.py` passes untouched |
| V. Conventional Commits | Scoped commits, e.g. `docs(research): ...` / `chore(CAR-001): ...` | `git log` review |
| VI. KISS, Simplicity & YAGNI | Requirements-level backlog, no speculative schema surface | Code/doc review against design concept Q4/Q8 |

**Constitution Check:** ✅ / ❌ (mark before proceeding to G1)

---

## Specification Context

### Basic Information

| Field | Value |
|-------|-------|
| **Spec ID** | CAR-001 |
| **Name** | Candidate Route Baseline and Role Contracts |
| **Branch** | `car-001-candidate-route-baseline` |
| **Dependencies** | None (Tier 1 sequential spike) |
| **Enables** | CAR-002 (Capability Probing, Telemetry Profile, and Exact-Treatment Contract) |
| **Priority** | P1 |

### Success Criteria Summary

From `docs/prd-claude-agent-routing.md` §3.1 (AC-1.1 through AC-1.7):

- [ ] AC-1.1 — Dated research record inventories all twelve named target agents (eleven current Claude agents + net-new `autopilot-fast-helper`) and every active source, skill, validation, evaluation, generated-payload, and installed-cache surface encoding or consuming their route policy
- [ ] AC-1.2 — Record cites current official Anthropic documentation (model IDs/aliases, subagent configuration fields, effort levels, model-resolution precedence, plugin-agent field support, fast mode, authentication modes, non-interactive telemetry); conflicting claims rejected or explicitly unresolved
- [ ] AC-1.3 — Every agent has an immutable production route (recorded as absent for the helper), role-specific contract, candidate model/effort tuples, prompt/context candidates when justified, and a fixture backlog; exclusions only for recorded evidence; `fable` enters executor-class candidate sets
- [ ] AC-1.4 — Platform facts, inferences, proposed SpecKit Pro policies, and unverified assumptions visibly separated; undocumented unavailable-model behavior recorded as a mandatory probe question
- [ ] AC-1.5 — Spike ends with provisional candidate-route manifest, role-contract catalog, fixture backlog, telemetry requirements, unresolved capability questions, and go/no-go handoff to CAR-002; no dependency on CAR-002 results, no shipped-default changes, no executability claims
- [ ] AC-1.6 — Versioned `agent_route_candidate_manifest` covering all twelve named agents published before scored screening, with production route or recorded absence, alias + expected resolved ID per candidate, required capabilities, `agent_contract_id`, instruction hash, rationale, incompatibilities, qualification artifacts, and invalidation triggers; project-level eligibility distinguished from environment-time availability
- [ ] AC-1.7 — Current Layer 6 Claude path labeled bare prompt emulation; all historical results labeled `non_release_evidence`

---

## Phase 1: Specify

**When to run:** At the start of the spike. Focus on **WHAT** and **WHY**, not implementation details. Output: `specs/car-001-candidate-route-baseline/spec.md`

### Specify Prompt

```text
/speckit-specify

## Feature: CAR-001 Candidate Route Baseline and Role Contracts

### Problem Statement
SpecKit Pro ships eleven Claude Code agents pinned uniformly to
frontmatter routes (five at opus/max, six at sonnet/max) with no recorded
evidence that these are the right model/effort routes, no qualified fallbacks
when a preferred model is unavailable, and no contract record that would let
later specs evaluate alternatives safely. Before any capability probing
(CAR-002) or scored evaluation (CAR-003) can run, the project needs a dated,
cited research baseline: what each agent's role contract actually is, what
its immutable production route is today, which candidate routes are
role-eligible, and which platform behaviors are documented facts versus
inferences versus proposed policies.

### Users
- CAR-002 through CAR-011 implementers, who consume the manifest, role
  contracts, fixture backlog, capability questions, and go/no-go handoff.
- speckit-pro maintainers, who need an auditable evidence trail for every
  future route decision.
- Plugin consumers (indirectly), whose agents will eventually get
  evidence-backed routes and graceful fallbacks without contract changes.

### User Story (single story — research spike)
As a CAR-series implementer, I need a dated, cited candidate-route and
role-contract research record plus a versioned machine-readable
agent_route_candidate_manifest covering all twelve named agents, so that
CAR-002 can freeze the executable candidate set against probed capabilities
without re-deriving role contracts or re-litigating undocumented platform
behavior.

### Key design decisions (from docs/ai/specs/.process/CAR-001-design-concept.md)
- Two artifacts: Markdown research record at
  docs/ai/research/claude-agent-route-candidates.md PLUS separate JSON
  manifest at docs/ai/research/claude-agent-route-candidate-manifest.json
  (Q1, Q2 — repo convention for machine-consumed data; CAR-002/003/006 bind
  to the JSON programmatically).
- Immutable production comparator pinned to the latest published release tag
  at research time (speckit-pro 2.19.0) plus commit SHA, with agents/*.md
  content hashes recorded for drift detection (Q3).
- Instruction identity = sha256 over the frontmatter-stripped agent body;
  full-file sha256 recorded alongside (Q4 — a pure route change must never
  invalidate instruction identity).
- Fact-table rows carry URL + access date + short verbatim quote (Q5).
- Candidates identified by the four documented aliases (opus, sonnet, haiku,
  fable) with expected resolved IDs recorded alongside; no legacy dated
  snapshots as separate candidates (Q6).
- autopilot-fast-helper contract derived from
  speckit-pro/codex-agents/autopilot-fast-helper.toml as a
  contract-equivalent translation with an explicit platform-field mapping
  table; Claude-only fields (e.g. small maxTurns) get proposed values
  labeled "proposed SpecKit Pro policy" (Q7 — CAR-010 finalizes).
- Fixture backlog entries are requirements-level only: role contract,
  representative tasks, required evidence, pass/fail sketch per agent
  (Q8 — full fixture specs are CAR-003 Work Package B's job).
- Capability questions get stable IDs (CAP-Q1…CAP-Qn) in a dedicated record
  section; the go/no-go handoff is the record's final section.

### Constraints
- Time-boxed: one autopilot run; unverified mandatory facts become no-go
  items or capability questions, never box extensions (Q9).
- Read-only with respect to the plugin: no agent frontmatter, prompt,
  payload, or shipped-default changes; nothing lands under speckit-pro/'s
  allowlisted payload directories.
- Python 3.11+ standard library only for any hash computation; no new Bash.
- Official Anthropic documentation is the only source of platform *facts*;
  anything else is labeled inference, open question, or proposed policy
  (AC-1.2/AC-1.4).
- `fable` enters executor-class candidate sets and is excluded only by
  recorded probe or contract evidence, never product-announcement status.

### Out of Scope
- Live corpus execution, qualification, scoring, or fallback ordering
  (CAR-003+).
- The plugin-owned route-policy manifest and preflight (CAR-006).
- Legacy dated model snapshots as separate screening candidates (Q6).
- Full draft fixture specs (Q8).
- Any claim that a candidate is executable before CAR-002 probing (AC-1.5).
```

### Specify Results

<!-- Fill in after running the command -->

| Metric | Value |
|--------|-------|
| Functional Requirements | |
| User Stories | |
| Acceptance Criteria | |

### Files Generated

- [ ] `specs/car-001-candidate-route-baseline/spec.md`

### SpecKit Traceability Markers

Use these markers in spec.md for traceability through later phases:

| Marker | Purpose | Example |
|--------|---------|---------|
| `[US1]`, `[US2]` | User story reference | `[US1] Implementer consumes the manifest` |
| `[FR-001]` | Functional requirement | `[FR-001] Manifest covers all twelve named agents` |
| `[NEEDS CLARIFICATION]` | Flag for Clarify phase | `Alias-to-ID binding [NEEDS CLARIFICATION]` |
| `[P]` | Parallel-safe task | `[P] Can run alongside other tasks` |
| `[Gap]` | Missing coverage | `[Gap] No section covers telemetry requirements` |

---

## Phase 2: Clarify (Optional but Recommended)

**When to run:** When spec has areas that could be interpreted multiple ways. 10-20 minutes here saves hours of rework later.

**Best Practice:** Maximum 5 targeted questions per Clarify session.

### Clarify Prompts

#### Session 1: Manifest Schema and Identity Contract

```text
/speckit-clarify Focus on the agent_route_candidate_manifest JSON schema and
identity contract: exact field set per agent entry (production route or
recorded absence, candidate tuples with alias + expected resolved ID,
agent_contract_id format, instruction hash + full-file hash, required
capabilities, rationale, incompatibilities, qualification artifacts,
invalidation triggers); schema_version convention; how project-level
eligibility vs environment-time availability is represented; how the
manifest is content-addressed/versioned. Design concept Q1/Q2/Q4/Q6 fix the
shape — resolve anything they left open.
```

#### Session 2: Fact Table Sourcing and Capability Questions

```text
/speckit-clarify Focus on the primary-source fact table and capability
questions: the exact official documentation set to cite (subagent
configuration, model configuration/aliases, effort levels, fast mode,
authentication, cost/monitoring, pricing); how a fact row degrades to a
CAP-Q probe question when the docs do not bind an alias to a dated model ID
(design concept Open Question 1); the CAP-Q ID scheme; and which
undocumented behaviors are mandatory probe questions (unavailable-model
behavior, alias re-pointing manifestation, fable resolution — PRD OQ-4).
```

#### Session 3: Helper Parity and Role Contracts

```text
/speckit-clarify Focus on the autopilot-fast-helper derivation and the
eleven role contracts: the platform-field mapping table (sandbox_mode
read-only to the shared read-only disallowedTools denylist; codex-spark to
haiku + explicit low effort as starting hypothesis); which Claude-only
fields need proposed-policy values (maxTurns — design concept Open Question
2); and what each role contract must record per agent (role boundary,
safety/grounding/mutation contract, output contract, expected tool/skill
use, representative tasks) so CAR-003 fixtures can be derived from it.
```

### Clarify Results

| Session | Focus Area | Questions | Key Outcomes |
|---------|------------|-----------|--------------|
| 1 | | | |
| 2 | | | |
| 3 | | | |

---

## Phase 3: Plan

**When to run:** After spec is finalized. Generates technical implementation blueprint. Output: `specs/car-001-candidate-route-baseline/plan.md`

### Plan Prompt

```text
/speckit-plan

## Tech Stack
- Artifacts: Markdown (research record) + JSON (manifest) under
  docs/ai/research/ — no production code, no payload changes
- Hash computation: Python 3.11+ standard library (hashlib sha256), run
  ad hoc; any helper snippet stays inside the research record or spec dir,
  NOT in shipped or test trees
- Research tooling: official Anthropic documentation pages fetched live
  (WebFetch/Tavily or equivalent available web tools); every fact row cites
  URL + access date + short verbatim quote
- Validation: python3 tests/speckit-pro/run-all.py (must pass untouched);
  JSON validity check on the manifest; privacy scan for absolute paths

## Constraints
- Design concept decisions are binding: two artifacts (Q1/Q2), release-tag
  comparator pin (Q3), frontmatter-stripped-body sha256 instruction identity
  (Q4), quoted citations (Q5), alias-only candidates (Q6),
  contract-equivalent helper translation with mapping table (Q7),
  requirements-level fixture backlog (Q8), one-autopilot-run timebox (Q9).
- Read-only inventory sources: speckit-pro/agents/*.md,
  speckit-pro/codex-agents/autopilot-fast-helper.toml,
  tests/speckit-pro/layer6-efficiency/ (two current fixtures:
  consensus-synthesizer, gate-validator), plus every surface that encodes
  route policy (skills, references, validators, dist payloads,
  installed-cache mirrors) for the AC-1.1 surface inventory.
- The immutable production comparator records the eleven current frontmatter
  tuples (five opus/max: phase-executor, implement-executor,
  analyze-executor, checklist-executor, clarify-executor; six sonnet/max:
  uat-runbook-author, domain-researcher, codebase-analyst,
  spec-context-analyst, consensus-synthesizer, gate-validator) at the
  pinned release tag.
- AC-1.7: label the current Layer 6 Claude path (frontmatter-stripped body
  piped to `claude -p --model`) bare prompt emulation; historical results
  are non_release_evidence.

## Architecture Notes
- The JSON manifest is the machine contract; the Markdown record is the
  evidence and rationale. Cross-reference by agent name and
  agent_contract_id; never duplicate data that can drift (constitution VI).
- Reference docs/ai/specs/.process/CAR-001-design-concept.md for the full
  Q&A rationale during planning.
```

### Plan Results

| Artifact | Status | Notes |
|----------|--------|-------|
| `plan.md` | ⏳ | Technical context, execution flow |
| `research.md` | ⏳ | Decision rationales (if needed) |
| `data-model.md` | ⏳ | Manifest schema field definitions |
| `contracts/` | ⏳ | Manifest JSON shape (if emitted) |
| `quickstart.md` | ⏳ | How CAR-002 consumes the handoff |

---

## Phase 4: Domain Checklists

**When to run:** After `/speckit-plan` — validates both spec AND plan together. Run multiple times for different domains.

**Best Practice:** Don't guess which domains to check. Analyze the spec first, then generate enriched prompts with spec-specific focus areas.

### Step 1: Analyze Spec for Recommended Domains

This spike produces research documentation and a machine-readable manifest —
no API, UI, or runtime code. Domains chosen from the spec's actual risk
surface:

| Signal in Spec/Plan | Recommended Domain |
|---|---|
| Twelve agent entries, seven ACs, nine design decisions that must all trace | **traceability** |
| JSON manifest schema, identity hashes, version pins, invalidation triggers | **data-integrity** |
| Fact/inference/policy labeling, citation quality, parity-translation fidelity | **research-rigor** |

**Target: 2-4 domains.** These three cover the spike's failure modes.

### Step 2: Run Enriched Checklist Prompts

#### 1. traceability Checklist

Why this domain: seven acceptance criteria, twelve agents, and nine binding
design decisions must each map to record/manifest content — silent omissions
are this spike's most likely defect.

```text
/speckit-checklist traceability

Focus on CAR-001 Candidate Route Baseline and Role Contracts requirements:
- Every AC-1.1 through AC-1.7 clause maps to a named section or manifest
  field; every design-concept decision (Q1-Q9) is honored or explicitly
  revised with a note
- All twelve named agents appear in both the record and the manifest, with
  the helper's production route recorded as absent
- The AC-1.1 surface inventory covers source, skills, validation,
  evaluation, generated-payload, and installed-cache surfaces
- Pay special attention to: the go/no-go handoff — AC-1.5's required
  contents (manifest, role-contract catalog, fixture backlog, telemetry
  requirements, capability questions, go/no-go) each present and complete
```

#### 2. data-integrity Checklist

Why this domain: the manifest is the machine contract three later specs bind
to — schema drift, ambiguous hashes, or an unreproducible comparator pin
poison the whole CAR series.

```text
/speckit-checklist data-integrity

Focus on CAR-001 Candidate Route Baseline and Role Contracts requirements:
- Manifest JSON is valid, versioned (schema_version), and every agent entry
  carries the full AC-1.6 field set
- Instruction hash = sha256 of frontmatter-stripped body AND full-file
  sha256 both present and computed from the pinned release-tag content, not
  the working tree
- Comparator pin (release tag + commit SHA) is recorded and reproducible;
  eligibility vs availability distinction is structurally represented, not
  prose-only
- Pay special attention to: invalidation triggers (including alias
  re-pointing) — present per candidate and actionable, not boilerplate
```

#### 3. research-rigor Checklist

Why this domain: AC-1.2/AC-1.4 make labeling and citation quality hard
gates; paraphrase drift or unlabeled inference is how this record fails
review.

```text
/speckit-checklist research-rigor

Focus on CAR-001 Candidate Route Baseline and Role Contracts requirements:
- Every fact row: official URL + access date + short verbatim quote; no
  paraphrase presented as fact; conflicting claims rejected or explicitly
  unresolved
- Facts, inferences, proposed SpecKit Pro policies, and unverified
  assumptions visibly separated (distinct labeled tables or tags)
- Undocumented behaviors (unavailable-model handling, alias re-pointing
  manifestation, fable resolution) recorded as mandatory CAP-Q probe
  questions, never assumed
- Pay special attention to: the helper parity mapping table — every Codex
  field mapped or explicitly marked no-equivalent-with-proposed-policy
```

### Checklist Results

| Checklist | Items | Gaps | Spec References |
|-----------|-------|------|-----------------|
| traceability | | | |
| data-integrity | | | |
| research-rigor | | | |
| **Total** | | | |

### Addressing Gaps

When checklist identifies `[Gap]` items:

1. Review the gap — is it a genuine missing requirement?
2. Update `spec.md` or `plan.md` to address it
3. Re-run the checklist to verify coverage
4. If the gap is intentionally out of scope, document why

---

## Phase 5: Tasks

**When to run:** After checklists complete (all gaps resolved). Output: `specs/car-001-candidate-route-baseline/tasks.md`

### Tasks Prompt

```text
/speckit-tasks

## Task Structure
- Small, verifiable chunks; clear acceptance criteria referencing FR-xxx
  and AC-1.x
- Dependency ordering: inventory → fact research → contracts/manifest →
  fixture backlog/telemetry → handoff
- Mark parallel-safe tasks explicitly with [P] (e.g. per-agent contract
  inventory entries are parallel; the fact table and the manifest schema
  are not, they gate later tasks)
- Single user story (research spike) — organize by deliverable, not layer

## Implementation Phases
1. Inventory: eleven agent definitions + Codex helper toml + AC-1.1 surface
   sweep + Layer 6 fixture-gap inventory (two current / ten missing)
2. Primary-source fact table: official docs research with URL + date +
   verbatim quote per row; CAP-Q entries for every undocumented behavior
3. Role contracts + manifest: agent_contract_id records, candidate tuples,
   hashes computed from the pinned release tag, invalidation triggers;
   validate JSON
4. Fixture backlog (requirements-level), telemetry requirements, AC-1.7
   harness labeling, go/no-go handoff assembly

## Constraints
- Deliverables land in docs/ai/research/ (record + manifest) and
  specs/car-001-candidate-route-baseline/ (spec artifacts) ONLY — nothing
  under speckit-pro/ payload dirs, dist/, or tests/speckit-pro/
- Design-concept Non-goals bound task generation: flag any task that would
  edit agent frontmatter, build fixtures, order fallbacks, or claim
  executability — those cross CAR-002/003/006/010 boundaries
- Every task's done-check must be observable (section exists, JSON
  validates, suite passes, hash reproducible)
```

### Tasks Results

| Metric | Value |
|--------|-------|
| **Total Tasks** | |
| **Phases** | |
| **Parallel Opportunities** | |
| **User Stories Covered** | |

---

## Atomicity Route

**When this is filled:** After the Tasks phase / gate G5, the autopilot SKILL runs
the read-only atomicity classifier and records its decision here. This is a
**placeholder** until then — leave the cells blank during scoping. The classifier
emits one machine-readable decision; the SKILL is what writes it into this section
(the script never writes a file of its own). This route is recorded only here in the
workflow file — never in the spec map. It is read downstream by the layer-planner and
multi-PR emission work that builds on top of it; recording it now wires no PR creation
or branch splitting on its own.

The decision answers "can this change be split into multiple small PRs safely?" by
inspecting the change's structural seams (independent additive capabilities), not its
line count. Surface the four fields the SKILL extracts from the emitted decision:

| Field | Value | Meaning |
|-------|-------|---------|
| **Route** | | One of `split-PR`, `one-navigable-PR`, `single-atomic-PR`, `branch-by-abstraction`, or `out-of-scope`. |
| **Releasable** | | `true`, or `false` for a destructive-migration or concurrency-sensitive change (a passing CI run does not prove such a change is safe to release). |
| **Signals** | | The decisive detector findings behind the route and releasability reading (may be empty when the classifier abstains). |
| **Warnings** | | Any release-safety warning attached to the change (empty when there is no releasability risk). |

To produce the decision, run the classifier against the feature directory:

```text
runner helper atomicity-route specs/car-001-candidate-route-baseline
```

---

## Phase 6: Analyze

**When to run:** Always run after generating tasks to catch issues.

### Analyze Prompt

```text
/speckit-analyze

Focus on:
1. Constitution alignment — principles II (no new Bash), IV (suite passes
   untouched), VI (no speculative schema surface beyond AC-1.6's field set)
2. Coverage gaps — every AC-1.1 through AC-1.7 clause and every FR has a
   task; all twelve agents covered in both record and manifest tasks
3. Design-concept drift — docs/ai/specs/.process/CAR-001-design-concept.md
   is the source of truth for the nine scoping decisions; if spec.md,
   plan.md, or tasks.md contradicts a Q1-Q9 decision without an explicit
   revision note, the downstream artifact is wrong
4. Boundary integrity — no task touches speckit-pro/ payload dirs, builds
   fixtures, orders fallbacks, or claims candidate executability
```

### Analyze Severity Levels

| Severity | Meaning | Action Required |
|----------|---------|-----------------|
| `CRITICAL` | Blocks implementation, violates constitution | **Must fix before G6 gate** |
| `HIGH` | Significant gap, impacts quality | Should fix |
| `MEDIUM` | Improvement opportunity | Review and decide |
| `LOW` | Minor inconsistency | Note for future |

### Analysis Results

| ID | Severity | Issue | Resolution |
|----|----------|-------|------------|
| | | | |

---

## Phase 7: Implement

**When to run:** After tasks.md is generated and analyzed (no coverage gaps).

### Implement Prompt

```text
/speckit-implement

## Approach: Evidence-First Research Authoring

This is a documentation spike — TDD's red/green cycle maps to
evidence-then-claim:

1. **EVIDENCE**: Fetch/read the primary source (official doc page, agent
   file at the pinned tag, Codex toml) BEFORE writing the claim
2. **CLAIM**: Write the record/manifest entry citing that evidence (URL +
   access date + verbatim quote for docs; path + hash for repo sources)
3. **LABEL**: Classify every claim — fact / inference / proposed SpecKit
   Pro policy / unverified assumption / CAP-Q probe question
4. **VERIFY**: JSON validates; hashes reproduce from the pinned tag;
   cross-references between record and manifest resolve

### Pre-Implementation Setup

1. Confirm working directory is the car-001-candidate-route-baseline
   worktree, branch car-001-candidate-route-baseline (never main)
2. Run python3 tests/speckit-pro/run-all.py to confirm a green baseline
3. Resolve the latest release tag (expect speckit-pro 2.19.0) and its
   commit SHA; extract the eleven agents/*.md contents at that tag for
   hashing — not the working tree

### Implementation Notes
- Consult docs/ai/specs/.process/CAR-001-design-concept.md Q&A log for the
  "why" behind each decision before deviating from any prompt
- Decisions captured in the design concept but missing from tasks.md are
  gaps to surface before writing, not silently drop
- No absolute local paths in any authored artifact (privacy scan gate) —
  repo-relative references only
- Timebox: one autopilot run; at the end, unverified mandatory facts go
  into the go/no-go handoff as no-go items or CAP-Q entries
```

### Implementation Progress

| Phase | Tasks | Completed | Notes |
|-------|-------|-----------|-------|
| 1 - Inventory | | | |
| 2 - Fact table | | | |
| 3 - Contracts + manifest | | | |
| 4 - Backlog, telemetry, handoff | | | |

---

## Post-Implementation Checklist

- [ ] All tasks marked complete in tasks.md
- [ ] Default suite passes untouched: `python3 tests/speckit-pro/run-all.py`
- [ ] Manifest JSON validates (python3 -m json.tool or equivalent)
- [ ] Instruction/full-file hashes reproduce from the pinned release tag
- [ ] Privacy scan: no absolute local paths in authored artifacts
- [ ] No changes under speckit-pro/ payload dirs, dist/, or installed-cache mirrors
- [ ] Manual verification complete (AC-1.1 through AC-1.7 walk-through)
- [ ] PR created and reviewed (plain-English title with conventional-commit prefix)
- [ ] Merged to main branch

---

## Lessons Learned

### What Worked Well

-

### Challenges Encountered

-

### Patterns to Reuse

-

---

## Project Structure Reference

```
racecraft-plugins-public/
├── docs/ai/research/                  # ← record + manifest land here
├── docs/ai/specs/.process/           # workflow + design concept (this file)
├── specs/car-001-candidate-route-baseline/  # spec.md, plan.md, tasks.md, SPEC-MOC.md
├── speckit-pro/agents/               # read-only inventory source (11 agents)
├── speckit-pro/codex-agents/         # autopilot-fast-helper.toml parity source
└── tests/speckit-pro/layer6-efficiency/  # fixture-gap inventory source (read-only)
```

---

Template based on SpecKit best practices. Populated by `/speckit-pro:speckit-scaffold-spec CAR-001` on 2026-07-13.
