---
title: Start
description: "SpecKit Pro runs the GitHub Spec Kit workflow inside Claude Code and Codex — specify, plan, break work into tasks, then build and verify each one so changes land in small, reviewable slices."
template: splash
hero:
  title: From spec to a reviewable pull request.
  tagline: speckit-pro runs the GitHub Spec Kit workflow inside Claude Code — specify, plan, break the work into tasks, then build and verify each one — so changes land in small, reviewable slices instead of one big diff.
  image:
    light: ../../assets/mark-light.svg
    dark: ../../assets/mark-light.svg
    alt: Racecraft logomark
  actions:
    - text: Start your first workflow
      link: /racecraft-plugins-public/first-run/
      variant: primary
    - text: View on GitHub
      link: https://github.com/racecraft-lab/racecraft-plugins-public
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid>
	<Card title="Structured, not improvised">
		Each feature moves through clear phases — specify, plan, tasks, implement — so nothing gets built before it's understood.
	</Card>
	<Card title="Sized for review">
		Work is sliced into small, self-contained pull requests with a trail from requirement to code, so reviewers aren't handed a wall of diff.
	</Card>
	<Card title="Runs in your editor">
		Install it as a Claude Code plugin and drive the whole workflow with a few commands — no extra services to wire up.
	</Card>
</CardGrid>
