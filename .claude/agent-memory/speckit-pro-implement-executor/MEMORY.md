# Memory Index

## Feedback
- [Test harness arrays](feedback_test_harness_arrays.md) — speckit-pro test harness uses hardcoded arrays (run-all.sh + validate-scripts.sh), not auto-discovery; register new scripts/tests in both or they silently never run
