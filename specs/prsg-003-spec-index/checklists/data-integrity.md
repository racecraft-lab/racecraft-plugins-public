# Data-Integrity Checklist: Generated index/PRs/backlinks + status integration + phase-gate regen

**Purpose**: Unit-tests-for-requirements covering data-integrity concerns specific to
this feature — determinism/byte-identity, whole-zone replacement, ID-join correctness,
byte-identity across creation paths, PRS manifest integrity, read-only `--check`
no-write integrity, and dogfood lint-cleanliness. Each item interrogates whether the
**requirements** (in spec.md / plan.md) are complete, clear, consistent, and
measurable — NOT whether the implementation works.
**Created**: 2026-06-06
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on
feature context and requirements. Items reference spec sections `[Spec §X]` /
`[Plan §X]` where a requirement exists, or carry a Gap marker where a requirement
is missing or under-specified on the evaluated surface (spec.md / plan.md).

## Determinism & Canonical Ordering

- [ ] CHK001 Is byte-identical determinism stated as a requirement — identical committed inputs MUST produce identical output? [Completeness, Spec §FR-003]
- [ ] CHK002 Is the re-run zero-diff (idempotent) outcome specified as a measurable success criterion? [Measurability, Spec §SC-001]
- [ ] CHK003 Is the cross-spec ordering key (normalized identifier ascending) specified? [Completeness, Spec §FR-005]
- [ ] CHK004 Is the fixed within-spec artifact precedence (spec → plan → tasks → data-model → research → contracts → checklists → process exhaust) enumerated explicitly? [Clarity, Spec §FR-005]
- [ ] CHK005 Is the tie-break after artifact precedence specified (path order within each bucket)? [Clarity, Spec §FR-005]
- [ ] CHK006 Is independence from filesystem enumeration order stated as a requirement, not merely implied? [Completeness, Spec §FR-005/SC-009]
- [ ] CHK007 Is the cross-machine byte-identity outcome under differing enumeration orders specified as a measurable criterion? [Measurability, Spec §SC-009]
- [ ] CHK008 Is the deterministic-sort mechanism (the `LC_ALL=C sort` of file lists that makes ordering enumeration-independent) stated where a reviewer of spec/plan would see it? [Plan §Constraints]
- [ ] CHK009 Is "no network / no external service at generation time" specified as a determinism precondition (offline success)? [Completeness, Spec §FR-010/SC-008]

## Whole-Zone Replacement (the #1 stale-content risk)

- [ ] CHK010 Is whole-zone replacement (replace the entire body between a zone's start/end markers) stated as a hard requirement? [Completeness, Spec §FR-002]
- [ ] CHK011 Is partial / in-place patching of zone content explicitly forbidden? [Clarity, Spec §FR-002]
- [ ] CHK012 Are the three zones specified as independently bounded by their own marker pairs (so one zone's rebuild cannot corrupt another)? [Completeness, Spec §FR-001]
- [ ] CHK013 Is the bytes-outside-markers-unchanged guarantee specified as an acceptance scenario? [Measurability, Spec §US1-AC1]
- [ ] CHK014 Is the exact marker/sentinel byte-spelling pinned in one authoritative place referenced from the plan (so "whole zone" has unambiguous bounds)? [Clarity, Plan §D1]
- [ ] CHK015 Is sentinel matching specified as full-line string equality (not a loose substring/regex that could mis-bound a zone)? [Clarity, Plan §D1]
- [ ] CHK016 Is the empty-zone shape pinned byte-exactly (two consecutive sentinel lines, no body between) so an emptied zone is unambiguous? [Clarity, Plan §D2]

## ID-Join Correctness (normalization reuse, opaque suffix)

- [ ] CHK017 Is reuse of the existing canonical identifier-normalization join required for every cross-spec match? [Completeness, Spec §FR-004]
- [ ] CHK018 Is introducing a second normalization implementation explicitly prohibited? [Clarity, Spec §FR-004]
- [ ] CHK019 Is the namespace-aware nature of the normalization join (the `(namespace, number-suffix)` key) stated in the requirements/data model? [Clarity, Spec §Key Entities / Data-Model E5]
- [ ] CHK020 Is opaque whole-segment number-suffix comparison (e.g. `013a1` never truncated to `013a`) stated as a requirement on the evaluated surface, rather than left only to the reused library's own docs? [Plan §Primary Dependencies]
- [ ] CHK021 Is the producer of the normalized key named so the "reuse, don't reinvent" constraint is verifiable (the specific shared normalizer)? [Traceability, Data-Model §E5]

## Byte-Identity Across Creation Paths (template-born vs inject-if-missing)

- [ ] CHK022 Is a single shared anchor + zone order required for both the template (FR-017) and the inject-if-missing path (FR-008)? [Consistency, Spec §FR-008/FR-017]
- [ ] CHK023 Is the byte-identical outcome between a template-born map and an injection-migrated map stated explicitly? [Completeness, Spec §FR-008/FR-017]
- [ ] CHK024 Is idempotent injection (insert the zones exactly once; repeated runs make no further change) specified? [Completeness, Spec §FR-008]
- [ ] CHK025 Is the canonical anchor position defined unambiguously enough to be byte-stable (not just "near the end")? [Clarity, Plan §D2]
- [ ] CHK026 Are the inter-zone and trailing-newline framing rules pinned (one blank line between zones; single trailing newline) so the two creation paths cannot drift? [Clarity, Plan §D2]
- [ ] CHK027 Is the byte-identity-across-creation-paths claim tied to a measurable check (the determinism fixture / SC-001/SC-009)? [Measurability, Spec §SC-001/SC-009]

## PRS Manifest Integrity

- [ ] CHK028 Is the PRS zone required to render only from a repository-local committed source (never a live network/`gh` call)? [Completeness, Spec §FR-010]
- [ ] CHK029 Is the absent-or-empty PRS data behavior specified as an empty-but-valid zone (not an error, not a partial write)? [Completeness, Spec §FR-011]
- [ ] CHK030 Is the malformed / unreadable PRS-manifest behavior specified on the evaluated surface (fail-safe, no partial write), distinct from the absent/empty case? [Spec §Edge Cases / Plan §D3]
- [ ] CHK031 Is deterministic row ordering for PRS records specified (normalized slice ascending, then PR ascending)? [Completeness, Spec §FR-005 / Data-Model §E4]
- [ ] CHK032 Is the minimal PRS input contract (its required fields and schema version) pinned so the renderer's input is unambiguous? [Clarity, Plan §D3]
- [ ] CHK033 Is the PRS rows' link-free rendering requirement stated so the zone cannot introduce a link the stale-index lint must resolve? [Consistency, Plan §G7-1 / D3]
- [ ] CHK034 Is the schema-version-mismatch handling for the PRS manifest specified, or intentionally bounded, on the evaluated surface? [Plan §D3]

## Read-Only `--check` No-Write Integrity

- [ ] CHK035 Is a read-only check mode that writes nothing to disk specified? [Completeness, Spec §FR-012]
- [ ] CHK036 Is the no-write guarantee specified to hold on ALL paths, including error paths? [Clarity, Spec §FR-012]
- [ ] CHK037 Is the status dashboard's read-only contract (invokes check, surfaces staleness, modifies zero files) stated and measurable? [Measurability, Spec §FR-013/SC-003/SC-004]
- [ ] CHK038 Are the three distinguishable outcomes (current / stale / error) required so consumers can act unambiguously? [Completeness, Spec §FR-015]
- [ ] CHK039 Is the error result kept distinct from the stale result (error never conflated with "stale") in the requirements? [Clarity, Spec §FR-015/FR-016]
- [ ] CHK040 Is the no-partial-write / no-corruption guarantee on a malformed-or-unreadable target stated? [Completeness, Spec §FR-016]
- [ ] CHK041 Is the non-regular-file target (directory/symlink where a map note is expected) rejected-not-written-through requirement specified? [Coverage, Spec §Edge Cases / Data-Model state transitions]
- [ ] CHK042 Is the phase-gate writer's commit-only-on-non-empty-diff behavior specified so a no-op boundary writes/commits nothing? [Completeness, Spec §FR-014/SC-005]

## Dogfood Lint-Cleanliness (PRSG-002 orphan + stale-index lints stay green)

- [ ] CHK043 Is the requirement that BACKLINKS links be relative to the map note's own directory stated where a reviewer of spec/plan would see it? [Clarity, Plan §G7-1 / Data-Model §E3]
- [ ] CHK044 Is "only files that exist on disk are enumerated" (no dangling links emitted) stated as a requirement? [Completeness, Data-Model §E3 / Plan §G7-1]
- [ ] CHK045 Is the injector's non-mutation of `up:` / frontmatter stated as a requirement (so the orphan lint stays green)? [Completeness, Plan §G7-3 / Data-Model §E2]
- [ ] CHK046 Is the per-spec enumeration boundary (only that spec's own tree, never another tree) specified? [Completeness, Spec §FR-018]
- [ ] CHK047 Is the dogfooding-on-real-spec-maps outcome (real content on at least two specs) stated as a measurable criterion? [Measurability, Spec §SC-006]
- [ ] CHK048 Is keeping the PRSG-002 orphan + stale-index lints green on the dogfooded maps stated as a required, verifiable outcome on the evaluated surface (spec success criteria), not only in a plan-level narrative? [Spec §SC-011]

## Scope, Deferrals & Consistency

- [ ] CHK049 Is the roadmap-level INDEX path's dormancy (built/fixture-tested, renders nothing live here) documented as a deliberate deferral, not an open question? [Assumption, Spec §FR-019 / Assumptions]
- [ ] CHK050 Is the "no version marker → exempt/unmodified" contract stated consistently for both discovery and the skip-and-leave-unmodified guarantee? [Consistency, Spec §FR-007/SC-007]
- [ ] CHK051 Is the single-zone-skip behavior (a map missing one marker pair still rebuilds its other present zones) specified and measurable? [Coverage, Spec §FR-009/SC-007]
- [ ] CHK052 Is the data-integrity surface free of conflicts between spec.md requirements and the plan's pinned decisions (D1–D4, G7)? [Conflict]

## Remediation Verification (re-run pass — confirms the prior batch closed cleanly)

This batch re-evaluates the **edited** spec.md / plan.md afresh, checking whether the
five concerns flagged in the first pass (CHK008/020/030/034/048) are now stated on the
surface a reviewer reads. All five verify as closed; no new distinct data-integrity gap
surfaces at correct altitude.

- [ ] CHK053 Is the deterministic-sort mechanism that makes ordering enumeration-independent now stated on the plan surface? [Resolved, Plan §Constraints — "`LC_ALL=C sort` before rendering"]
- [ ] CHK054 Is the opaque whole-segment number-suffix guarantee (`013a1` not truncated to `013a`) now stated on the plan surface beside the normalizer-reuse note? [Resolved, Plan §Primary Dependencies]
- [ ] CHK055 Is the malformed/unreadable PRS-manifest behavior now stated on the spec surface, distinct from absent/empty? [Resolved, Spec §Edge Cases + Plan §D3]
- [ ] CHK056 Is the unknown-`schemaVersion` handling for the PRS manifest now bounded on the plan surface (conservative render; FR-016 fail-safe on unparseable)? [Resolved, Plan §D3]
- [ ] CHK057 Is keeping the orphan + stale-index lints green on the dogfooded maps now a measurable spec success criterion? [Resolved, Spec §SC-011]
- [ ] CHK058 Does the read-only `--check` no-write guarantee remain consistently coupled to the error-path discipline after the malformed-PRS edit (no new write path introduced on the error branch)? [Consistency, Spec §FR-012/FR-016]

## Notes

- Check items off as completed: `[x]`
- A Gap marker (in square brackets on an item) marks a requirement that is missing
  or under-specified on the evaluated surface (spec.md / plan.md). A detail living
  only in `contracts/` or `data-model.md` still counts as a gap on the spec/plan
  surface until spec/plan state it or point to it.
- Items reference `[Spec §FR-NNN]`, `[Spec §SC-NNN]`, `[Plan §Dn]`, `[Plan §G7-n]`, or
  `[Data-Model §En]` for traceability.
