# Error Handling Checklist: Scaffold Integration — blind-spot pass and autopilot chain

**Purpose**: Requirements-quality validation of every degraded, declined, and interrupted path this feature can take — the fail-open dispatch, the degraded-seeding path and its durable record, the three no-chain endings, the interrupted planning stage and its resume command, and the absent-roadmap-field degrades.
**Created**: 2026-08-12
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/blind-spot-pass.md](../contracts/blind-spot-pass.md) · [contracts/chain-handoff.md](../contracts/chain-handoff.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements. It tests whether the requirements are written well enough to implement without interpretation — not whether an implementation works.

**Depth**: formal release gate. **Audience**: reviewer at PR time, plus the `tasks.md` generator.

**Relationship to the api-contracts domain**: that domain closed the wait deadline, the three-outcome contract, and the pre-chain predicate. Those are re-verified here as passing rather than re-litigated; this domain spends its effort on the paths those items did not reach.

## Fail-Open Recording — Mandate Versus Mechanism

- [x] CHK001 - Is fail-open stated as a requirement rather than a default, with retry-then-halt and gate-treatment both excluded explicitly? [Completeness, Spec §FR-007, Contract blind-spot-pass §7]
- [x] CHK002 - Is the recording of the gap required in **both** sinks — the operator output and the design concept — rather than in either one? [Completeness, Spec §FR-007]
- [x] CHK003 - Is a **writer** assigned for the design-concept header line? FR-007 and FR-010 both state the line MUST exist, but neither names the party that writes it. The only mechanism anywhere in the artifact set is one imperative sentence inside the FR-008 seeded block asking the interview to record it, which makes a MUST-level durable record depend on another skill obeying prose. [Resolved] [Spec §FR-007, §FR-010, §FR-008]
- [x] CHK004 - Is there a **detection** requirement for the case where the block travels but the header line is never written? Nothing in the spec, the plan, or either contract re-reads the design concept for the `**Blind-spot pass:**` key, so a missing durable record is indistinguishable at every downstream phase from a pass that was never attempted — the exact confusion SC-004 exists to prevent. [Resolved] [Spec §SC-004, §FR-010]
- [x] CHK005 - Is the **repair** behaviour specified when detection finds the line absent, and is it shown to stay inside scaffold's declared grant and the no-new-machinery constraint? [Resolved] [Spec §FR-010, §FR-002, §FR-023]
- [x] CHK006 - Is the detection point specified **per platform**? The Claude variant already re-reads the design concept after the interview and asserts required content, but the Codex variant has no equivalent read, so a requirement written once would land on one platform only and open a divergence outside the SC-011 closed list. [Resolved] [Spec §SC-011, §FR-022]
- [x] CHK007 - Are the requirements explicit that scaffold already holds every value the header line needs (outcome, reason, N, M) at the moment it renders the operator status line, so the durable record needs no second derivation? [Clarity, Spec §FR-006, §FR-010]
- [x] CHK008 - Is every path that reaches the interview covered, including the path where the interview **aborts** before writing a design concept at all, so no path can arrive with no record written? [Resolved] [Spec §FR-001, §FR-007]
- [x] CHK009 - Is the mandatory-pass constraint stated so that no invocation path — including worktree reuse and branch reuse — reaches the interview without attempting the pass? [Coverage, Spec §FR-001, Contract blind-spot-pass §1]

## The Degraded-Seeding Path

- [x] CHK010 - Is it stated that the labelled block still travels in **all three** outcomes, with the reason omitting it would destroy the degraded record's only mechanism? [Completeness, Spec §FR-007, Contract blind-spot-pass §7]
- [x] CHK011 - Is the FR-008 block template's second placeholder, `<the set-aside line>`, resolved for the two degraded outcomes? The block is one fixed shape used in two places, but a degraded run has no set-aside line to put there, and the zero-findings run would render the sentinel twice — once as the status line and once as the set-aside line. [Resolved] [Spec §FR-008, §FR-006, Contract blind-spot-pass §6, §8]
- [x] CHK012 - Are the two degraded outcomes given one operator status-line shape each, so the block's first placeholder resolves in every outcome? [Completeness, Spec §FR-006]
- [x] CHK013 - Is the `<reason>` clause drawn from an enumerated set rather than left free-form, and is it stated that the same clause is reused verbatim in the durable record? [Clarity, Spec §FR-006, §FR-010]
- [x] CHK014 - Is it stated that exactly one status line is emitted per run, so two outcomes cannot both be reported? [Consistency, Spec §FR-006]
- [x] CHK015 - Is the disposition of every finding fixed for the **ran** outcome — resolved answer or Open Question, none dropped silently? [Completeness, Spec §FR-009, §SC-003]
- [x] CHK016 - Are the Codex pinned validator strings that share the seeding edit site enumerated, so a degraded-path edit cannot silently break one? [Coverage, Contract blind-spot-pass §8, Plan §X4]

## Seed Degradation and Absent Roadmap Fields

- [x] CHK017 - Is the degrade behaviour fixed when the `Key Files` section is absent or differently named? [Completeness, Spec §FR-003, Contract blind-spot-pass §3]
- [x] CHK018 - Is a degrade behaviour fixed for a roadmap entry with **no `Depends On` line at all**? FR-003 declares the dependency chain a required input and contract §3's "behaviour when absent" column asserts the field is present in every entry of all eleven roadmaps — but `pr-size-governance-technical-roadmap.md` carries zero `Depends On` lines across its fourteen entries, so the required-input claim has a live counterexample and the pass has no defined behaviour on it. [Resolved] [Spec §FR-003, Contract blind-spot-pass §3]
- [x] CHK019 - Is the payload-assembly rule defined for the absent-required-field case, given the dispatch block is byte-identical across platforms and its framing sentence refers to "each spec named in Depends On" unconditionally? [Resolved] [Spec §FR-005, Contract blind-spot-pass §4.1]
- [x] CHK020 - Is it stated that a degraded seed is **not** a degraded outcome — that the pass still runs and still reports one of the three outcomes? [Clarity, Spec §FR-003, §FR-006]
- [x] CHK021 - Is the archived-dependency case (a `Depends On` spec whose artifacts an archive sweep removed) distinguished from the absent-field case, so the chase instruction is not applied to a field that does not exist? [Consistency, Spec §FR-004, Contract blind-spot-pass §3]
- [x] CHK022 - Is the payload-assembly rule's "nothing else is appended" constraint stated so the degrade cannot be implemented by substituting invented text? [Clarity, Contract blind-spot-pass §4.1]
- [x] CHK051 - Is a **renamed** dependency heading distinguished from a genuinely absent one? Opened during the remediation of CHK018, when re-measuring showed the roadmap with no `Depends On` label spells the same field `**Deps:**` on every entry, twelve of them naming real chains — so a degrade rule keyed on the standard label alone would write a false "no dependencies" statement into the payload rather than a missing one. [Resolved] [Spec §FR-003, Contract blind-spot-pass §3]

## The Three No-Chain Endings

- [x] CHK023 - Are the three no-chain paths enumerated, and is their shared **behaviour** fixed — no chain, hand-off command printed, nothing rolled back? [Completeness, Spec §FR-015, Contract chain-handoff §6]
- [x] CHK024 - Are the three no-chain paths **distinguishable to the operator** in the closing report? All three render under one heading, and only the decline case has a fixed outcome line, so a run that stopped because the pre-chain check failed is reported in words indistinguishable from a run the operator deliberately stopped. [Resolved] [Spec §FR-018, §FR-015, Contract chain-handoff §8.3]
- [x] CHK025 - Are FR-013a's two sub-failures — the rooting test and the cleanliness test — distinguished in what the operator is told? The two are differently actionable (a dirty tree is fixed in place; a mis-rooted session requires a new session rooted at the worktree), yet the requirements collapse both into one unreported "check failed". [Resolved] [Spec §FR-013a, §FR-018]
- [x] CHK026 - Is the next step fixed for each no-chain path, and is it stated whether the hand-off command differs between them? [Completeness, Spec §FR-015, §FR-018]
- [x] CHK027 - Is the confirmation budget stated so that the pre-check-fails path asks nothing at all, rather than asking and then refusing? [Measurability, Spec §SC-007, §FR-015a]
- [x] CHK028 - Is the "no structured confirmation mechanism" path stated as defensive rather than ordinary on Codex, with the evidence that makes it so? [Assumption, Spec §Assumptions, Contract chain-handoff §6]
- [x] CHK029 - Is the per-platform chain condition stated with its cause, so the Codex printed hand-off reads as a forced platform limit and not a failure? [Clarity, Spec §FR-015a, §FR-015b]

## Interrupted Planning Stage and Resume Derivability

- [x] CHK030 - Is the completion test stated as a read of the workflow file alone, with no live session and no state file? [Clarity, Spec §FR-019, Contract chain-handoff §9]
- [x] CHK031 - Is the completion test's second condition fixed on the **verdict value**? Both the requirement and the contract ask only for "a recorded G6.5 confidence-gate verdict", so a recorded non-PASS verdict — the strict-mode stop, which is precisely the failure case this requirement exists to report — satisfies the test as written and would be reported under `## Planning Complete`. [Resolved] [Spec §FR-019, Contract chain-handoff §9]
- [x] CHK032 - Is the resume command's `<phase>` argument derivable in **every** state that produces the `## Planning Incomplete` heading? The derivation reads "the first planning-phase row without a terminal status", but after a strict-mode gate stop all six planning rows are terminal and the non-terminal row is `Confidence Gate`, which the shipped `--from-phase` vocabulary has no token for — so the one case is undefined and an implementer must either invent a token the autopilot rejects or emit no command. [Resolved] [Spec §FR-019, Contract chain-handoff §9.0]
- [x] CHK033 - Is `<phase>` constrained to the shipped `--from-phase` vocabulary, and is it stated that a value outside the resolved stage's range is rejected before any phase work? [Resolved] [Spec §FR-019]
- [x] CHK034 - Is the failed-phase case shown to derive correctly — that a phase which failed carries no terminal status, because the shipped terminal set contains only Complete and Skipped variants while Blocked is an open status? [Coverage, Spec §FR-020, §FR-019]
- [x] CHK035 - Is the requirement to name **which** planning phases reached a terminal status stated alongside the resume command, and shown to come from the same single read? [Consistency, Spec §FR-019, Contract chain-handoff §9.0]
- [x] CHK036 - Is the `Stage` row identified as corroborating rather than the test, so a run in flight is not reported as finished? [Clarity, Contract chain-handoff §9]
- [x] CHK037 - Is the terminal-status vocabulary required to be **read** from the shipped frozenset rather than re-declared, in every artifact on the transcription path? [Traceability, Spec §FR-020, Contract chain-handoff §9.1]
- [x] CHK038 - Is the reuse case (a partially complete workflow file from an earlier run) stated as the same read rather than as a special case? [Coverage, Spec §Edge Cases, Contract chain-handoff §9.1]
- [x] CHK039 - Is the artifact index's behaviour under an interrupted planning stage fixed — that it lists what was written before the interruption and omits what was not? [Completeness, Spec §FR-018, §SC-009]

## Decline as a Normal Outcome

- [x] CHK040 - Is the decline path's outcome line fixed in words that read as a clean stop — stopped at the operator's request, nothing rolled back? [Clarity, Spec §FR-018, Contract chain-handoff §8.3]
- [x] CHK041 - Is the heading vocabulary neutral for the decline case, avoiding failure or error vocabulary? [Clarity, Spec §FR-018]
- [x] CHK042 - Does any requirement, edge case, or success criterion treat the decline as a degraded, exceptional, or error state? [Consistency, Spec §Edge Cases, §FR-015]
- [x] CHK043 - Is the decline case's field content fixed for all four report elements rather than only the outcome line? [Completeness, Contract chain-handoff §8.3]
- [x] CHK044 - Is the non-destructiveness of declining stated as a measurable outcome rather than only as prose reassurance? [Measurability, Spec §SC-006]
- [x] CHK045 - Is the closing report required to render on the decline path immediately, rather than being conditional on a planning stage that never ran? [Completeness, Spec §FR-017]

## Error-Path Coverage Integrity

- [x] CHK046 - Is the wait deadline fixed as a single value with the poll-versus-deadline boundary stated, so a still-running dispatch is never misreported as "did not run"? [Completeness, Spec §FR-002a] *(closed by the api-contracts domain; re-verified here)*
- [x] CHK047 - Are the three reply outcomes disjoint, exhaustive, and mechanically testable, including a reply arriving after the deadline? [Coverage, Spec §FR-006] *(closed by the api-contracts domain; re-verified here)*
- [x] CHK048 - Is the pre-chain predicate stated in the shipped guard's own words, with the canonicalise-and-compare implementation excluded explicitly? [Clarity, Spec §FR-013a] *(closed by the api-contracts domain; re-verified here)*
- [x] CHK049 - Are the degraded paths free of any requirement that would introduce new executable machinery, a new tool grant, or an agent edit? [Consistency, Spec §FR-002, §FR-023, §SC-012]
- [x] CHK050 - Are the error-path requirements stated once per behaviour, with the spec and the owning contract agreeing rather than each carrying a variant? [Consistency, Spec §FR-006, §FR-007, §FR-018, §FR-019]

## Remediation Log

Ten items opened as gaps on the first pass, and an eleventh (CHK051) opened
during the remediation of one of them. All eleven were closed by adding or
sharpening a requirement, never by deleting one. Items marked `[Resolved]` carry
the gap text as written when it was found, so the audit trail survives.

| Items | Gap | Fix landed in |
|---|---|---|
| CHK003, CHK004, CHK005, CHK006 | The design-concept record was mandated by FR-007 and FR-010 but had no assigned writer, no detection, and no repair. Its only mechanism was one imperative sentence inside the seeded block asking another skill to write it — best-effort in substance while stated as a MUST. Nothing anywhere re-read the design concept for the key, so a missing durable record was indistinguishable from a pass never attempted, defeating SC-004. The Claude variant already re-reads the design concept after the interview and asserts required content; the Codex variant has no such read, so a single-site fix would have opened an SC-011 divergence | New **FR-010a** in `spec.md` assigning scaffold the verify-and-repair duty on the read it already performs, per platform, using `Read` and `Edit` from the existing grant; new `contracts/blind-spot-pass.md` §9.1. FR-007's "records" split into the print (scaffold) and the durable record (scaffold verifies, writes when absent) |
| CHK008 | No path statement covered the interview aborting before any design concept exists | `spec.md` Edge Cases plus FR-010a's closing sentence: on that path the operator-output line is the only record and no durable record is owed, because the run does not continue |
| CHK011 | FR-008's block template carried `<the set-aside line>` unconditionally, but a degraded run has no such line and the zero-findings run would render the sentinel twice | `spec.md` FR-008 and `contracts/blind-spot-pass.md` §8: the second placeholder is now explicitly conditional, present only in the two ran-with-findings shapes |
| CHK018, CHK019 | FR-003 declared `Depends On` a required input and contract §3 asserted it is present in every entry of all eleven roadmaps. `pr-size-governance-technical-roadmap.md` carries zero across fourteen entries, so the universality claim was false and the pass had no defined behaviour | `spec.md` FR-003 and `contracts/blind-spot-pass.md` §3, §4.1. **Corrected mid-remediation:** the first fix treated the PRSG case as a plain absence and would have appended `none`. Re-measuring showed PRSG spells the same field `**Deps:**` on all 14 entries, 12 of them naming real chains, so that fix would have written a *false* dependency statement into the payload and silently disabled the FR-004 archaeology on the roadmap that most needs it. FR-003 now splits the case in two, mirroring its own `Key Files` handling: read a renamed variant as the chain; append `none` only when no declaration exists in any spelling. The measured counts replace the false universality claim. The Scope row is corrected in the same table: every entry carries scope text in its body, but the `**Scope:**` label is likewise not universal, so the requirement reads the text Step 2 already extracts rather than matching a heading |
| CHK024, CHK025 | The three no-chain endings shared one heading and only the decline had a fixed outcome line, so a failed pre-check read exactly like a deliberate stop; and FR-013a's two sub-failures were collapsed into one unreported "check failed" although they are differently actionable | `spec.md` FR-018 and `contracts/chain-handoff.md` §8.3 now fix one outcome line per no-chain cause, with the FR-013a line naming which of the two tests failed. Behaviour is unchanged and stays identical across the three, per CHK031 of the api-contracts domain; only the derived outcome line differs |
| CHK031, CHK032, CHK033 | The completion test asked only for "a recorded G6.5 verdict" with no verdict value, so a strict-mode stop would report `## Planning Complete`; and `<phase>` was underivable in that same state, because all six planning rows are terminal while the non-terminal row is `Confidence Gate`, which has no `--from-phase` token | `spec.md` FR-019 and `contracts/chain-handoff.md` §9, §9.0. **Corrected before sign-off:** the first fix required a recorded **PASS**, which would have inverted the heading on the ordinary run — G6.5 is advisory by default, and in advisory mode `NO_DATA` soft-skips while `FAIL` proceeds to Phase 7, so planning really does complete without a PASS. Condition 2 is now a recorded verdict **plus** a `Confidence Gate` row that is not blocked, which is the file-readable fact that separates a strict-mode stop (row blocked) from an advisory run that proceeded (row legitimately pending). The derivation is constrained to the six shipped tokens, and the all-six-terminal case resumes with the chain invocation carrying no `--from-phase`, the shipped bare-invocation behaviour that re-enters at the gate |

Cross-artifact block identity was re-verified mechanically after every edit: the
seeded block, the report layout, the operator status lines, and the header lines
remain byte-identical between `spec.md` and their owning contract once the
surrounding list indentation is removed.

## Notes

- Check items off as completed: `[x]`
- A Gap marker flags a requirement that is missing or under-specified, not a defect in an implementation.
- Traceability references use `[Spec §FR-x]`, `[Contract <file> §n]`, or `[Plan §section]`.
