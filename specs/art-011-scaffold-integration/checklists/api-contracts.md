# API Contracts Checklist: Scaffold Integration — blind-spot pass and autopilot chain

**Purpose**: Requirements-quality validation of every contract boundary this feature crosses — the `codebase-analyst` dispatch, the finding shape that travels to two consumers, the three-outcome reply classification, the ART-006 chain invocation, the FR-013a pre-chain predicate, and the closing report's field contract.
**Created**: 2026-08-12
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/blind-spot-pass.md](../contracts/blind-spot-pass.md) · [contracts/chain-handoff.md](../contracts/chain-handoff.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements. It tests whether the requirements are written well enough to implement without interpretation — not whether an implementation works.

**Depth**: formal release gate. **Audience**: reviewer at PR time, plus the `tasks.md` generator that consumes these contracts as transcription sources.

## Dispatch Contract — inputs, await, and returned shape

- [x] CHK001 - Is the dispatch identifier specified per platform, in the form the runtime actually accepts? [Completeness, Contract blind-spot-pass §2]
- [x] CHK002 - Is the requirement to **await** the dispatch stated as a requirement rather than only as a contract note? The Claude agent carries `background: true`, so an un-awaited dispatch returns an identifier instead of findings, yet no FR in spec.md states the await — it appears only in `contracts/blind-spot-pass.md` §2 and in plan.md's under-determination section. [Resolved] [Spec §FR-002]
- [x] CHK003 - Is the bound on the Codex `wait_agent` loop specified? The contract requires a "bounded" loop but fixes no wait budget, iteration count, or elapsed-time limit, so the moment at which a still-running dispatch becomes the "did not run (timeout)" outcome is undecidable. [Resolved] [Contract blind-spot-pass §2, §5]
- [x] CHK004 - Is it stated unambiguously that a status update or a timeout is not the result on Codex? [Clarity, Contract blind-spot-pass §2]
- [x] CHK005 - Is the observation that distinguishes "no reply at all" from "still waiting" defined on Claude, where the dispatch is explicitly `run_in_background: true`? [Resolved] [Spec §FR-006, Contract blind-spot-pass §5]
- [x] CHK006 - Is the assembly of the dispatch payload specified — that the roadmap Scope text, the `Depends On` chain, and the optional `Key Files` section are appended below the fixed block the requirements quote? The block's own words are "the Scope text below", but nothing states that scaffold appends it or in what order. [Resolved] [Spec §FR-003, §FR-005]
- [x] CHK007 - Are the required and optional seed elements distinguished, with the degrade behaviour for the optional one fixed? [Completeness, Spec §FR-003, Contract blind-spot-pass §3]
- [x] CHK008 - Is the archived-dependency chase stated as an instruction inside the dispatch block rather than as ambient prose the implementer must remember to include? [Consistency, Spec §FR-004, Contract blind-spot-pass §3]
- [x] CHK009 - Is the returned finding's field set fixed — title, 1-3 sentences, repo-relative pointer, impact line, surprise line — and is each field's disposition by the caller stated? [Completeness, Spec §FR-005, §FR-006]
- [x] CHK010 - Is it defined whether a finding missing its impact or surprise line still counts as "a finding in the fixed shape" for the §5 usability test? [Ambiguity, Contract blind-spot-pass §5]
- [x] CHK011 - Is the enforcement of the five-finding cap assigned to a party? FR-006 states it as a property of what the pass returns, but the pass is an LLM reply; the requirement that scaffold itself truncates and counts appears only in an Edge Case bullet. [Resolved] [Spec §FR-006]
- [x] CHK012 - Are the prohibitions on the dispatch — no agent edit, no `allowed-tools` widening — stated in both the requirement set and the contract so neither can be relaxed unilaterally? [Consistency, Spec §FR-002, §SC-012, Contract blind-spot-pass §2]

## Finding Shape Across Two Boundaries

- [x] CHK013 - Is the labelled block specified as one shape used in both the operator output and the seeded `scope` string, with a stated reason the two cannot drift? [Consistency, Spec §FR-008, Contract blind-spot-pass §8]
- [x] CHK014 - Do the block's two authoritative copies (spec.md FR-008 and contract §8) agree character-for-character, and is one of them named as the source if they ever diverge? [Consistency, Spec §FR-008]
- [x] CHK015 - Do the dispatch block's two authoritative copies (spec.md FR-005 and contract §4) agree character-for-character, and is byte-identity across the two platform variants stated as a requirement? [Consistency, Spec §FR-005, Contract blind-spot-pass §4]
- [x] CHK016 - Is it stated that the block still travels in all three outcomes, so the degraded paths retain a channel into the interview and the design-concept record? [Completeness, Spec §FR-007, Contract blind-spot-pass §7]
- [x] CHK017 - Are the strings that must survive verbatim on the Codex seeding edit enumerated, so the block insertion cannot silently break a pinned validator string? [Coverage, Contract blind-spot-pass §8, Plan X4]

## Three-Outcome Reply Classification

- [x] CHK018 - Are the three outcomes stated as disjoint with a mechanical test each, with no judgement call between them? [Clarity, Spec §FR-006, Contract blind-spot-pass §5]
- [x] CHK019 - Are the three outcomes exhaustive over every reply the dispatch can produce, including a reply that arrives after the interview has begun? [Coverage, Spec §FR-006]
- [x] CHK020 - Is an operator-facing status-line shape fixed for the **returned nothing usable** outcome? FR-008's block placeholder reads "the FR-006 status line for the outcome" for all three outcomes, but FR-006 fixes shapes only for the ran case. [Resolved] [Spec §FR-006, §FR-007]
- [x] CHK021 - Is an operator-facing status-line shape fixed for the **did not run** outcome, including where its stated reason goes? [Resolved] [Spec §FR-006, §FR-007]
- [x] CHK022 - Is a design-concept header-line shape fixed for each of the three outcomes? FR-010 fixes the key and the content each line must carry but no shape, while SC-004 requires a reader to tell the outcomes apart from the header alone. [Resolved] [Spec §FR-010, §SC-004]
- [x] CHK023 - Is it stated that the sentinel sentence serves as both the analyst-to-scaffold signal and the operator-facing string, so an implementer does not invent a second wording for the operator? [Clarity, Spec §FR-006, Contract blind-spot-pass §4, §6]
- [x] CHK024 - Is the mapping from each outcome to exactly one header line stated in one place, so the outcome table and the header requirement cannot disagree? [Consistency, Spec §FR-010, Contract blind-spot-pass §5, §9]
- [x] CHK025 - Are the fail-open consequences specified for each degraded outcome without either becoming a gate or a retry? [Completeness, Spec §FR-007, Contract blind-spot-pass §7]

## Chain Invocation Against the ART-006 Contract

- [x] CHK026 - Is the stage token fixed as the literal lowercase `plan` drawn from a closed vocabulary of `plan`, `implement`, `full`, with aliases, alternate casing, and long-form spellings excluded explicitly? [Clarity, Spec §FR-014, Contract chain-handoff §5]
- [x] CHK027 - Is the Codex runnable invocation recorded as a **deviation** from ART-006 §3 rather than presented as a quotation of it? FR-014 says the form is "fixed by the ART-006 contract §3", but §3's table shows the Codex row with no command token, and the `$speckit-autopilot` prefix is a resolution the plan supplied. Only `contracts/chain-handoff.md` §5 records that. [Resolved] [Spec §FR-014]
- [x] CHK028 - Is the normative source and its recovery command cited where an implementer building the invocation will read it, given the contract is not in the working tree? [Traceability, Spec §Normative sources]
- [x] CHK029 - Is the workflow file path fixed as the sole hand-off token, with the excluded alternatives enumerated rather than implied? [Completeness, Spec §FR-014, Contract chain-handoff §5]
- [x] CHK030 - Is the confirmation contract fully specified — mechanism per platform, exact question text, option count, option order, and mutual exclusivity? [Completeness, Spec §FR-013, Contract chain-handoff §4]
- [x] CHK031 - Are the three no-chain paths enumerated and shown to produce identical behaviour, so decline, absent mechanism, and failed pre-check cannot diverge? [Consistency, Spec §FR-015, Contract chain-handoff §6]
- [x] CHK032 - Is the confirmation budget stated as a measurable outcome (at most one, exactly one when attempted, none when the pre-check fails)? [Measurability, Spec §SC-007]
- [x] CHK033 - Is the per-platform chain condition stated with its cause, so the Codex divergence reads as a forced platform limit rather than an optional choice? [Clarity, Spec §FR-015a, §FR-015b, §FR-022]

## FR-013a Pre-Chain Predicate

- [x] CHK034 - Do the spec and the contract state the rooting predicate in the **same words as the shipped guard**? The guard at `speckit-pro/codex-skills/speckit-autopilot/references/prerequisites-codex.md` reads "If the supplied workflow path **exists** inside that checkout, continue", while FR-013a and contract §2 both say "**resolve** inside" — and §2 claims the wording is taken verbatim. [Resolved] [Spec §FR-013a, Contract chain-handoff §2]
- [x] CHK035 - Is the predicate worded so it could not be implemented as a directory or root comparison by mistake? "Resolve" invites a realpath-then-compare-roots reading, which is precisely the equivalent-looking test FR-013a exists to forbid. [Resolved] [Spec §FR-013a]
- [x] CHK036 - Does FR-013a state its consequence once rather than twice? The sentence "If any part fails, scaffold MUST NOT ask, and MUST print the hand-off command instead" appears twice in the same requirement, with "any part" and "any check" as competing subjects. [Conflict, Spec §FR-013a]
- [x] CHK037 - Is the second test (`git status --porcelain` clean) scoped to the same checkout the first test resolved, so the two cannot be applied to different trees? [Clarity, Spec §FR-013a, Contract chain-handoff §2]
- [x] CHK038 - Is the excluded test (the most recent commit) stated with the reason it is excluded, so an implementer does not reintroduce it as a strengthening? [Completeness, Spec §FR-013a]
- [x] CHK039 - Is the no-new-machinery claim substantiated by naming where each command already runs, rather than asserted? [Traceability, Spec §FR-023, Contract chain-handoff §2]
- [x] CHK040 - Is the stale-same-named-workflow hazard documented as the reason the predicate is path resolution rather than root identity? [Completeness, Spec §Edge Cases, Contract chain-handoff §2]

## Closing Report Field Contract

- [x] CHK041 - Is the report's field list closed, ordered, and stated identically in the spec and the contract? [Consistency, Spec §FR-018, Contract chain-handoff §8]
- [x] CHK042 - Is a heading fixed for every terminal condition the spec names? The layout offers `## Planning Complete` and `## Stopped Before Planning`, but US4's own independent test names three conditions — declined, chained and complete, chained and interrupted — and the interrupted case has no heading. [Resolved] [Spec §FR-018, §FR-019, §US4]
- [x] CHK043 - Is each field classified as fixed, conditional, or derived, so an implementer knows which strings are literal and which are computed? [Clarity, Spec §FR-018]
- [x] CHK044 - Is the draft-PR line's conditional behaviour fixed in both branches, with the absent-PR wording given literally and fabrication excluded? [Completeness, Spec §FR-018, §SC-008, Contract chain-handoff §8.1]
- [x] CHK045 - Is the artifact index's **candidate set** defined, so the "no written artifact is missing from the list" half of SC-009 is verifiable? The index is required to be derived and exact in both directions, but no source enumerates what could be listed. [Resolved] [Spec §FR-018, §SC-009, Contract chain-handoff §8.2]
- [x] CHK046 - Is the **existence test** for each candidate path defined, given FR-002 forbids widening scaffold's `allowed-tools` with Grep, Glob, or Bash and FR-023 forbids new machinery? Without it, an implementer either invents a forbidden mechanism or guesses the list. [Resolved] [Spec §FR-018, §FR-002, §FR-023]
- [x] CHK047 - Is the set-aside findings count excluded from the report explicitly, with its actual home named? [Consistency, Spec §FR-018, Contract chain-handoff §8]
- [x] CHK048 - Is the decline case's field content fixed for all four elements rather than only for the outcome line? [Completeness, Spec §FR-018, Contract chain-handoff §8.3]
- [x] CHK049 - Is the **resume command** given a fixed form? FR-019 requires the report to "give the resume command" but no requirement or contract fixes its shape, unlike the hand-off command whose form is fixed in the invocation table. [Resolved] [Spec §FR-019, Contract chain-handoff §9]
- [x] CHK050 - Is it stated that the resume command occupies the Next step slot in the interrupted case, given the field list is closed at four elements? [Clarity, Spec §FR-018, §FR-019]
- [x] CHK051 - Is the ordering of the pre-existing "Scaffold Complete" report and the closing report fixed per platform, with the reason the existing one comes first? [Completeness, Spec §FR-016, §FR-017, Contract chain-handoff §1]
- [x] CHK052 - Is "printed, not written to a file" stated as a requirement rather than left to the reader of the layout block? [Clarity, Spec §FR-017]

## Completion Test and the Terminal-Status Vocabulary

- [x] CHK053 - Is the completion test stated as reading the workflow file alone, with no live session and no state file? [Clarity, Spec §FR-019, Contract chain-handoff §9]
- [x] CHK054 - Are both completion conditions stated — terminal status on every planning-phase row, plus a recorded G6.5 confidence-gate verdict — and are the planning phases enumerated? [Completeness, Spec §FR-019, Contract chain-handoff §9]
- [x] CHK055 - Is the `Stage` row identified as corroborating rather than the test, so a run in flight is not read as finished? [Clarity, Contract chain-handoff §9]
- [x] CHK056 - Is the requirement to **read** the shipped `WORKFLOW_TERMINAL_STATUSES` frozenset stated, with the owning file path given? [Traceability, Spec §FR-020]
- [x] CHK057 - Does any artifact in this spec re-declare the six status literals? `contracts/chain-handoff.md` §9.1 lists all six "for reference only" inside a document whose stated purpose is that "the implementation is a transcription rather than an interpretation" — which makes the copy the most likely thing an implementer transcribes into the two `SKILL.md` files, exactly what FR-020 forbids. [Resolved] [Contract chain-handoff §9.1, Spec §FR-020]
- [x] CHK058 - Is the read-the-shipped-set requirement stated **where an implementer will see it** — on the plan's insertion-map rows that `tasks.md` is generated from? Rows C6 and X7 carry the closing report's requirement range but no instruction about the frozenset; the pointer appears only in the plan's Project Structure tree. [Resolved] [Plan §Execution Flow C6/X7, Spec §FR-020]
- [x] CHK059 - Is the reuse case (a partially complete workflow file from an earlier run) stated as the same read rather than as a special case? [Coverage, Spec §Edge Cases, Contract chain-handoff §9.1]

## Cross-Cutting Contract Integrity

- [x] CHK060 - Are the permitted platform divergences enumerated as a closed list, and does the count in the spec match the count in the success criterion? [Consistency, Spec §FR-022, §SC-011]
- [x] CHK061 - Is each contract file's stated scope (which FRs it fixes) complete against the FRs it actually constrains, so no requirement is fixed by a contract without an owning FR? [Traceability, Contract blind-spot-pass §Scope, Contract chain-handoff §Scope]
- [x] CHK062 - Are the assumptions this feature depends on — analyst presence, worktree rooting at pass time, prompt-level framing sufficiency — stated with the evidence that validates each? [Assumption, Spec §Assumptions]
- [x] CHK063 - Is every contract claim of verbatim quotation checkable against a cited file and line, rather than asserted? [Traceability, Contract chain-handoff §2, §7]

## Remediation Log

Seventeen items opened as gaps on the first pass. All seventeen were closed by
adding requirements, never by deleting one. Items marked `[Resolved]` carry the
gap text as written when it was found, so the audit trail survives.

| Items | Gap | Fix landed in |
|---|---|---|
| CHK002, CHK003, CHK005 | The await was contract-only, its "bounded" loop had no bound, and a `wait_agent` poll expiring was treated as proof the pass never ran — which contradicts the shipped Codex rule that a poll timeout "is one bounded mailbox poll, not proof that an agent is stuck" | New **FR-002a** in `spec.md`; new bound table and poll-versus-deadline rule in `contracts/blind-spot-pass.md` §2. Deadline fixed at 5 minutes from dispatch, platform-general. **Amended at consensus:** the first fix also made "3 consecutive expired polls" an independent trigger, which was mis-scoped — "poll" has no Claude-side referent, so a "whichever comes first" rule would let Codex abandon earlier than Claude, a divergence outside SC-011's closed list. The poll count is now a Codex-only cue for checking the one deadline, and the five-minute value is recorded as stipulated rather than precedented, since the repository states this deadline requirement in three shipped places and fixes a number in none |
| CHK006 | The dispatch block says "the Scope text below" but nothing said what was appended, or in what order | `spec.md` FR-005 assembly paragraph; new `contracts/blind-spot-pass.md` §4.1 |
| CHK010, CHK011 | "A finding in the fixed shape" was undefined, and no party enforced the five-finding cap on a model reply | `spec.md` FR-006; `contracts/blind-spot-pass.md` §5 and §6 |
| CHK020, CHK021, CHK022, CHK024 | Only the "ran" outcome had operator strings; the two degraded outcomes had none, and no header-line shape was fixed for any outcome although SC-004 depends on telling them apart from the header alone | `spec.md` FR-006 (two new status lines) and FR-010 (three header shapes); `contracts/blind-spot-pass.md` §5 outcome table now maps each outcome to one string and one header line, with §6 and §9 carrying the shapes |
| CHK027 | FR-014 presented the Codex invocation as "fixed by the ART-006 contract §3" when §3's table omits the command token | `spec.md` FR-014 now carries a per-platform table with a `Provenance` column marking the Codex row a recorded deviation; `plan.md` under-determination entry 2 updated to match |
| CHK034, CHK035, CHK036 | The predicate was paraphrased as "resolve inside" against the guard's "exists inside", the contract claimed the paraphrase was verbatim, "resolve" invites the root comparison the requirement forbids, and FR-013a stated its consequence twice | `spec.md` FR-013a rewritten to the guard's own two steps, with an explicit prohibition on canonicalise-and-compare and the duplicate sentence removed; `contracts/chain-handoff.md` §2 matched |
| CHK042, CHK043 | Two headings for three terminal conditions, and no statement of which fields are fixed, conditional, or derived | `spec.md` FR-018 and `contracts/chain-handoff.md` §8 now carry a closed three-value heading vocabulary, adding `## Planning Incomplete`, plus a fixed/conditional/derived classification |
| CHK045, CHK046 | SC-009 demands two-way exactness against an undefined candidate set, with no existence test available inside scaffold's declared grant | `spec.md` FR-018 and `contracts/chain-handoff.md` §8.2 now close the candidate set and fix the existence test as a read of the candidate path |
| CHK049, CHK050 | The resume command had no fixed form and no stated slot in a field list closed at four elements | `spec.md` FR-019 and new `contracts/chain-handoff.md` §9.0, using the autopilot's own `--from-phase` flag with the phase derived from the same workflow-file read |
| CHK057 | `contracts/chain-handoff.md` §9.1 listed all six terminal-status literals inside a document whose purpose is verbatim transcription into the two `SKILL.md` files | The list is removed from §9.1, which now states the omission is deliberate and points at the frozenset. No artifact in this spec re-declares the literals |
| CHK058 | The read-the-shipped-set requirement was absent from the plan's insertion-map rows that `tasks.md` is generated from | `plan.md` rows C6 and X7 now carry it, plus a new **Terminal-status constraint** note beside the existing payload and pinned-string constraints |

Cross-artifact block identity was re-verified mechanically after every edit: the
dispatch block, the seeded block, the report layout, the two degraded status
lines, and the three header lines are byte-identical between `spec.md` and their
owning contract once the surrounding list indentation is removed.

## Notes

- Check items off as completed: `[x]`
- A Gap marker flags a requirement that is missing or under-specified, not a defect in an implementation.
- Traceability references use `[Spec §FR-x]`, `[Contract <file> §n]`, or `[Plan §section]`.
