# Tasks: Final-PR Template Set — Slice 2, the Annotated Diff Artifact

**Input**: Design documents from `specs/art-003-final-pr-template-set-slice-2/`

**Prerequisites**: `plan.md`, `spec.md`, `research.md`, `data-model.md`, `quickstart.md`. No `contracts/` directory ships in this slice: the export payload contract this artifact must match already exists at `specs/art-003-final-pr-template-set/contracts/export-payload-contract.md`, is present on this branch, and forking it would fork a pinned literal set. This slice reads it and does not edit it.

**Tests**: This slice's test surface is the repository suite, not new test files. TDD here means the three shared-validation literals that bind this template land **before** the artifact they judge (T004), and the binding is proved live rather than assumed (T034). FR-042a fixes the shared-validation change at three literals, so no test file is created and none is renamed.

**Reviewability**: Budget is `warn` — 750 authored lines declared against an 800 block, 1 production file, 13 total files, one primary surface (docs/process). Headroom is 50 lines. The ceiling is not a prediction; it is a checkable constraint enforced at **four** checkpoints with a stop rule (T022, T024, T032, T035). Task generation adds no file beyond the three in `plan.md` § Declared File Operations. If any checkpoint fails, stop and apply `plan.md` § "The reduction levers, in order" — do not add tasks and do not absorb the overrun.

**Organization**: Tasks are grouped by user story. US1 (P1) is the MVP and delivers standalone value: a reviewer reads an annotated diff. US2 (P2) adds per-hunk objection capture and both exports.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different file, no dependency on an incomplete task)
- **[Story]**: US1 or US2, on user-story phases only
- Paths are repository-relative throughout. **Never write an absolute filesystem path into any artifact** — a tree-wide privacy scan fails on one.

## Path Conventions

This is not an application. The three surfaces are fixed by `plan.md` § Declared File Operations:

- `speckit-pro/artifact-gallery/templates/annotated-diff.html` — NEW, the whole production surface
- `speckit-pro/artifact-gallery/manifest.json` — MODIFIED, exactly one value
- `tests/speckit-pro/unit/test-artifact-fill-regions.py` — MODIFIED, exactly three literals

**One HTML file means most work is sequential.** Only four tasks carry `[P]`, each because it touches a genuinely different surface.

## Read before the first suite run

**The suite is not read-only.** `validate-plugin-payload.py` runs the real payload builder, so **every** suite run rewrites `dist/**`. Two consequences bind every task below that runs the suite:

- Never `git add -A` after a suite run. Stage paths by name.
- Restore a rewritten generated path with `git show HEAD:<path> > <path>`, never by hand-editing it.

**`--layer 4` alone overcounts.** Running Layer 4 by itself reports more failures than the same tree reports under a full run. Judge a red against `python3 tests/speckit-pro/run-all.py`, never against the layer alone.

**`refresh-release-artifacts.py --check` exits 1 on a correct uncommitted regeneration.** It compares against the committed tree. That exit is not a failure and must not be chased; it resolves on commit.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Establish the baseline the closeout is measured against, obtain the upstream source read-only, and satisfy the one worktree prerequisite.

- [ ] T001 [P] Record the G0 baseline by running `python3 tests/speckit-pro/run-all.py` from the worktree root and writing the three layer counts into the run record. **Verify**: totals match `quickstart.md` § "The baseline" — **7379 passing** (Layer 1 1447, Layer 4 5746, Layer 5 186), which is slice 1's shipped state and this branch's starting point. If they differ, record the delta now; do not recompute the baseline at a later stage, because T040 verifies an increase against this number. This run rewrites `dist/**`; restore any rewritten generated path with `git show HEAD:<path> > <path>` and stage nothing by wildcard.
- [ ] T002 [P] Fetch upstream `03-code-review-pr.html` read-only from `anthropics/html-effectiveness` (`main`) into the session scratchpad directory, outside the repository tree. **Verify**: the fetched file is readable from the scratchpad and `git status --porcelain` lists it nowhere. **Upstream bytes are never staged and never committed**; only the branded derivative is. This is ART-002's recorded protocol, carried forward in `spec.md` § Assumptions. The file measures 638 lines — 389 CSS, 237 markup, 12 lines of script in a single element, three disclosure elements, and no button at all — so it supplies the diff structure and the annotation mechanism and **no export behaviour whatsoever**.
- [ ] T003 [P] Install the docs-site dependencies once for this worktree with `pnpm --dir docs-site install --frozen-lockfile`. **Verify**: exit code 0. `docs-site/` is the only surface with dependencies, and T037 cannot run without this.

**Checkpoint**: Baseline recorded, upstream source available read-only, docs-site ready.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The three shared-validation literals, the file skeleton, both canonical blocks embedded byte for byte with their markers, the attribution header, and the two-line slot inventory. Nothing in either user story can be judged until the artifact is a valid gallery file.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

**T004 is ordered first and carries no `[P]`, deliberately.** Slice 1 marked the equivalent task `[P]`, which left a task whose verification depended on those literals free to land before them. The literals bind the artifact that every later verification is judged against, so they go first and they do not float.

### Expected intermediate red — three families, not one

From T005 until the artifact is both flipped (T033) and regenerated into the payloads (T036), a full-suite run is red **on purpose**. On slice 1 this was **eight failures**, and it is three separate families with one root cause: the artifact exists in the gallery source but is not yet claimed by the catalog or mirrored into the payloads.

| Family | Where | Resolved by |
|---|---|---|
| Catalog orphan — file present, entry still reads `planned`; the contract binds status and file presence in **both** directions | `test-artifact-gallery` | **T033** catalog flip |
| Payload parity — the artifact is under the gallery source and absent from the claude and codex payloads | `test-artifact-gallery` | **T036** `refresh-release-artifacts.py` |
| Release-readiness fixtures **and** the bash-confinement guard, which read the payload and the cache proofs | `test-speckit-pro-gates` | **T036** |

**The catalog flip briefly makes the count worse before it makes it better.** Flipping the entry to `shipped` activates checks that stay dormant while it reads `planned`, so the failure count can rise at T033 and then fall to zero at T036. That is the expected shape, not a regression.

`quickstart.md` additionally tracks a **stale docs reference**, because `test-artifact-fill-regions.py` is a tracked `.py` file under `tests/speckit-pro/`. It is cleared by **T037**, and CI's `artifact-consistency` job fails the pull request on it whether or not the local suite does.

**Do not silence any of these early.** Flipping the catalog before T033 breaks the both-directions guarantee this artifact exists to prove; regenerating payloads before the artifact is final desynchronises them and they must be regenerated again anyway. Record the actual count the first time it is observed and treat it as the known set for the rest of the run. **Anything outside these families is a real failure** — stop and read it rather than waiting it out. The full suite is required green only at T040.

- [ ] T004 Add exactly three literals to `tests/speckit-pro/unit/test-artifact-fill-regions.py` and no fourth: `"annotated-diff": ("hunks",)` in the `FLOOR` dict near line 85; `"annotated-diff": ("hunks",)` in the `LIST_SLOTS` dict near line 100; and `"git-diff"` as a member of the `SOURCE_ARTIFACTS` tuple near line 138. **Verify**: run the import probe in `quickstart.md` § "Prove the validation is not passing vacuously" and confirm it prints `annotated-diff` present in `FLOOR` with `("hunks",)`, `LIST_SLOTS["annotated-diff"]` equal to `("hunks",)`, and `git-diff` a member of `SOURCE_ARTIFACTS`; then `python3 tests/speckit-pro/run-all.py --layer 4` still passes. The floor row names `hunks` and nothing else because the floor literal traces to the roadmap and to nothing else, and the roadmap describes this template as a unified diff with margin annotations, severity tags and jump links, naming no other region; `feature-header` is chrome and is deliberately absent, exactly as it is absent from slice 1's floor row. **The two rows carrying the same value is not redundancy** — the module's own comment records why: floor membership proves only that a region of that name exists, never that its items are addressable. `git-diff` is the first member of the closed set that is not a filename; membership is tested by plain string equality with no filename shape enforced, so an extensionless value is legal. This is the whole of the slice's change to shared validation (FR-041, FR-042, FR-042a). Do not widen the floor-scoped universe and do not add a general catalog-to-floor guard — both are recorded out-of-scope decisions, and the second contradicts a decision recorded inside the file being edited.
- [ ] T005 Create `speckit-pro/artifact-gallery/templates/annotated-diff.html` with the document skeleton only: one HTML file, no build step, no bundler, no preprocessor, and no sibling asset of any kind. **Verify**: the file stem equals the `id` its catalog entry declares (`annotated-diff`), and the directory gains no second file (FR-001, FR-002).
- [ ] T006 Embed the `BRAND-KIT` block from `speckit-pro/artifact-gallery/brand-kit.css` into `speckit-pro/artifact-gallery/templates/annotated-diff.html` verbatim, with its markers, byte for byte, exactly once, start before end (318 lines). **Verify**: a byte comparison of the embedded span against the source block reports zero characters of drift, and `python3 tests/speckit-pro/run-all.py --layer 4` reports no drift finding naming this artifact. Never hand-edit the copy; re-copy from source if it drifts (FR-005, SC-009).
- [ ] T007 Embed the `GALLERY-HEAD` block from `speckit-pro/artifact-gallery/theme-toggle.html` into `speckit-pro/artifact-gallery/templates/annotated-diff.html` verbatim, with its markers, byte for byte, exactly once, start before end (140 lines). The security policy declaration, the font request, the pre-paint theme application, and the theme control all arrive inside it. **Verify**: byte comparison reports zero drift, and none of those four is authored, replaced, wrapped, or moved anywhere in the file (FR-006, SC-009).
- [ ] T008 Write the attribution header as an HTML comment near the top of `speckit-pro/artifact-gallery/templates/annotated-diff.html`, using the five exact labels the contract fixes plus the upstream copyright line verbatim. **The upstream file it names MUST be `03-code-review-pr.html`** — this slice's source, not slice 1's — and the repository it names MUST be the one the contract names. **Verify**: `--layer 4` gallery scanner passes its attribution check, including agreement between the named upstream file and the catalog entry's `source.file`, which reads `03-code-review-pr.html`. A header carried over from slice 1 with its filename unchanged passes nothing (FR-009).
- [ ] T009 Write the slot inventory as a single HTML comment placed immediately after the attribution header in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: **two lines**, one per slot, each reading `Slot: … | Fills: … | Source: …` in that order with no pipe inside any value. Use the two lines recorded verbatim in `spec.md` FR-011 — they already carry `feature-header`'s two-identifier instruction and, on the `hunks` line, all three obligations that must survive the first fill: keep a stable unique anchor on every hunk item, a hunk carrying no annotation says so in words, and a finding's content comes from the self-review block the workflow log writes. **Verify**: both slot names are filename-safe kebab-case and unique; the inventory carries none of the attribution header's labels or literals so the header stays the first comment a scanner recognises as one; both `Source:` values are members of the closed set as extended by T004 (`spec.md` and `git-diff`); and an agent reading the inventory alone can name both regions and what fills each (FR-014, FR-015, FR-016, FR-017, FR-017a, FR-042b, SC-012). The self-review obligation rides in `Fills:` rather than `Source:` because the workflow log's filename varies per spec, and putting it in `Source:` would require inventing a filename.
- [ ] T010 Sweep `speckit-pro/artifact-gallery/templates/annotated-diff.html` for prohibited constructs and drop any the upstream source uses: a base element, a reference beginning with two slashes and no scheme, an event-handler attribute, a `srcdoc` attribute, a form element with a submission target, and a `ping` attribute. Confirm no external reference loads a resource other than the brand typeface request carried inside `GALLERY-HEAD`, and no relative reference into a skills directory of the form the Codex payload build rewrites. **Verify**: `--layer 4` gallery scanner passes the prohibited-construct and external-reference scans (FR-007, FR-008, FR-010).
- [ ] T011 Verify the reviewability budget against the realized task and file scope before implementation proceeds, and record the result in the run record: the task list adds no file beyond the three in `plan.md` § Declared File Operations, so 1 production file and 13 total files stand. **Verify**: the counts match the declaration in `plan.md` § "Declared Figures"; the split decision is recorded as "this spec is the split, slice 3 is the named follow-up, no further split available"; no `Reviewability-Exception` pragma is claimed, because none of `refactor`, `infra`, or `upgrade` honestly describes net-new template work; and the plan-phase estimator's `pass` is recorded as a **known-blind diagnostic** with its reason beside it — `projected = production * 40`, the helper opens no file it counts, and all three declared entries fail `is_production_file`, so `production = 0` and `projected = 0`. Its green line is not evidence about this slice.

**Checkpoint**: The artifact is a structurally valid gallery file with both canonical blocks, an attribution header naming the right upstream file, and a complete two-line inventory. Region work can begin.

---

## Phase 3: User Story 1 - Read the diff with the review attached (Priority: P1) 🎯 MVP

**Goal**: A reviewer opens the artifact straight from a filesystem, with no server and no install, and reads a unified diff in which the hunks that need comment carry the reviewer's own annotation beside them, each finding's severity stated as a word, and jump links that take them from one finding to the next.

**Independent Test**: Open the shipped template from the filesystem with the network unavailable. Both hunks render with their sample content, one annotated and one clean, the annotated hunk's finding states a severity as a word, the jump link reaches the finding and puts focus on it, the browser console reports nothing, no load fails, and the theme control works. The only visible difference from an online render is typeface substitution.

**Sample content rule for every task in this phase**: representative and fictional, held to the minimum that demonstrates the region's shape — non-empty everywhere, expansive nowhere. Every region uses the **same** invented change, named once and reused (FR-020d).

**Why both CSS checkpoints live in this phase.** M1 and M2 both measure `css`, and CSS is the dimension `plan.md` names as the risk. Writing all CSS here — the diff CSS, then M1, then the objection and export CSS, then M2 — means the total CSS ceiling is proved **before a single line of the export routine is paid for**. The objection and export CSS styles controls US2 builds; authoring it here does not make US1 depend on US2, and US1 stays independently testable at the phase checkpoint.

### Implementation for User Story 1

- [ ] T012 [US1] Add the `feature-header` region to `speckit-pro/artifact-gallery/templates/annotated-diff.html` with its own marker pair: the invented change's identifier carrying `id="feature-id"` and its name carrying `id="feature-name"`, both **inside** the pair because an export may carry nothing the reader could not see. Place the artifact's own displayed title, reading exactly `Annotated Diff`, carrying `id="artifact-title"`, **outside every marker pair**, so no fill can delete it. Inside the pair, add FR-020e's one sentence saying in visible text that the content is invented, naming the invented change, reusing the muted-paragraph rule so it adds no CSS rule. **Verify**: all three ids resolve uniquely; `artifact-title` sits outside both marker pairs and the two feature ids sit inside `feature-header`'s; the notice is present and names the same invented change every other region uses (FR-010a, FR-011a, FR-020d, FR-020e).
- [ ] T013 [US1] **Compare the artifact's displayed title against its catalog entry's `title` by running a comparison, not by reading either.** Run the command recorded in `quickstart.md` § "The title comparison no test performs" against `speckit-pro/artifact-gallery/templates/annotated-diff.html` and `speckit-pro/artifact-gallery/manifest.json`. **Verify**: the command prints `EQUAL`, with zero characters of difference including case. **`MISMATCH` is a stop** — fix the artifact title, never the catalog, because the catalog value is fixed by ART-001 and FR-040 permits exactly one changed value and it is `status`. Record the result; T039 carries it into the acceptance evidence. **Nothing in the suite asserts this agreement.** Slice 1 shipped a title that differed from its catalog entry in case, every export therefore opened with a value the catalog did not carry, the whole suite passed green, and only independent review caught it. This task exists because a reading would have passed on slice 1 too (FR-010a, SC-008).
- [ ] T014 [US1] Build the `hunks` section chrome in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: the section, its heading **outside** the marker pair, the jump links, the grouping element **outside** the pair carrying a pinned `id` so the export routine can read it by that id, and exactly one `FILL:hunks:START` / `FILL:hunks:END` pair with the items inside it. Follow the shape `module-map` ships — `<div class="modules" id="module-list">` outside the pair, items inside carrying `id="modules-<slug>"`. Also add FR-019c's standing sentence naming the three row markers, as one muted paragraph placed **outside every fill pair**, because blank-means-context is a learned convention rather than a self-evident one and a fill that deleted the sentence would leave the diff unreadable to a first-time reader. **Carry across no upstream region beyond these two.** **Verify**: `--layer 4` fill-region checks report exactly **two** regions, `feature-header` and `hunks`; no pair encloses another; each pair delimits a whole subtree with no element opening on one side of a boundary and closing on the other; the grouping element and the standing sentence both survive a fill because both sit outside every pair; and **no third region exists** — a `diff-summary` slot was proposed and rejected on evidence and would cost 3 to 9 lines of the tightest headroom in the feature (FR-011, FR-012, FR-013, FR-019c, FR-020c).
- [ ] T015 [US1] Build **hunk 1, the annotated one**, inside the `hunks` region in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: an item that requires an end tag, carrying a stable unique anchor of the form `hunks-<file-path-slug>-l<start>` where the slug is the **whole file path** with every run of characters outside `a-z0-9` replaced by one hyphen and the edges stripped, and `<start>` is the hunk's new-file start line prefixed by `l`. Its heading carries the file path and the new-file line range and sits **outside** the scroll container. Ship seven diff rows, each rendering three cells in document order — line number, state marker, code — and one annotation placed **immediately after that hunk's rows inside the same item**, opening by naming in words the row or rows it comments on, and carrying a severity because it is a finding. **Verify**: the item requires an end tag, because the fill-region parser performs no implied closing and an unclosed item would report as nested and silently vanish from the region's top level; the anchor uses the whole path rather than the file stem and the start line rather than the range; no individual diff row is addressable as a fragment; and `--layer 4` R5 counts this item at the region's own top level (FR-019c, FR-019e, FR-020, FR-020c, FR-023b).
- [ ] T016 [US1] Build **hunk 2, the clean one**, inside the `hunks` region in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: the same item shape and anchor derivation, its heading outside the scroll container, six diff rows, and **words saying it carries no annotation**. **Verify**: the region ships **exactly two** hunks and no third — the validation's floor of two anchored items and this template's cap coincide at the same number for independent reasons, and a third hunk would add an instance rather than a kind at the cost of a whole hunk's markup on the tightest budget in the feature; both anchors are unique; the clean hunk reads as **deliberately clean** rather than broken, unfinished, or awaiting content, and it says so **in words** rather than leaving the space an annotation would occupy empty; and **no selector distinguishes it** — the words are the carrier, and a `.hunk.clean` rule would be a second carrier for something already carried (FR-020, FR-020a, FR-020b, FR-020c).
- [ ] T017 [US1] Prove each diff row's state marker is **literal text in the document and not CSS generated content**, and that the line-number cell sets `user-select: none`, in `speckit-pro/artifact-gallery/templates/annotated-diff.html`. **Verify by extracting the marker from the DOM, not by eye**: read the marker cell's text content for one added, one removed, and one context row and confirm each returns a literal `+`, `-`, or space; then confirm no CSS rule in the authored block uses a content property to place any marker. Generated content is placed on the clipboard by some engines and not others, and FR-019 requires the marker to survive a copy. Two limits are **recorded rather than solved**: `+` and `-` are punctuation and are not announced at default screen-reader verbosity, and the clipboard exclusion is not uniform — Chrome still carries unselectable text on the paste-and-match path — which is why T039 requires an **actual paste** of one added, one removed, and one context row rather than an assertion that it works (FR-019, FR-019c, SC-006).
- [ ] T018 [US1] Give **each** hunk's scroll container in `speckit-pro/artifact-gallery/templates/annotated-diff.html` `overflow-x: auto`, `tabindex="0"`, a role that accepts a name, and an accessible name naming its hunk. **Verify**: both containers carry all four; the hunk heading is a heading and sits **outside** the container, which is the one placement that supplies the accessible name, distinguishes the header from its rows by position rather than by fill, and removes the row-background paint problem a block row inside a scroller otherwise has; and the page itself never scrolls horizontally at any supported width, with overflow contained inside the container rather than pushing the page. **The `tabindex` is required rather than inferred**: Safari does not make a scroll container keyboard-scrollable without it and Chrome's automatic behaviour is conditional, so without it a keyboard reader cannot reach the right-hand side of a wide hunk. **All five `overflow-x: auto` containers already shipped in this gallery lack this, and this artifact is the first to do it correctly**; repairing the other five is out of scope and is recorded as a known gap. Two costs are accepted and recorded: two additional tab stops, and one named region per hunk announced whether or not it currently overflows (FR-019d, FR-035).
- [ ] T019 [US1] Render severity in `speckit-pro/artifact-gallery/templates/annotated-diff.html` as a **word** from the closed set `blocking`, `major`, `minor`, preceded by a **fixed label naming it as a severity** so a text-only reading cannot mistake it for emphasis. **Verify**: the severity is text, never a colour, a bar, a dot, or a glyph's fill; the annotation that only explains a hunk carries **no severity element at all**, which is what makes its absence unreadable as a fourth level below `minor`; and the severity vocabulary is the one this repository already reviews in. The single shared style rule this requires is written at T021 and checked at T021's verification — a selector branching on which word it is, is exactly where colour, weight, or fill re-enters as the ranking carrier, which is the defect this requirement exists to prevent and the one upstream commits (FR-019a, FR-019f, FR-033, SC-006).
- [ ] T020 [US1] Wire the jump links in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: each finding carries a stable `id` **and** `tabindex="-1"`, and each jump link is an **ordinary same-document link**. **Write no JavaScript for the focus move.** A fragment navigation moves focus when the target is focusable, so `tabindex="-1"` on each finding is the entire mechanism, it costs zero authored JavaScript and zero authored CSS, and FR-019e forbids scripting it. **Verify**: following a jump link moves **focus** to the target and not only the scroll position, so the next Tab stop is the one after the target rather than the one after the link; the authored script contains no focus call associated with a fragment; the kit's own focus-visible rule already covers the indicator because its selector list includes `[tabindex]:focus-visible`, so no focus CSS is authored; and no individual diff row is addressable, because per-row fragments would add roughly thirty tab stops between two hunks and satisfy no requirement. No shipped template links between its own repeated items today — verified at zero same-document fragment links across all five — so this is new behaviour for the gallery rather than a ported one (FR-019e, FR-034, FR-036).
- [ ] T021 [US1] Write the **page chrome and diff CSS** in `speckit-pro/artifact-gallery/templates/annotated-diff.html`, applying every reduction lever from `plan.md` § "The reduction levers, in order" from the start rather than after a failure. Carry over slice 1's page-chrome block and **do not** carry its `ul.points` rules or its before/after panel rules — this artifact ships no bullet list and no two-panel comparison, and those two blocks are 32 lines together. Author the twelve diff-specific blocks `research.md` § 3 enumerates one at a time, including `.sev` as **one rule serving all three severity words with no selector branching on which word it is**. **Verify**: a search of the authored CSS finds **no selector that branches on a severity word** — the check is a search, not a judgement; the subtle border token appears **nowhere** in the authored CSS and **no CSS comment names it**, because FR-032's check is a search and a comment naming the token fails it; no custom property is referenced that the embedded block does not define, because an undefined one fails silently and renders plausibly; **no heading typeface is named at all**, inheriting the block's own per-level assignments, since the token is `--rc-font-display` and applies only to the first two levels; every foreground and background pairing comes from the kit's audited set; upstream's own diff colouring is carried across **nowhere**; and nothing is authored that the kit already supplies — the monospace assignment on `code` and `pre`, the link styling, the `[tabindex]:focus-visible` rule, and the reduced-motion block are all inherited at zero cost (FR-018a, FR-019f, FR-031, FR-032, FR-032a, FR-033, FR-037, FR-038, FR-038a).
- [ ] T022 [US1] **Measurement checkpoint M1** — run the instrument recorded verbatim in `quickstart.md` § "The size measurement" against `speckit-pro/artifact-gallery/templates/annotated-diff.html` and record all four numbers. Use that instrument; **do not write a second one**. **Acceptance criterion: `css` ≤ 150.** **Verify**: the instrument's reported `css` figure is 150 or lower, and all four numbers are written into the run record against the label M1. **On failure: STOP.** Do not write the objection or export CSS, and do not begin any export work. Apply the six reduction levers in `plan.md` order and re-measure until the criterion is met. If all six are applied and `css` still exceeds 150, the ceiling has failed on evidence rather than on discipline; escalate to the operator with the measurement rather than continuing. **This is the load-bearing checkpoint**: it is the earliest moment the ceiling can fail and the cheapest moment to fix it, so a failure surfaces at roughly 150 lines written rather than at 750, and none of the copied export work has been paid for yet. The number is arithmetic rather than a round figure: page chrome 70 plus diff CSS 79 is 149. Sanity-check the instrument against `speckit-pro/artifact-gallery/templates/pr-writeup.html` any time you doubt it — expect `authored 735 | css 227 | js 334 | markup 174`.
- [ ] T023 [US1] Write the **objection and export CSS** in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: the disclosure, the labelled field, the controls, the status region, and the fallback field. **Write exactly one disclosure rule set.** The objection control is the only disclosure in this artifact, unlike slice 1 where `file-by-file`'s items were disclosures too, and writing a second set is the single largest avoidable cost in this ceiling. **Verify**: the subtle border token still appears nowhere and no comment names it; no status, action, or distinction is carried by colour alone; the kit's focus indicator is not suppressed without an equivalent replacement; no positive tab index is added and focus is trapped nowhere; and any motion this template itself introduces is suppressed under a reduced-motion preference (FR-031, FR-032, FR-032a, FR-033, FR-036, FR-037).
- [ ] T024 [US1] **Measurement checkpoint M2** — run the same instrument from `quickstart.md` § "The size measurement" against `speckit-pro/artifact-gallery/templates/annotated-diff.html` and record all four numbers. **Acceptance criterion: `css` ≤ 275.** **Verify**: the instrument's reported `css` figure is 275 or lower, and all four numbers are written into the run record against the label M2. **On failure: STOP.** Reduce and re-measure **before writing a single line of the export routine**; the shared disclosure rule set in T023 and the two shade rules in T021 are the first two places to look. The measured arithmetic behind this ceiling is 195 carry-over plus 79 diff-specific, which is 274.

**Checkpoint**: Both hunks render with their sample content, one annotated and one clean, and **all** authored CSS is inside its ceiling with both CSS checkpoints passed. US1 is independently testable now: open the file from a filesystem with the network off and read it. This is the MVP, and it satisfies the catalog entry ART-001 already shipped.

---

## Phase 4: User Story 2 - Hand the objections back (Priority: P2)

**Goal**: Having read a hunk, the reviewer attaches an objection to it, repeats for as many hunks as they want, then copies every objection out of the page in one action — either as a pull-request comment or as an instruction to paste into a coding agent.

**Independent Test**: Type an objection into one of the two hunks, leave the other empty, and invoke each export control. Each export carries exactly the one objection written, naming the hunk it attaches to, and carries nothing from the empty field.

**Depends on**: US1 complete. Every objection attaches to a hunk that must already exist and already carry its anchor.

**The routine is a transplant, not an authored routine.** Every line already exists in a shipped file. Base is slice 1's measured 334. Item derivation comes from `module-map`; the settle path comes from slice 1. **`module-map`'s `announce`, `reveal`, and `copy` must not be copied** — the whole of their 23-line difference from slice 1's *is* the currency guard, and taking `module-map`'s derivation together with `module-map`'s settle would reproduce, byte for byte, the defect present in all three pre-slice-1 templates.

### Implementation for User Story 2

- [ ] T025 [US2] Build the per-hunk objection controls in `speckit-pro/artifact-gallery/templates/annotated-diff.html` from one routine at load, following the shape the shipped objection controls use in `module-map` and `implementation-plan`: a native disclosure closed on load plus one labelled text field. Take `module-map`'s `mount(anchorId)` (42 lines), `mountAll()` (18), and `labelOf(anchor)` (12); `mountAll` reads the grouping container by its pinned id, walks its children, and collects the ones carrying an `id`, so **no value from the document is ever concatenated into a selector string**. Mount each control **immediately after the item it questions**, which is the placement the three older templates use. **Verify**: the disclosure's own control states **in text** whether its hunk currently carries an objection, and that text is recomputed on **every change to the field** — not once at mount, and not only when the disclosure is next toggled, because a summary fixed at mount reports "no objection recorded" over a hunk that carries one, which is worse than reporting nothing; the field's visible label is programmatically associated with it and **placeholder text is not used as the accessible name**, because it is not one and it vanishes the moment the reviewer types; the disclosure carries **no ARIA role, no `aria-expanded`, and no `aria-pressed`**, because its state is exposed natively and forcing a role here has been observed to remove the exposed state rather than add it; the label is read from the item's **first heading** rather than from the item's whole text, which for a hunk would drag every diff row and every jump link into the exported reference line; the state words reuse the shipped pair `Objection on <label>: no note recorded` **byte-identically** rather than cohering with this artifact's noun; and each control is reachable and operable by keyboard alone (FR-021, FR-021a, FR-021b, FR-023c, FR-023d, FR-023f, FR-030e).
- [ ] T026 [US2] Add the export chrome to `speckit-pro/artifact-gallery/templates/annotated-diff.html`: exactly one control per declared export kind — the entry declares `prompt` and `markdown` — labelled "Copy as prompt" and "Copy as Markdown", **destination, never mechanism**. Ship the export region **hidden** as an attribute on a container line that exists anyway, and reveal it from the routine that already runs at load, so the affordance appears only where scripting can perform it. Add exactly one live status region, present from load rather than created on demand, sitting **outside every fill region**. Add the selectable fallback field, focusable and not disabled, with an accessible name. **Verify**: with scripting unavailable the export controls do not appear while both hunks, their rows, their annotations, the severity words, and the standing sentence all stay readable; the status region survives every fill because a fill cannot reach it, and a status region a fill could delete would make every later failure silent; the reveal costs the single budgeted line; and no third export control ships, because the entry declares two (FR-022, FR-028, FR-043).
- [ ] T027 [US2] Implement the export serializer in `speckit-pro/artifact-gallery/templates/annotated-diff.html`. **Derive the items from the anchors present in the region at the moment of invocation** using `module-map`'s container walk and `recorded()` (15 lines), so a fill that adds or removes hunks is carried without a code change. **This is a real difference from slice 1**, which pinned a fixed list of six slot names because its regions were a fixed set of sections; a list slot has no fixed count and pinning one would break the first real fill. Replace slice 1's six-name `SLOTS` array and its seven-line comment with one slot constant and one list-id constant and a shorter comment. Emit the shape in `data-model.md` § 7: the artifact title line, the `Feature: <id> <name>` line read from live state with the fallback chain in the slice 1 contract, a blank line, the single lead line naming the kind, then one blank-line-separated block per non-empty objection whose reference line reads `<slot> / <item label>  (#<anchor>)` with **two spaces** before the parenthesis. Both kinds serialize the same structure and differ in exactly one line, and **neither emits markdown syntax** — the `markdown` kind names its destination, not its encoding. **Verify**: an objection in 1 of the 2 hunks produces exactly 1 block and zero placeholders or empty entries for the other; both lead lines are byte-identical to the two authored leads pinned in `spec.md` FR-030d, which vary from the shipped pair by their noun alone; the export walks only the non-empty fields; it is derived from live state at the moment of invocation and carries no value the reviewer could not have inspected on screen; and no value is concatenated into a selector string (FR-023, FR-023a, FR-023b, FR-023c, FR-024, FR-025, FR-026, FR-030a, FR-030d, SC-004).
- [ ] T028 [US2] Implement the empty state in `speckit-pro/artifact-gallery/templates/annotated-diff.html`: when no objection has been written, both exports still emit the header lines and then say **in text** that nothing was recorded, and **deny that this is approval**. **Verify**: both bodies are byte-identical to the objection-noun rows already pinned in `specs/art-003-final-pr-template-set/contracts/export-payload-contract.md` — this slice's noun is already the contract's noun, so the two empty-state bodies reuse byte for byte with no new row and no new wording; no empty or invented document is produced; and the realistic misreading of an empty export as approval is refused in the text itself (FR-025, FR-030, FR-030a).
- [ ] T029 [US2] Implement the clipboard failure path in `speckit-pro/artifact-gallery/templates/annotated-diff.html`, taking **all four** steps: reveal the same exported text in the selectable field; keep that field focusable and not disabled, give it an accessible name, and move focus to it; report the single failure message; and do **not** report success. **Verify**: the message is byte-identical to the one the shipped export-carrying templates already ship, is the **only** failure message, covers every failure mode, and asserts **no cause** — the artifact cannot tell a refused permission from an unfocused document from a browser policy from an absent interface, so naming one would be a guess presented as a diagnosis; **no second copy attempt** is made through any deprecated interface, because that attempt's result is ambiguous; every invocation **re-hides the fallback field before it attempts its copy**, so a later success never leaves an earlier failure's payload on screen beside a success message; and the rejection is handled so the browser console stays silent. Failing silently and reporting success are the same defect wearing different clothes: the reader believes they hold text they do not hold (FR-029, FR-029a, FR-030a, FR-030b, SC-005).
- [ ] T030 [US2] Implement the invocation-currency guard in `speckit-pro/artifact-gallery/templates/annotated-diff.html` by taking slice 1's `announce(token, message)` (20 lines) and `reveal(token, text)` (15) **whole**, together with its `copy` (30) and its currency-token declaration and comment. The check is scoped **by effect, not by path**: it sits where a status write or a fallback reveal actually lands, and every path reaches those effects through it. **Reproduce slice 1's four check sites** — the entry to the status write and the deferred callback it schedules, and the entry to the fallback reveal and the deferred callback *it* schedules. **Verify**: with two exports invoked before the first completes, only the later invocation's outcome is reported — the superseded one changes no status text, reveals no fallback text, and moves no focus; **both** settle paths are guarded, so a slow success resolving after a fast failure cannot overwrite the failure message with a success while the fallback field still holds the other kind's text. Path scoping is insufficient and this is a property of the routine rather than a hypothesis about it: the status write is deferred behind a short timer because the region is cleared and rewritten so a repeated identical message is announced a second time, and the focus move is deferred behind a longer one, **so a path that decides synchronously still lands asynchronously**, in a later turn, after a second invocation may already have completed and re-hidden the field. Scoping by effect makes the exemption unnecessary rather than merely safer. This defect is present in **all three** export-carrying templates shipped before slice 1 and this artifact must not reproduce it; repairing those three is out of scope (FR-027, FR-027a, FR-027b, FR-027c, SC-007).
- [ ] T031 [US2] Confirm no string literal inside the script in `speckit-pro/artifact-gallery/templates/annotated-diff.html` names the local-file scheme; feedback text says "opened from a filesystem" instead. **Verify**: the gallery scanner's URL-shaped pattern reports no external reference from a script string literal — it treats a literal opening with a scheme and a colon, or carrying a scheme followed by two slashes anywhere, as one. The clipboard call itself is not a scanned call site; the wording is. **Also confirm the theme requirement is discharged.** Nothing this slice authors needs the current theme: the pre-paint theme application and the theme control both arrive inside `GALLERY-HEAD` and are never authored (T007), and neither the objection controls nor the export routine branches on appearance. FR-039 is therefore expected to be vacuous here, and the check is that it stayed vacuous — search the authored script for any read of stored theme state and for any stored value placed into markup, a selector, or an executable position, and confirm there is none. Record the result either way; the requirement is discharged by a stated finding, not by silence. If some later refinement does need the theme, it must read the resolved attribute from the document root (FR-030c, FR-039).
- [ ] T032 [US2] **Measurement checkpoint M3** — run the same instrument from `quickstart.md` § "The size measurement" against `speckit-pro/artifact-gallery/templates/annotated-diff.html` and record all four numbers. **Acceptance criterion: `js` ≤ 345.** **Verify**: the instrument's reported `js` figure is 345 or lower, and all four numbers are written into the run record against the label M3. **On failure: STOP.** Reduce and re-measure before authoring another line. **This checkpoint exists because slice 1 did not have it.** Slice 1 declared its JavaScript at 288 on the reasoning that the component was "measured against three shipped implementations of the same routine and is a floor rather than an estimate", then shipped **334** — a 46-line, 16% overrun and the largest single estimation error in the slice, absorbed only because CSS and markup came in 69 lines under together. The same reasoning is available for this slice's measured 342 and is **not** being relied on again. Run it after the export routine's last line and before any further authoring; the authored total is caught separately at M4.

**Checkpoint**: Both user stories work. A reviewer reads two hunks, attaches an objection to either, and exports it two ways with a working failure path and a guard that all three older templates lack.

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: The catalog flip, the non-vacuity proof, the final measurement, the payload and docs regeneration, the acceptance evidence, and the PR review packet inputs.

- [ ] T033 Flip exactly one value in `speckit-pro/artifact-gallery/manifest.json`: the `annotated-diff` entry's `status`, from `planned` to `shipped`. Change no other value on this entry, no other entry, and no shared foundation file — not `SPA-CONTRACT.md`, not `brand-kit.css`, not `theme-toggle.html`, not the signal vocabulary, and not the export vocabulary. **Verify**: a diff of the manifest shows one changed line; `title` still reads `Annotated Diff`, `exports` still reads `["prompt","markdown"]`, and `source.file` still reads `03-code-review-pr.html`. **This value and the artifact file MUST land in the same commit.** The contract binds status and file presence in **both** directions: the file without the flip fails as an orphan, the flip without the file fails as a missing artifact, and neither may be committed alone. Rollback is the same pair moving back together. Expect the failure count to **rise** here before it falls at T036, because flipping activates checks that stay dormant while the entry reads `planned` (FR-040, SC-011).
- [ ] T034 Prove the fill-region validation binds this template rather than passing vacuously. Run the import probe in `quickstart.md` § "Prove the validation is not passing vacuously" against `tests/speckit-pro/unit/test-artifact-fill-regions.py`, then run the deliberate negative check and revert it: flip the entry in `speckit-pro/artifact-gallery/manifest.json` back to `planned` with the file still present and confirm the suite fails as an **orphan**. **Verify**: the probe prints `annotated-diff` in `FLOOR` with `("hunks",)`, `LIST_SLOTS["annotated-diff"]` equal to `("hunks",)`, and `git-diff` in the source set; the negative check fails as predicted; and the flip is restored to `shipped` with `git diff` showing the manifest back to its T033 state. **This is the one result a green suite can hide**, because the module resolves its per-template universe by intersecting the catalog with its floor, so a shipped template the floor does not name is never parsed at all and a port with no regions and no inventory would pass every check green (FR-041, SC-010).
- [ ] T035 **Measurement checkpoint M4** — run the same instrument from `quickstart.md` § "The size measurement" against `speckit-pro/artifact-gallery/templates/annotated-diff.html` a final time, after all markup is written and **before the pull request**. **Acceptance criterion: `authored` ≤ 750.** **On failure: STOP.** Reduce and re-measure; **escalate to the operator above 800** rather than trimming. **Verify**: all four numbers are recorded with the checkpoint each was taken at, ready for the PR body, so a reviewer reads a measurement rather than a claim. Headroom is 50 lines below the block and the bottom-up measurement is 738, so the ceilings carry 12 lines of slack. If any later task forces a code change — including remediation from T039 — re-run this checkpoint (SC-013).
- [ ] T036 Regenerate the plugin payload with `python3 scripts/refresh-release-artifacts.py`, because the gallery ships inside the payload and a new template changes shipped bytes on both platforms. **Verify**: `dist/claude/**` and `dist/codex/**`, the runner trust metadata, the installed-cache fixtures, and the payload evidence all update; none is hand-edited. This clears **two** of the three intermediate-red families — payload parity, and the release-readiness fixtures together with the bash-confinement guard. `--check` exits 1 on a correct but uncommitted regeneration and that exit must not be chased; it resolves on commit. CI's `artifact-consistency` job fails the pull request if this is skipped (FR-045).
- [ ] T037 [P] Regenerate the docs reference with `pnpm --dir docs-site reference:generate`, required because `tests/speckit-pro/unit/test-artifact-fill-regions.py` is a tracked `.py` file under `tests/speckit-pro/` and its change restales the generated docs-site test reference. **Verify**: the generated reference updates, and `refresh-release-artifacts.py` is confirmed **not** to cover this surface — its own help text says so, and skipping it fails `artifact-consistency` on an otherwise green local tree. A different generated surface from T036's payload, so this runs alongside it.
- [ ] T038 Record the colour pairings the artifact actually uses as part of the acceptance evidence, each traced to the audited row that clears it and to the role that row permits — body text, large text, or meaningful non-text. **Verify**: every pairing in the list appears in the artifact and every pairing in the artifact appears in the list. The record sits with the evidence rather than in the artifact on purpose: the audit lives in a header that does not ship and the artifact carries only token names, so without the list the audited-set requirement is a claim a reviewer can only take on trust — and recording it beside the render discharges the same obligation at zero cost to the line budget, which matters more here than on slice 1 (FR-031, FR-031a).
- [ ] T039 Record the manual render as acceptance evidence by walking `quickstart.md` § "Part 2 — The manual render, which no test can replace", opening `speckit-pro/artifact-gallery/templates/annotated-diff.html` **directly from the filesystem** with the developer console open before loading and the network unavailable. Run Scenario A, Scenario B, and Scenario C in full. **Verify**: for **each theme**, capture a screenshot of the full page, a screenshot of the console showing it empty, and the exported text from a one-objection run; capture the greyscale rendering and confirm added, removed and context rows are still tellable apart and every severity still reads as a word; capture the **actual paste** of one added, one removed, and one context row into a plain-text editor, on the ordinary path and on the paste-and-match path, confirming each pastes as a valid unified-diff line with the marker present and the line number absent; confirm Scenario A step 8, that a jump link moves focus and not only scroll position; confirm Scenario B steps 12 and 13, the concurrent-invocation checks that all three pre-slice-1 templates fail in both directions, which is the pair that proves the currency guard survived the transplant; and record the T013 title comparison result alongside. Any remediation this step forces sends the work back through T035 (FR-046, SC-001 through SC-007).
- [ ] T040 Run the full repository suite from the worktree root: `python3 tests/speckit-pro/run-all.py`. **Verify**: zero failures, and the passing total is an **increase** against the T001 baseline of **7379** rather than merely green. Layer 4 must show the gallery scanner and the fill-region validation both binding this artifact. This run rewrites `dist/**`; do not `git add -A` afterwards, and restore any unintended generated change with `git show HEAD:<path> > <path>` (FR-044, SC-010).
- [ ] T041 Assemble the PR review packet inputs for this slice. **Verify**: it carries what changed, why, non-goals, review order, scope budget, traceability, verification evidence, known gaps, and rollback notes. **Review order** puts the authored markup and the diff CSS first — that is where every decision a reviewer can disagree with lives — then the export routine, then the validation and catalog changes, and states that the 458 canonical lines are byte-verified copies read last or not at all. **Scope budget** carries all four measured numbers with the checkpoint each was taken at, plus the re-declared 750 and the note that the plan-phase estimator reported `pass` with `projected: 0` and is structurally blind here, so its green line is a known-blind diagnostic and not evidence. **Traceability** comes from the requirement-to-file map in `data-model.md` § 12. **Known gaps** name the eight slice 1 recorded that remain true, plus the one this slice adds — that nothing asserts an artifact's title agrees with its catalog entry's title — plus the two recorded in `plan.md` § Complexity Tracking, including that the five shipped `overflow-x: auto` containers still lack the `tabindex` this artifact is the first to carry. **Deferred work** names slice 3, the generation step in ART-010, the ready flip, and the unrepaired currency defect in the three older templates. **Rollback** is the catalog value returning to `planned` **together with** the file's removal, because the contract binds them in both directions. The pull request **bases on slice 1's branch, never on the default branch**, and no merge happens inside this run. Validate the exact final PR title through the repository release-readiness gate before creating the pull request, in the form `<type>(<lowercase-scope>): <plain English description>`.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies. All three tasks are independent of each other.
- **Foundational (Phase 2)**: Depends on Setup. **T004 is ordered first and is not `[P]`** — every later verification is judged against the literals it adds, so it must not be free to land late. T005 through T011 are strictly sequential; they are the same file. BLOCKS both user stories.
- **User Story 1 (Phase 3)**: Depends on Foundational. No dependency on US2.
- **User Story 2 (Phase 4)**: Depends on Foundational **and** on US1, because every objection attaches to a hunk that must already exist and already carry its anchor. This is the one cross-story dependency in the slice, and it is inherent rather than incidental.
- **Polish (Phase 5)**: Depends on both stories. T033 must precede T034 and T036.

### Within Each Phase

- T006 and T007 precede T008 and T009 only by convention of file order; all four touch the same file and are sequential.
- T012 and T014 precede T015 and T016 — the region shells must exist before the hunks fill them.
- T013 follows T012 immediately, because it is the cheapest moment to catch a title mismatch.
- T017 through T020 follow T015 and T016; they verify and refine markup those two tasks establish.
- T021 follows every markup task in US1, because the CSS is written against markup that exists.
- **T022 (M1) follows T021 and precedes T023.** This ordering is the point of the checkpoint: the diff CSS is measured before any objection or export CSS is written on top of it.
- **T024 (M2) follows T023 and precedes every task in Phase 4.** No line of the export routine is written until total CSS is proved inside its ceiling.
- T027 follows T025 and T026; T028 through T031 refine the routine T027 establishes.
- **T032 (M3) follows T031**, after the export routine's last line.
- **T035 (M4) follows all authoring**, including any remediation T039 forces.

### Parallel Opportunities

Four tasks carry `[P]`, and every one is justified by touching a genuinely different surface:

| Task | Surface it touches | Why it is genuinely disjoint |
|---|---|---|
| T001 | `dist/**` only, as a side effect of the suite run | The suite is **not** read-only — it rewrites `dist/**` on every run. It touches no path T002 or T003 touches, and any rewritten generated path is restored with `git show HEAD:<path> > <path>`. |
| T002 | the session scratchpad, outside the repository | Fetches upstream read-only; no repository file is touched and no upstream byte is staged. |
| T003 | `docs-site/` dependencies | Writes only gitignored dependency state. |
| T037 | the generated docs-site reference | A different generated surface from T036's payload; `refresh-release-artifacts.py` does not cover it. |

**T004 is deliberately not among them**, unlike slice 1's equivalent. Everything else in Phases 2 through 5 is sequential, because **one HTML file means most work is sequential** — 27 of the 41 tasks edit `speckit-pro/artifact-gallery/templates/annotated-diff.html`.

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Complete Phase 1: Setup.
2. Complete Phase 2: Foundational — the literals first, then the file. Blocks both stories.
3. Complete Phase 3: User Story 1, ending at the M2 checkpoint.
4. **STOP and VALIDATE**: open the file from a filesystem with the network off and read both hunks. This satisfies the catalog entry ART-001 already shipped, and a reader who never writes an objection down has still received value.
5. M1 and M2 must both pass before any Phase 4 work begins.

### Incremental Delivery

1. Setup + Foundational → the artifact is a valid gallery file.
2. US1 → a reviewer can read the diff with the review attached. MVP.
3. US2 → a reviewer can hand their objections back.
4. Polish → the flip, the regenerations, the evidence, the packet.

The slice ships as **one** pull request, based on slice 1's branch. The increments are checkpoints inside it, not separate deliveries, because a self-contained HTML artifact cannot be divided across two pull requests and still render from a filesystem.

### The stop rule

Four checkpoints, each a hard gate with a numeric criterion:

| Checkpoint | Task | Runs after | Passes when |
|---|---|---|---|
| **M1** | T022 | the page chrome and the diff CSS, **before any objection or export CSS** | `css` ≤ 150 |
| **M2** | T024 | the objection and export CSS | `css` ≤ 275 |
| **M3** | T032 | the export routine | `js` ≤ 345 |
| **M4** | T035 | all markup, before the pull request | `authored` ≤ 750 |

A failure means stop, apply the reduction levers in `plan.md` order, and re-measure. It does not mean continue and reconcile later. **M1 is the load-bearing one**: it is the earliest moment the ceiling can fail, and putting it before the export work means a failure surfaces at roughly 150 lines written rather than at 750. **M3 is the new one**: slice 1 ran three checkpoints, its uncovered component was the export routine, and that component overran by 46 lines and 16%.

---

## Notes

- `[P]` tasks touch different surfaces and have no dependency on an incomplete task.
- Every task states its verification inline. A task with no stated check is not done.
- The measuring instrument is recorded verbatim in `plan.md` § "The instrument, reused rather than rewritten" and `quickstart.md` § "The size measurement". **Use that one.** A second instrument would measure a different quantity and the ceilings would stop meaning anything.
- **The suite is not read-only.** Every run rewrites `dist/**`. Never `git add -A` after a suite run; stage paths by name, and restore a rewritten generated path with `git show HEAD:<path> > <path>`.
- `--layer 4` alone overcounts. Judge a red against the full run.
- Never write an absolute filesystem path into any artifact this slice produces. A tree-wide privacy scan fails on one.
- Never hand-edit an embedded canonical block, a generated payload, an installed-cache proof, or a generated reference page.
- The catalog value and the artifact file land together in every direction, including rollback.
- Nothing in this slice may be shaped to suit slice 3, and no merge happens inside this run.
- The full suite must pass at the end, above the 7379 baseline: `python3 tests/speckit-pro/run-all.py`.
