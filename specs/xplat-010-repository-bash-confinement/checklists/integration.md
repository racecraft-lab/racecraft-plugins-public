# Integration Quality Checklist: Repository Bash Confinement and CI Dispatch Guard

**Purpose**: Validate the *integration-seam* requirements before implementation — the places where partial or out-of-order delivery breaks CI, the shipped-runner/repo-side boundary, or the release pipeline
**Created**: 2026-07-08 by `/speckit-checklist integration` (autopilot Phase 4, domain 2 of 4)
**Feature**: [spec.md](../spec.md)
**Validates**: spec.md + plan.md together (post-Plan). This is an integration-quality checklist ("unit tests for the requirements at the seams"), distinct from the requirements-quality checklist in [requirements.md](./requirements.md); both coexist per the `/speckit-checklist` append convention.

**Domain focus** (from the workflow prompt): (1) suite-manifest consumed identically by `run-all.py` and the shipped suite gate; (2) `pr-checks.yml` dispatch swap and branch-protection required-check renames; (3) release-workflow ordering (release-please publish, then composer body rewrite); (4) transitional `.sh` support lifetime (PR 2 → PR 10) and out-of-order-merge consequences.

## A. Suite-Manifest Dual-Consumer Contract

- [ ] CHK001 Is it specified that `run-all.py` and the shipped suite gate read the **same single** manifest as the one source of truth, rather than two independently-maintained lists? [Consistency, Spec §FR-007, §Key Entities: Suite Manifest]
- [ ] CHK002 Is the shipped gate's roster derivation (`DEFAULT_SUITE`/`EXTENDED_SUITE`/`ALLOWED_LAYERS` derived solely from the manifest, failing closed when it is absent or unreadable) specified? [Completeness, Spec §FR-007]
- [ ] CHK003 Is a deterministic drift-guard test required to assert the gate's advertised roster **and** dispatch kinds match the manifest exactly? [Completeness, Spec §FR-007]
- [ ] CHK004 Is each dispatch-kind's consumer behavior defined (`python-module` → `run-layer-scripts.py`; `internal-check` → in-process in the suite gate; `shell-legacy-transitional` → temporary `.sh` dispatch)? [Clarity, research §D3, Contract suite-manifest.schema.json §dispatch]
- [ ] CHK005 Is cross-consumer agreement bounded — because both `run-all.py` and the gate derive selection from the one manifest (§FR-007) and `run-all.py` additionally reproduces the prior runner's flag/scope behavior (§FR-006) — so the two consumers cannot silently select different layers? [Consistency, Spec §FR-006, §FR-007]
- [ ] CHK006 Is the shipped-gate-reads-a-repo-only-manifest seam reconciled — the manifest lives under `tests/speckit-pro/` (never shipped) while `gates/suite.py` ships, so an installed-context invocation with the manifest absent is dispositioned by the fail-closed rule? [Coverage, Spec §FR-007, §Assumptions]

## B. CI Dispatch Swap & Branch-Protection Integration

- [ ] CHK007 Is the `pr-checks.yml` residual `bash tests/speckit-pro/check-toolchain.sh` dispatch swap to a direct Python-runner invocation specified with its PR-stack boundary (PR 5)? [Completeness, Spec §FR-026]
- [ ] CHK008 Is the same-PR update of the self-referential workflow validator required whenever `pr-checks.yml`/`release.yml` jobs change (so the sentinel validator never lags the workflow it validates)? [Consistency, Spec §FR-026, plan §Constraints]
- [x] CHK009 Are the branch-protection required-check changes enumerated **completely and consistently** across the stack? [Resolved → Spec §FR-018, §FR-026] (Previously gap-marked: PR 12 mandated the manual branch-protection addition of `validate-release-note` (§FR-022), but PR 11's addition of NEW required Linux preflight checks carried no equivalent callout, and PR 5's swap was not stated to be required-check-neutral. Remediated by extending **FR-018** with the PR 11 branch-protection callout plus a stack-wide branch-protection-change ledger, and **FR-026** with the statement that PR 5's `run:`-step swap renames no required check.) [Consistency, Spec §FR-018, §FR-022, §FR-026]
- [ ] CHK010 Is the new `validate-release-note` required status check's manual branch-protection addition **and** `release-note/skip` label creation required in its landing PR? [Completeness, Spec §FR-022]
- [ ] CHK011 Is it specified that the confinement guard (PR 10) rides existing required checks with no new CI job, no new required status check, and zero branch-protection change? [Clarity, Spec §FR-005, §Clarifications Session 2]

## C. Release-Workflow Ordering & Composer Integration

- [ ] CHK012 Is the publish-then-compose ordering enforced by a `needs:` dependency on the publishing job (not merely sequential steps)? [Completeness, Spec §FR-024]
- [ ] CHK013 Is the composer's own-job least-privilege permission specified (job-level `permissions: {contents: write}` only, never inheriting the publishing job's `actions: write`/`pull-requests: write`)? [Completeness, Spec §FR-024]
- [ ] CHK014 Is the composer's gating on the component-scoped `speckit-pro--release_created` output — with the tag/body outputs as inputs and release-ID resolution by tag — specified? [Clarity, Spec §FR-023, research §D9]
- [ ] CHK015 Is composer idempotency specified (appendix always from the release action's body output; re-dispatch without a new release is gated off)? [Completeness, Spec §FR-023]
- [x] CHK016 Is a composer-**job** failure that occurs **after** the release is already published dispositioned — does the workflow surface the failure for visibility while the published release stands with the raw release-please body, and what is the recovery path given a fresh `release.yml` dispatch is gated off when no new release is created? [Resolved → Spec §FR-023, §Edge Cases] (Previously gap-marked: FR-023 covered idempotency and the composition-failed diagnostic but not the post-publish partial-success case. Remediated by extending **FR-023** — publication stands with the raw body, the job surfaces a red run for visibility, recovery is re-running the failed composer job (not a gated-off fresh dispatch) — and adding a matching **Edge Cases** bullet.) [Edge Case, Spec §FR-023]
- [ ] CHK017 Is the deliberate scope asymmetry consistent — `validate-release-note` shape-gates only feat/fix PRs while the composer defensively harvests every merged PR since the last tag? [Consistency, Spec §FR-022, §FR-023]

## D. PR-Stack Sequencing & Transitional Lifetime

- [x] CHK018 Is the no-Bash/no-`jq` suite guarantee reconciled with the transitional window, during which the manifest points not-yet-ported layers at `.sh` files (`shell-legacy-transitional`) that require Bash to dispatch? [Resolved → Spec §FR-006, §SC-002] (Previously gap-marked: FR-006/SC-002 stated the full suite runs with only Python present, unconditionally, without scoping the claim to the stack's end-state or specifying no-Bash-platform behavior mid-stack. Remediated by scoping **FR-006** to the PR-stack end-state and requiring a Bash-absent platform to skip still-unported `shell-legacy-transitional` layers with an explicit diagnostic while CI runs every layer, and adding the matching end-state note to **SC-002**.) [Conflict, Spec §FR-006, §SC-002, Contract suite-manifest.schema.json §dispatch]
- [x] CHK019 Is the `shell-legacy-transitional` lifecycle **enforced** rather than only documented — is there a requirement that no such dispatch kind survives after PR 10, and that every manifest `scripts[].path` resolves to an existing file (the drift-guard asserts gate-vs-manifest agreement, so it does not catch a surviving transitional entry or a dangling path)? [Resolved → Spec §FR-007] (Previously gap-marked: the "none remain after PR 10" invariant lived only in research §D3 and the data model, enforced by nothing; the drift-guard's gate-vs-manifest scope misses a surviving transitional entry or a dangling path. Remediated by extending **FR-007** with two independent manifest-integrity invariants — every `scripts[].path` MUST resolve to an existing file, and no layer may carry `shell-legacy-transitional` after PR 10 (terminal absence asserted, not merely documented).) [Coverage, research §D3, Spec §FR-007, §Key Entities: Suite Manifest]
- [ ] CHK020 Is the atomic same-PR swap (port + manifest flip + `.sh` deletion together) required so no layer ever runs with zero coverage during the stack? [Completeness, Spec §FR-012]
- [x] CHK021 Is the enforcement mechanism and failure mode of an **out-of-order** merge specified per ordering constraint — i.e., which constraints are CI-enforced/fail-closed (a violating merge goes red) versus reviewer-merge-discipline, and what breaks if violated? [Resolved → Spec §Reviewability Budget: Split decision] (Previously gap-marked: the ordering constraints were listed but their enforcement mechanism and out-of-order consequences were unstated. Remediated by extending the **Split decision** with per-constraint enforcement: PR-2-before-3–10 and PR-10-after-3–9 are CI-self-enforcing via FR-012 (manifest edit targets a not-yet-existing file) and FR-005 (guard live-scan), while PR-11-last and PR-13-early are review-ordering preferences whose violation only degrades evidence, never breaking `main`.) [Coverage, Spec §Reviewability Budget: Split decision]
- [x] CHK022 Is the case of two shipped-runner PRs (among PRs 2/7b/8/9/10/13) in flight simultaneously dispositioned — the proof-hash rows in `speckit-pro-runner.manifest.json` conflict, and the later-merging PR must rebase and re-run the payload/proof regeneration ritual rather than hand-merge the hashes? [Resolved → plan §Constraints] (Previously gap-marked: the plan bound the regen ritual to the shipped-runner PRs individually but never addressed two of them in flight at once. Remediated by extending **plan §Constraints** — because all six rewrite the manifest sha256 proof rows and `dist/**`, a later-merging PR MUST rebase and re-run the full ritual so hashes match the post-merge tree; conflicting proof rows MUST NOT be hand-merged, since a hand-resolved hash matches no real payload state and fails release-readiness verification.) [Coverage, plan §Constraints, §Declared File Operations]
- [x] CHK023 Is the payload/proof regeneration ritual bound to the specific shipped-runner PRs (2/7b/8/9/10/13), with release-readiness evidence regenerated last and home-directory sanitization? [Resolved → plan §Constraints, tasks T016/T071/T078/T085/T098/T126]
- [ ] CHK024 Is the guard-activation dependency specified — the confinement guard (PR 10) turns on only after PRs 3–9 have cleaned every scanned surface, so its own live scan is the mechanism that keeps it green? [Clarity, Spec §FR-005, research §D11]

## E. Cross-Seam Consistency

- [ ] CHK025 Is the shared `<label>: {passed}/{total} passed` headline output contract — parsed by `run-all.py`, the shipped suite gate, and `run-layer-scripts.py` — specified so a port cannot drift the format and break a downstream parser? [Consistency, Spec §FR-006, §FR-010, research §D5]
- [ ] CHK026 Is the two-seam boundary specified (repo-side never-shipped tooling under `tests/speckit-pro/`, `scripts/`, `.claude/hooks/`, `.github/workflows/` vs. the shipped `speckit_pro_runner/` whose byte changes trigger the regen ritual)? [Clarity, plan §Structure Decision, §Project Type]
- [ ] CHK027 Are the native `check_layer5`/`check_layer7`/`check_layer8` retirements bound to their own port-PR boundaries with no permanent equivalence shim, so each layer has exactly one implementation of record? [Completeness, Spec §FR-008, §Clarifications Session 1]
- [ ] CHK028 Is the estimator restoration's caller-contract integration bounded — the restored `estimate-spec-size` op (PR 13) matches the input/output shape the grill-me and speckit-prd skills already send, pinned by the existing golden fixtures? [Assumption, Spec §Assumptions, §FR-025, research §D7]

## Domain Checklist Notes

- Coverage summary: 28 items across five integration-seam clusters (manifest dual-consumer; CI dispatch/branch-protection; release-workflow ordering; PR-stack sequencing/transitional lifetime; cross-seam consistency). Traceability ≥ 80% satisfied — every item carries a `[Spec §…]`, `research.md §…`, `plan §…`, or contract reference.
- Focus areas selected: the four workflow-prompt seams plus cross-seam consistency. Depth: standard (formal post-Plan gate). Audience: reviewer (PR) + autopilot G4 gate.
- Emphasis per the prompt: transitional `.sh` lifetime (CHK018, CHK019, CHK020, CHK024) and out-of-order-merge consequences (CHK021, CHK022) received dedicated items, since end-state functional requirements systematically under-specify mid-stack timing.
- Gap remediation (this run): 6 gaps found and closed, all grounded in the repository's own ratified artifacts (no external research needed — these were internal-consistency and mid-stack-timing gaps forced by existing decisions). CHK009 → **FR-018** (PR 11 branch-protection callout + stack-wide branch-protection ledger) and **FR-026** (PR 5 swap is required-check-neutral). CHK016 → **FR-023** + **Edge Cases** (post-publish composer-job failure disposition and recovery). CHK018 → **FR-006** + **SC-002** (no-Bash guarantee scoped to end-state; transitional-window skip-with-diagnostic). CHK019 → **FR-007** (manifest path-existence + terminal absence of `shell-legacy-transitional` after PR 10). CHK021 → **Split decision** (per-constraint out-of-order enforcement). CHK022 → **plan §Constraints** (simultaneous shipped-runner PRs: rebase + re-run the regen ritual, never hand-merge proof rows). Post-remediation `count-markers` (gaps): spec 0 / plan 0 / checklists 0. No items remain open for consensus.
