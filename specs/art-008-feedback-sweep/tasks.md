# Tasks: Feedback Sweep, slice 1 of 2 — the checkpoint

**Input**: Design documents from `specs/art-008-feedback-sweep/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md

**Tests**: Required. The spec asks for TDD explicitly (FR-008a, FR-008b pin a
golden-fixture corpus), so every helper branch and every fixture path gets its
failing test before its implementation.

**Reviewability**: The plan's hand-derived budget is **~630 reviewable LOC
(515–830), 7 production files, 15 authored files, 1 primary surface**, and it
is the plan-time figure, superseded by the note below. At that figure it is
**WARN on reviewable LOC** (630 against 400) and **WARN on production files**
(7 against 6), with the high end at **810–830** crossing the 800 block. T014 is
the mandatory checkpoint that forces the operator decision before
implementation starts. The file-set claim is restated against the live figure in
the second superseding note below.

**First superseding budget note, itself superseded.** The figures above are the
plan-time ones and are left as written. After the artifact verification repair
(**595–910**) and the trust-boundary remediation, which made **FR-012f** and
**FR-007g** into one helper surface (T081 through T087) at **110 to 170
reviewable lines**, the range was **705–1080, midpoint near 890**, with
**production files at 7** and **authored files at 16** — FR-004d's `.gitignore`
entry (T088), which is repository configuration rather than production. That
paragraph is left as written because it records what was true when it was; the
note below supersedes it.

**Second superseding budget note — the live figure, and the one T014 decides
against.** The live figure has one home, `spec.md`'s **second** Reviewability
Budget superseding note, and this note repeats it without re-deriving it. The
consumer-scoping pass (T094 through T109) ships `sweep-classifier` and
`sweep-analyst` on both platforms and carves them out of the Layer 5
no-allowlist rule, for a delta of **415 to 640** reviewable lines and a live
range of **1120–1720, midpoint near 1420**, which crosses the 800 block at the
midpoint. **Production files move from 7 to 12** — the two Claude agent
definitions, their two Codex mirrors, and the `install.py` line the closed
`REQUIRED_CODEX_AGENT_NAMES` inventory forces — which crosses the 8-file block.
**Authored files move from 16 to 22**: those five production paths plus the
Layer 5 validator. Twenty-two is over the warn of 15 and under the block of 25,
so authored files stay a warn. **Both blocks are size-only and both are
OPERATOR-ACCEPTED**, recorded at T014 as lever (b). No task below expands the
file set past the plan's Declared File Operations block, which the
consumer-scoping pass updated to carry all twelve production entries.

**Organization**: Tasks are grouped by user story so each story can be
implemented and tested independently.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: `[US1]`, `[US2]`, `[US3]` — user-story phases only
- Every path is repository-relative

## Path Conventions

Plugin source under `speckit-pro/`, repository-only validation under
`tests/speckit-pro/`, planning artifacts under
`specs/art-008-feedback-sweep/`. No new directories under plugin source; one
new fixture directory, `tests/speckit-pro/unit/fixtures/feedback-sweep/`. The
sweep's runtime byproducts go under `specs/<feature>/.process/feedback-sweep/`,
ignored and removed (FR-004d); no task writes a file there.

## Two hard boundaries

1. **Neither `SKILL.md` may be edited.** The Codex autopilot skill body is
   **7997 words against a hard 8000-word Layer 1 cap — three words of
   headroom** (plan, "Two files deliberately absent from this block"). No task
   below adds a line to `speckit-pro/skills/speckit-autopilot/SKILL.md` or
   `speckit-pro/codex-skills/speckit-autopilot/SKILL.md`, and none may be
   added. An implement-phase agent that "helpfully" registers the new helper in
   either skill index fails Layer 1.
2. **The eight generated paths are regenerated, never hand-edited.** The four
   `dist/` copies, the runner `.sha256` and `.manifest.json`, and the two
   installed-cache copies are covered by T074 and T075 alone. No authoring task
   below names any of them.

---

## Phase 1: Setup (design-artifact corrections)

**Purpose**: Four settled decisions changed what gets built and the contract
still shows the pre-decision shape. Correcting the artifacts first means the
implementation tasks read off a correct interface rather than carrying the
correction in prose.

- [ ] T001 [P] Change the export record from the singular `export.matched_line` to `export.matched_lines`, a list of 1-based integers in ascending order, in the Response block and the Recognition section of `specs/art-008-feedback-sweep/contracts/sweep-pr-feedback.md`. Acceptance: FR-007f's "reports **all** matched line numbers in ascending order rather than a single one" is satisfied by the contract, and the security checklist's CHK049 conflict is closed. A body carrying two registered leads is an ordinary workflow this feature's own design invites, not an adversarial edge case. Add `anchors_dropped`, a non-negative integer, beside `anchors` in the Response block's export record, and record the anchor grammar `#[a-z0-9-]{1,64}` and the sixty-four cap in the Recognition section (FR-007e).
- [ ] T002 [P] Change `export.matched_line` to `export.matched_lines` (array of integers, ascending) in the swept-comment field table at `specs/art-008-feedback-sweep/data-model.md`. Acceptance: data-model and contract agree; no artifact still describes a single matched line (FR-007f). Bound `export.anchors` the same way — each entry the run after `#` of a value matching `#[a-z0-9-]{1,64}`, at most sixty-four — and add the `export.anchors_dropped` row (integer, the count of dropped anchors).
- [ ] T003 Add the `self_login` non-empty rule to "Preconditions the helper validates" in `specs/art-008-feedback-sweep/contracts/sweep-pr-feedback.md`: the value MUST be a non-empty string **after surrounding whitespace is stripped**; absent, empty, or whitespace-only returns `invalid_input`. Add the matching Diagnostics row. Acceptance: FR-006b's validation half is itemized rather than implied by the Input-rules "Required: Yes" column. Depends on T001 (same file).
- [ ] T004 Add `feature_dir` to the request's `inputs` block and its Input-rules row in `specs/art-008-feedback-sweep/contracts/sweep-pr-feedback.md`, recording that it **arrives as an explicit input and is never inferred**, because the one inference mechanism available keys off a branch-name pattern this feature's own branch (`art-008-feedback-sweep`) does not match. Record the FR-012b rule-2 write-point check **and the redaction surface** — FR-012f's three outbound legs and FR-007g's analyst-payload leg — as named surfaces of this operation, and give the redaction surface its own Request, Response, and Diagnostics blocks, itemized per leg. In: `leg` from the closed set `amendment`, `log_row`, `reply`, `analyst_payload`; a non-empty `comment_id`; on the three outbound legs `lines`, an array of strings; on the analyst-payload leg `text` (the capture-truncated body as captured), `truncated` (the parse's echo), and `matched_lines` (ascending 1-based integers, empty for an ordinary comment). Out: on an outbound leg `lines` and a `redactions` array of `{rule, line}` events, **one line in, one line out**; on the analyst-payload leg the returned `text` (the block) and a `report` carrying `budget_bytes`, `truncated`, `leads_removed`, `spans_withheld`, `spans_unclosed`, and the per-span list of kind, first line, line count, and unclosed. Fix the literal delimiter and statement lines FR-007g leaves to the contract; the placeholder grammar is fixed in FR-007g and the contract repeats it without owning it. Correct the Reply-templates section's marker to the FR-015b form — prefix `<!-- speckit-pro:feedback-sweep`, then the answered comment's id, then `-->` — shown as line 1 alone with the disposition starting on line 2. Diagnostics: `invalid_input` on a leg outside the set, an empty comment id, a non-array `lines`, or a `matched_lines` index past the last line, the last naming the comment id. Acceptance: FR-012c's explicit-input requirement is on the interface, not only in the spec, **and T083 and T084 have a contract shape to fixture against rather than prose** — the surface's inputs, outputs, and diagnostics are itemized per leg the way the parse's are. Depends on T003 (same file). **Scope note**: the contract as shipped carries neither the input nor either surface; see the Known Interface Gap section below before starting.
- [ ] T005 [P] Register `tests/speckit-pro/unit/test-feedback-sweep-parse.py` in `tests/speckit-pro/suite-manifest.json` with `baseline: null`, matching the shape of the existing `test-speckit-pro-read-only-helpers` entry. Acceptance: the new test is selected by `python3 tests/speckit-pro/run-all.py`.
- [ ] T006 [P] Create `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json` and `tests/speckit-pro/unit/fixtures/feedback-sweep/expected-envelopes.json` as empty, well-formed skeletons keyed by case name. Acceptance: the directory is named for durable behavior, never for the spec id; both files parse as JSON.
- [ ] T094 [P] Create `specs/art-008-feedback-sweep/contracts/sweep-classifier-output.md`, copying the header shape of the sibling `contracts/sweep-pr-feedback.md`: the `# Contract: …` H1, the `**Feature**` and `**Date**` line, a `**Mode**` line, `---`, then Request, Response, and rules-table blocks. It fixes two records. **The classifier's structured record**: `class` from the closed set `amended`, `answered`, `deferred`, `no action`; `target` one of `spec.md`, `plan.md`, `tasks.md`, or `null`; `reason`, a single-line string of at most 512 bytes as UTF-8 carrying neither pipe nor newline. **The analyst's structured edit**: `file` from the same three-name allowlist, `anchor` bounded at **512 bytes** as a verbatim excerpt of the target file's current bytes, and `replacement`, bounded at 8192 bytes per line — the figure `data-model.md` and the sibling contract already fix for reviewer text — and at a whole-replacement cap the contract states, chosen so the assembled request stays under the runner's 32 KiB string limit. Record that both strings reach the **existing** FR-012f redaction surface before any use, the `reason` on the `log_row` leg and the `replacement` on the `amendment` leg: **the leg set stays closed at four**, because a fifth leg is `read_only.py` code this feature does not budget and anything outside the four returns `invalid_input`. Record that the record and the edit are the whole of what crosses back, so no comment body returns from either agent. Diagnostics: a class outside the four, a target outside the allowlist, an over-cap reason, an over-cap anchor or replacement, an anchor that resolves zero times or twice, and a return that is not the record — each a malformed agent return the run reports rather than repairs. Acceptance: T096 and T103 fixture against a shape rather than prose, the way T004 gives T083 and T084 one. This is a planning artifact and declares no production file. Different file from T001, T003, and T004, so parallel-safe.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The helper must exist, be registered on all seven touch points,
and be reachable by the harness before any story branch can be tested.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [ ] T007 Append `"sweep-pr-feedback"` to `EXPECTED_HELPERS` (line 55) and to `NO_BASH_ANCESTOR` (line 83) in `tests/speckit-pro/unit/test-speckit-pro-read-only-helpers.py`. Acceptance: the test now FAILS on the missing registry entry and the missing fixture record — that failure is the red state the rest of this phase turns green. The helper is new behavior with no Bash ancestor, so `registry.py` records `None` for the script and `python_only` as the comparison mode.
- [ ] T008 Insert one record for `sweep-pr-feedback` into `tests/speckit-pro/unit/fixtures/read-only-helpers/fixture-manifest.json` **at the array position matching its position in `EXPECTED_HELPERS`**. Acceptance: both sibling assertions pass — `fixture_ids == EXPECTED_HELPERS` compares **in order** (line 361) while the registry-dispatch assertion compares against `sorted(EXPECTED_HELPERS)` (line 238). The two differ deliberately; satisfy both. Depends on T007.
- [ ] T009 [P] Create the canonical request fixture `tests/speckit-pro/unit/fixtures/read-only-helpers/requests/sweep-pr-feedback.json` carrying `schema_version`, `request_id`, `helper_id`, `operation`, `mode: read_only`, and the four inputs `workflow_file`, `self_login`, `feature_dir`, `pr_observation`. Acceptance: `PYTHONPATH=speckit-pro python3 -m speckit_pro_runner < tests/speckit-pro/unit/fixtures/read-only-helpers/requests/sweep-pr-feedback.json` reaches the helper. Use `PYTHONPATH=speckit-pro`, never the installed plugin cache — a run through the cache reports a tree that is not the one you edited.
- [ ] T010 Add the `sweep_pr_feedback(inputs, repo_root)` skeleton to `speckit-pro/speckit_pro_runner/helpers/read_only.py`, modeled on `resolve_autopilot_stage` (line 1472): takes an orchestrator-supplied observation, classifies it offline, reports without deciding. Acceptance: returns a `stdout_json` envelope with `tool`, `surfaces_read`, `counts`, `candidates`, `excluded`. It runs no `gh`, touches no network, writes no file, and assigns no class.
- [ ] T011 Add the three registration touch points in `speckit-pro/speckit_pro_runner/helpers/read_only.py`: the allowed-inputs map entry `{"workflow_file", "self_login", "feature_dir", "pr_observation"}` beside line 256, the argument-derivation branch beside the `resolve-autopilot-stage` branch at line 341, and the dispatch-table entry beside line 4466. Acceptance: contract Registration-checklist rows 2, 3, and 4 are satisfied. Depends on T010 (same file).
- [ ] T012 [P] Add one `HelperEntry` for `sweep-pr-feedback` to `speckit-pro/speckit_pro_runner/helpers/registry.py`, matching the `resolve-autopilot-stage` shape at lines 181–188, with `None` for the script and `python_only` as the comparison mode. Acceptance: contract Registration-checklist row 5; the registry-dispatch assertion passes.
- [ ] T013 Write the harness manifest diagnostics text for the helper in `tests/speckit-pro/unit/fixtures/read-only-helpers/fixture-manifest.json`. Acceptance: **no remediation or rollback string contains the substring `bash` in any casing** — the harness manifest rejects it. Depends on T008.
- [ ] T014 **Reviewability checkpoint — operator decision required before Phase 3.** Record in the workflow file which lever was taken against the plan's budget: midpoint **~630 reviewable LOC**, high end **810–830 which crosses the 800 block**, production files **7 against a warn of 6 and a block of 8**. **Take the decision against the live figure, not that plan-time one**: its only home is `spec.md`'s **second** Reviewability Budget superseding note, which reads **1120–1720, midpoint near 1420** — the artifact-verification repair (595–910), then the trust-boundary remediation's **110 to 170** lines of helper code and reference prose (705–1080), then the consumer-scoping pass's **415 to 640** — so **the midpoint crosses the 800 block**, not only the high end. **Lever (b) is taken, and the decision is the operator's, recorded as such.** The consumer-scoping pass puts production files at **12** — the four sweep agent definitions across both platforms, plus the `install.py` edit the closed `REQUIRED_CODEX_AGENT_NAMES` bundle forces — which crosses the **block at 8**, so both halves of the budget are now over a block. **Both are OPERATOR-ACCEPTED**, on the ground that the trust boundary is not separable from the feature: F-1 and F-2 are the feature's own consumers reading attacker-controllable text, and a slice that ships the reader without the scoping ships the vulnerability. **Precedent: PRSG-013 recorded a size-only block at 1800 reviewable LOC across 78 files and the run continued.** Lever (a) — defer the serialization-family registry rows `feature-flags`, `prompt-tuner`, `triage-board`, saving 15 to 30 lines at the cost of FR-007b — stays available and unexercised; lever (c), re-slice, is refused for the same reason the plan refused the read-path-only split. Acceptance: the block is recorded as accepted with its reason and its precedent; the plan's rejected split (read-path-only 1a) is not silently revived, because it produces a checkpoint that reads feedback and acts on none of it.

**Checkpoint**: Helper registered on all seven touch points, harness green on
registration, budget decision recorded. Story work can begin.

---

## Phase 3: User Story 1 — The sweep reads and classifies draft-PR feedback (Priority: P1) 🎯 MVP

**Goal**: Read both comment surfaces, keep only write-capable authors,
recognize artifact exports by registered lead sentence, skip already-logged and
self-reply comments, and give every survivor exactly one class.

**Independent Test**: Point the sweep at a draft pull request carrying a
trusted plain comment, a trusted exported markdown block, an untrusted comment,
a comment already recorded from a prior run, and a resolved thread. Confirm the
run reports exactly the trusted, unrecorded, unresolved items as classified
candidates and names every excluded comment with its exclusion reason.

### Tests for User Story 1 ⚠️

> **Write these FIRST and confirm they FAIL before implementing.**

- [ ] T015 [US1] Create `tests/speckit-pro/unit/test-feedback-sweep-parse.py` with the golden-fixture harness: load `comment-corpus.json`, run each case through the helper, compare against `expected-envelopes.json`. Acceptance: the file runs standalone as `python3 tests/speckit-pro/unit/test-feedback-sweep-parse.py`; it is named for durable capability, never for the spec id.
- [ ] T016 [US1] Add corpus cases pinning **every registered sentence in both shapes** to `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json`: the verbatim payload shape with the lead on line four behind `Artifact: <title>`, a feature line, and a blank line; and the same paste header-trimmed. Acceptance: FR-007, FR-008a. Depends on T015.
- [ ] T017 [US1] Add corpus cases for normalization and truncation: a body delimited with carriage returns, a body at the 8192-byte budget with `truncated` true, and a body **over** budget expecting `invalid_input` naming the offending comment id. Acceptance: FR-008, FR-008a; the helper validates rather than silently re-truncating, because the runner's `BOUNDED_TEXT_INPUT_BYTES` rejects the whole request rather than the offending string. Depends on T016.
- [ ] T018 [US1] Add one corpus case per excluded author-association value — CONTRIBUTOR, FIRST_TIMER, FIRST_TIME_CONTRIBUTOR, MANNEQUIN, NONE — each expecting `untrusted_author`, plus a ninth-value case expecting `invalid_input` as a malformed observation rather than an untrusted author. Add one **admitted** case per allowed value too — OWNER, MEMBER, COLLABORATOR — each expecting a candidate rather than an exclusion, so all eight values of the closed enum are pinned in the direction the filter actually decides them. FR-008a names the five excluded values as a minimum; without the three admitted cases an allowlist that wrongly rejected `MEMBER` would pass every fixture here, because nothing else in the corpus varies the association across the trusted set. Acceptance: FR-005, SC-004, FR-008a's "each of the five excluded association values". Depends on T017.
- [ ] T019 [US1] Add the self-reply corpus cases: the marker at position 0 with a matching author expecting `self_reply`; the same marker with a **different** author expecting a candidate; a **quoted** reply whose marker is not at position 0 expecting a candidate; and the marker carrying a **different comment id** after its fixed prefix, asserting the FR-006 anchor still matches on the prefix. Acceptance: FR-006, FR-006a, FR-008a; anchoring is what keeps a reviewer who quotes a sweep reply to disagree with it visible. Depends on T018.
- [ ] T020 [US1] Add the `self_login` validation cases: empty string and whitespace-only, each expecting `invalid_input`. Acceptance: FR-006b's validation half; an empty account matches **no** real comment author, so the author condition is permanently false and **no comment is ever excluded as a self-reply** — the rule is disabled, not narrowed to its marker half. Depends on T019.
- [ ] T021 [US1] Add the recognition-edge corpus cases: a shared empty-export sentence expecting `template_ambiguous` true and `template_id` null; one recognized export on **each** of the two surfaces; a body carrying **two** registered lines asserting both appear in `matched_lines` in ascending order; an export whose anchor is malformed — `(#Phase 2)`, an uppercase letter and a space — asserting `anchors` is empty and `anchors_dropped` is 1; an export carrying sixty-five conforming anchors, asserting `anchors` holds the first sixty-four in body order and `anchors_dropped` is 1; a comment whose author cannot be resolved; and a comment carrying no registered sentence expecting `export` null. Add one case per **serialization-family** template — `triage-board`, `feature-flags`, `prompt-tuner` — because those three carry **no lead sentence at all** and so are reached by a recognition path none of the cases above exercises. Their paste opens with a three-line header block, `# <Name> Export` / `Artifact: <template-id>` / `Export kind: markdown`, and the template id is read from the second line rather than inferred from a sentence. Assert each resolves its own `template_id` with `template_ambiguous` false, and that a body carrying the header lines in the wrong order is **not** recognized. Acceptance: FR-007a, FR-007b, FR-007e's anchor bound, FR-007f, FR-008a. Depends on T020.
- [ ] T022 [US1] Add the already-logged cases: a comment id present in the Feedback Sweep Log expecting `already_logged`, a resolved thread expecting `thread_resolved`, and a workflow file carrying **no** Feedback Sweep Log expecting an empty skip set (the first-sweep case). Add a log row whose comment-id cell cannot be read, expecting a stop. Acceptance: FR-009, FR-009a. Depends on T021.
- [ ] T023 [US1] Add the derive-from-manifest registry test to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: read `speckit-pro/artifact-gallery/manifest.json` and the template sources, derive every template the manifest says exports in every kind it declares, and assert the registry matches. Pin the skip list as exactly `["uat-walkthrough"]`, which the manifest declares as exporting both kinds but which has **no template file**. **Skip only on both conditions — named AND file still absent.** A template that goes missing is not on the list, so it fails; and if `uat-walkthrough` later ships its file it stops being skipped and must be derived, because it declares a `prompt` kind and a name-only skip would leave that imperative lead unregistered (FR-007c). Acceptance: FR-008a; a reworded lead or a new exporting page fails this test instead of silently disabling recognition. **The test reads templates and edits none**, so it crosses no non-goal and triggers no payload regeneration.
- [ ] T024 [US1] Add the two trust-boundary assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`. First: **no comment body appears anywhere in the parse's output** — assert the candidate and excluded records carry id, surface, author, association, truncation flag, and export metadata and **no body field at all**, so a later field addition cannot quietly reintroduce the leak. This is scoped to the parse envelope: the redaction surface returns text by design, and T082 is where its output is asserted. Second: an excluded comment's body and every registered line **never appear in an assembled analyst payload**, asserted against a captured payload. Store the captured payloads and captured commands as sibling top-level keys inside `expected-envelopes.json` rather than creating an undeclared fixture file. Acceptance: FR-008b. Depends on T022.
- [ ] T081 [US1] Add the shaping corpus cases to `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json`, each a request to the redaction surface's analyst-payload leg — body, comment id, `truncated`, `matched_lines` — with its expected returned block and report stored under the same case name in `tests/speckit-pro/unit/fixtures/feedback-sweep/expected-envelopes.json`: a plain body with no spans; a closed backtick fence with an info string; a tilde fence closed by a longer run; a four-backtick fence a three-backtick line does not close; a closer line carrying trailing text, which does not close; an HTML comment inside one line with prose on both sides; a multi-line HTML comment with prose before the opener and after the closer; an unclosed fence and an unclosed HTML comment; **the overlapping-span body** (`<!-- draft note`, a fence line, `--> keep this`, a fence line, `The real objection is X.`), which FR-007g's scan resolves to exactly one block; a fence containing `<!--`; an HTML comment containing a fence line; backticks following `-->` on the same line, which are not an opener; `<!-->`, which does not close; a fence whose info string contains `<!--`, which is one fence span and no comment span; a 200-byte info string cut to 32; an info string whose 32nd byte would split a character; forty minimal fences; an indented code block, which passes through; **the body truncated inside a fence** with `truncated` true, whose report and statement line both say truncated and both count one unclosed span; a body over 8192 bytes whose fence closes past the budget, which the surface cuts first and then withholds as unclosed; a body whose 8192nd byte falls inside a multibyte character, cut on the boundary; a body at budget carrying the parse's `truncated` flag, which the report carries through; a carriage-return body with `matched_lines` `[4]`, which removes the lead only when the normalized array is indexed; two matched lines `[4, 6]`; a matched line that sits inside a fence, counted as removed and then withheld; a `matched_lines` index past the last line, returning `invalid_input` naming the comment id; a non-matched line with two trailing spaces, preserved byte for byte; an imperative sentence written as plain prose, which reaches the block unchanged; and a body line identical to the closing delimiter line, which passes through unchanged. Acceptance: FR-007g, FR-007f, FR-008a, SC-005. **The expected blocks are the surface's own output, reviewed and committed, never typed by hand** — a test that compares a typed string to itself executes nothing, which is the defect the producer closes. The parse-side at-budget and over-budget bodies stay in T017; what this task adds for truncation is the surface's own bound and its flag. Depends on T022.
- [ ] T082 [US1] Add the shaping assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: run every T081 case through the redaction surface via the runner, with `PYTHONPATH=speckit-pro`, and compare the returned block and report **byte for byte** against `expected-envelopes.json`. Then assert across every case, independent of the golden copy: the seeded span text is absent from the block and its placeholder is present; every placeholder sits **between** the opening and closing delimiter lines; both delimiter lines carry the case's comment id; the counts on the statement line equal `spans_withheld`, `spans_unclosed`, `leads_removed`, and `truncated` in the report; no placeholder exceeds 96 bytes and no info-string echo exceeds 32; every returned body is valid text and the over-budget cases' bodies are at most 8192 bytes of reviewer text; the indented-block, trailing-whitespace, plain-prose, and delimiter-shaped cases return their body unchanged; and the overlapping-span case returns the one block FR-007g defines, with a two-entry span list naming the comment span as closed and the fence span as unclosed. Acceptance: FR-007g, FR-007f, SC-005. The captured-payload keys T024 stores remain for FR-008b's second assertion only; nothing in this task asserts against a hand-written payload. Depends on T024 and T081.
- [ ] T088 [US1] Add the byproduct directory's self-ignore write, the root `.gitignore` entry, and their tests. In both phase-execution references, make the sweep's first write into `specs/<feature>/.process/feedback-sweep/` a `.gitignore` containing exactly `*`, before any byproduct, so the directory ignores itself in any consumer repository; add to `tests/speckit-pro/unit/test-feedback-sweep-parse.py` a scratch-repository test that initializes a throwaway git repository with no root ignore, creates the directory the way the sweep does, writes one byproduct, and asserts `git add -A --dry-run` names nothing under it, going red when the self-ignore write is skipped. The root entry `specs/*/.process/feedback-sweep/` is already present at the end of the root `.gitignore`, under a one-line comment saying the directory holds feedback-sweep transport byproducts the sweep writes and removes; then add to `tests/speckit-pro/unit/test-feedback-sweep-parse.py` a test that reads the root `.gitignore` relative to the test file, with no absolute path, and asserts that exact line is present, so removing the line is a red test. Acceptance: FR-004d's first fixture; the directory the sweep writes to is ignored by a committed entry a fresh clone carries, unlike the per-clone `.git/info/exclude` entry the pull-request packet directory relies on. `.gitignore` is the sixteenth authored file and is repository configuration, not production. Depends on T082.
- [ ] T095 [P] [US1] Add the untrusted-input carve-out to `tests/speckit-pro/layer5-tool-scoping/validate-tool-scoping.py`: the tuple `UNTRUSTED_INPUT_CONSUMERS = ("sweep-classifier", "sweep-analyst")` beside the existing role tuples at line 29, and one new test method **appended to `TEST_METHOD_ORDER`** (line 49) — `build_suite()` iterates that tuple alone, so a method left off it never runs and passes by never executing. Three assertions. **(1)** Members are exempt from `test_operator_tool_surface_no_tools_allowlist_pinning`'s no-allowlist rule; that test loops every `agents/*.md`, so the skip branch must exist **before** either agent file lands or a suite-wide test fails on the new `tools:` line. **(2)** Each member pins **exactly** its allowlist — `sweep-classifier` `Read`, `sweep-analyst` `Read, Grep, Glob` — read with `_yaml_field(agent_file, "tools")`, split on commas, stripped, and compared as a **set by equality**, never containment — the shape FR-008c fixes, which is red both on adding a tool and on dropping one while staying silent about ordering, which no requirement pins. **(3)** Membership is asserted exactly: `assertEqual(UNTRUSTED_INPUT_CONSUMERS, ("sweep-classifier", "sweep-analyst"))` and no member also in `OPEN_EXECUTORS`, so adding an open executor to the tuple fails. Members also deny `Agent`, `TeamCreate`, `SendMessage`, and `Skill` in `disallowedTools`, in the `READ_ONLY_ROLES` style at line 187. Assert `sandbox_mode` is `read-only` for both TOMLs **without adding them to `CODEX_READ_ONLY_ROLES`**, whose sibling assertions pin `gpt-5.5` and an effort band this design never specified. **Assert each member's file exists before reading a field from it** — `_disallowed_tools` on an absent path raises, and an ERROR at the US1 checkpoint reads as a broken test rather than the pending work it is. Put the rationale in the module docstring at line 2: capability inheritance is right for an agent acting on trusted input and wrong for an agent reading attacker-controllable text; this is the only exemption, and pinning its membership is what keeps it the only one. Acceptance: FR-008c, FR-011b, SC-015. Written red it fails on both absent agent files; the classifier half goes green at T098 and the analyst half at T104, and **that analyst half is the one red state crossing a phase boundary** — named here rather than discovered there. The count in `tests/speckit-pro/parity/bash-to-python/validate-tool-scoping-baseline.txt` is not enforced at run time, so new subtests need no baseline edit. Different file from the parse test, so parallel-safe.
- [ ] T096 [US1] Add the classifier dispatch assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`, against captured dispatches stored as a sibling top-level key inside `tests/speckit-pro/unit/fixtures/feedback-sweep/expected-envelopes.json`, beside the captured commands T024 stores and the captured surface calls T092 stores. Per case: **exactly one `sweep-classifier` dispatch per candidate and none for any excluded comment**, the count **derived from the case's expectations, never typed beside them**; each dispatch carries **one** body, byte-identical to the block the redaction surface's `analyst_payload` leg returned for that candidate, so an unsanitized body reaching the classifier is a red test; and no dispatch in any case names `codebase-analyst`, `spec-context-analyst`, `domain-researcher`, or `consensus-synthesizer`. Per returned record: `class` is one of the four values, `target` is one of the three artifacts or `null`, `reason` is at most 512 bytes and carries neither pipe nor newline, and a record failing any of those is reported as a malformed agent return rather than mapped onto a class. Then the identity: the reason the orchestrator carries into the `Disposition` cell and into the run report is **byte-identical to the `log_row` response for it**, taken before FR-013's escaping — a report assembled from the orchestrator's pre-call copy fails here. Acceptance: FR-010a, FR-008a's captured-call paragraph extended to the classifier, SC-014's orchestrator half. Depends on T043 and T088; serial with every other task that edits the parse test file.
- [ ] T097 [US1] Add the piped-observation assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`, against the captured commands T024 stores: the helper request reaches the runner **on stdin from the `gh` read** — the captured argv shows the pipeline and names no request-file path — and no byproduct file any captured command writes carries a raw comment body, tested by searching each write for that case's seeded body strings. **Scoped to the observation path rather than to files in general**: the reply bodies FR-004b passes by path legitimately sit on disk and carry the redaction surface's output, not raw text. Acceptance: FR-004d, FR-010a's piped-observation half; a run that spools the observation to a file before parsing fails here rather than passing T090, which asks where a file sits and not what is in it. Depends on T096.

### Implementation for User Story 1

- [ ] T025 [US1] Implement request validation in `sweep_pr_feedback()` in `speckit-pro/speckit_pro_runner/helpers/read_only.py`: `pr_observation.ok` must be the JSON literal `true` (a truthy non-`true` value is not a successful read, per the `observation_pull_requests` precedent); `surface` closed to `review_thread` and `pr_conversation`; `author_association` closed to the eight GitHub values; every body at most 8192 bytes as UTF-8; `workflow_file` readable. Acceptance: T017 and T018 go green; each failure returns `invalid_input` naming the offending comment id. Depends on T014.
- [ ] T026 [US1] Implement `self_login` validation in the same helper: non-empty after stripping surrounding whitespace, otherwise `invalid_input`. Acceptance: T020 goes green. The helper cannot go further than presence — its contract forbids reaching the network, so it has no second independently sourced value to compare against; verification is the orchestrator's job through provenance, not the parse's through checking (FR-006b). Depends on T025 (same file).
- [ ] T027 [US1] Implement the workflow-file skip-set read: parse the Feedback Sweep Log's comment-id column and nothing else. A file with no such table yields an empty skip set. **A row whose comment-id cell cannot be read stops the run** rather than being guessed at, because an unreadable key is indistinguishable from an absent one and the two guesses fail in opposite directions — reading it as absent re-processes a handled comment, reading it as present skips an unhandled one. Acceptance: T022 goes green; FR-009, FR-009a. Depends on T026 (same file).
- [ ] T028 [US1] Implement line-ending normalization (CRLF and CR to LF) and per-comment truncation reporting. Acceptance: the same observed comment data always yields the same candidate set; T017 goes green; FR-008, SC-005. Depends on T027 (same file).
- [ ] T029 [US1] Implement the author-association allowlist — OWNER, MEMBER, COLLABORATOR — **ahead of everything else in the helper**, and return `excluded` records carrying `reason: untrusted_author`. Acceptance: candidate records carry id, surface, author, association, truncation flag, and export metadata and **no body**, so an untrusted comment's text is absent from the helper's output by construction rather than by an orchestrator remembering to drop it (plan, Trust Boundary mechanism 1). The allowlist is a **proxy** for write access, not a permissions check. T018 goes green; FR-005, FR-008b. Depends on T028 (same file).
- [ ] T030 [US1] Implement the two-condition self-reply test: the fixed HTML-comment prefix `<!-- speckit-pro:feedback-sweep` matched **anchored at position 0** — the prefix only, since the answered comment's id and the closing `-->` follow it (FR-015b) — AND an exact match between the comment author and `self_login`. Both conditions are required. Every self-reply exclusion is reported in `excluded` with `reason: self_reply`, so a marker collision drops a candidate **visibly**. Acceptance: T019 goes green; FR-006, FR-006a. Depends on T029 (same file).
- [ ] T031 [US1] Build the export lead registry in `speckit-pro/speckit_pro_runner/helpers/read_only.py`: 14 lead sentences (7 note-payload templates × 2 kinds), the 6 distinct empty-export sentences, and header identities for the 3 serialization-family templates, each entry carrying template id and kind. Cover **every shipped template that declares an export, in every kind it declares**. Acceptance: T023 goes green; FR-007a, FR-007b. **No shipped gallery template or payload copy is edited** — recognition is by registry, not by template change. Depends on T030 (same file).
- [ ] T032 [US1] Implement recognition: match registered sentences as **whole-line exact matches against the body's first ten lines**, after normalizing line endings and stripping trailing whitespace. The lead is not the first line — a verbatim paste puts it on line four — and the ten-line window survives a reviewer trimming the header and a template later adding one. Report `template_id` null with `template_ambiguous` true when the matched sentence is declared by more than one template. Parse anchors under FR-007e's grammar: the parenthesised value ending an export line is kept when the whole of it matches `#[a-z0-9-]{1,64}`, the record carries the run after the `#`, at most sixty-four are kept in body order, and every other one is dropped and counted in `anchors_dropped`. Acceptance: T016 and T021 go green; FR-007, FR-007a. Depends on T031 (same file).
- [ ] T033 [US1] Implement `matched_lines`: report **every** matched registered line number in ascending order, never the first alone. Acceptance: T021's two-lead case goes green; FR-007f. Removing only the first would leave the second sitting inside the delimited block, which is the first-match sanitization failure that recurs across input-validation defects. Depends on T032 (same file).
- [ ] T034 [US1] Assemble the response envelope and assert the invariant **`counts.observed == len(candidates) + len(excluded)`**. `candidates` are trusted, unrecorded, non-self-reply comments in the order observed, with `export` null when no registered line matched. `excluded` carries exactly one reason per comment from the closed set `untrusted_author`, `self_reply`, `already_logged`, `thread_resolved`. Ordering follows the observation; no set iteration reaches the output. Acceptance: nothing is dropped without appearing on one of the two lists, which is what backs SC-001; FR-008, SC-005. Depends on T033 (same file).
- [ ] T035 [US1] Add the Phase 7 setup sweep sequence to `speckit-pro/skills/speckit-autopilot/references/phase-execution.md`, inserted **ahead of** "Phase 7 Setup: Open the Implementation-Notes Record" (line 1210). Acceptance: FR-001. The sweep **adds no row to the Workflow Overview table** and changes neither the phase-coverage guard's governed phase-id list, the stage-to-phase map, nor the workflow template (FR-002). Hold the reference to the sequence rather than restating the spec's rationale in it — the budget depends on it.
- [ ] T036 [P] [US1] Mirror the sweep sequence into `speckit-pro/codex-skills/speckit-autopilot/references/phase-execution-codex.md`, inserted **ahead of** the line "**Open the implementation-notes record before the first task is dispatched.**" (line 933). Acceptance: FR-001, FR-003 — identical behavior for the same input, at roughly 70% of the Claude reference's length, which is the measured ratio between the two reference sets. Different file from T035, so parallel-safe.
- [ ] T037 [US1] Document the two `gh` reads in both phase-execution references: **every review thread whose resolved flag is false and every pull-request conversation comment**, never review summary bodies; both **paginated to exhaustion**; both requesting the `authorAssociation` field explicitly, because no shipped query requests it today and FR-005's filter has no input unless this read supplies it. **No comment text reaches a shell argument in either direction** — reads pass their query by file or by structured argument. Acceptance: FR-004, FR-004a, FR-004b, SC-009. This corrects the nearest shipped precedent, which caps at a fixed page with no pagination and interpolates comment text into a command string. Depends on T035 and T036.
- [ ] T038 [US1] Document the all-or-nothing observation rule in both phase-execution references: the two reads are **one observation**, succeeding only when both surfaces are read to exhaustion. Three failures fall under it — one surface readable and the other not, a page failing partway through pagination, and output that cannot be parsed. **A failed observation is discarded rather than swept**: zero rows, zero replies, zero commits, and a stop. Nothing needs unwinding because every read precedes every write. Acceptance: FR-004c, SC-011. Depends on T037.
- [ ] T039 [US1] Document `self_login` provenance in both phase-execution references: the orchestrator reads the account from the **live authenticated session at call time**, never from configuration or a remembered value, the same way FR-004a requires the author-association field be read fresh. Acceptance: FR-006b's provenance half; no shipped reference documents how the orchestrator learns its own login, so nothing today guarantees the value arrives correct. Depends on T038.
- [ ] T040 [US1] Document the **classifier dispatch loop** in both phase-execution references: it iterates **`candidates` and nothing else**, and for each candidate dispatches **exactly one `sweep-classifier`**, handed the sanitized delimited block the redaction surface returns for that candidate (FR-007g, T083), the closed class vocabulary, and nothing else. **The orchestrator reads no comment body on any path.** Classification moves out of it entirely: what returns is the structured record `{class, target, reason}` that `contracts/sweep-classifier-output.md` fixes, and the orchestrator consumes the two enums and the bounded reason only. No path enumerates the observation directly, and no path reads a body for the orchestrator's own judgment. Acceptance: plan Trust Boundary mechanism 2 and the F-1 remediation — mechanism 1 filters an envelope and nothing reads around it, because the orchestrator is never handed a body. **The orchestrator keeps `Bash`; the control is that a body never reaches it, which is by construction rather than by enforcement, and the reference says so.** FR-008b's second assertion and T092's per-candidate dispatch count are the fixtures. Depends on T039.
- [ ] T041 [US1] Document the four-value classification rule in both phase-execution references **as the vocabulary the dispatch hands `sweep-classifier`, not as orchestrator judgment**: every trusted, unrecorded comment takes **exactly one** class from the closed set amended, answered, deferred, no action, and the classifier returns exactly one. The **comment** is the unit, so a recognized export carrying several distinct objections still yields one class, one row, one reply. When one comment's objections warrant different classes, **`amended` wins**, and every non-dominant objection is named in the `reason` and so reaches the disposition and the reply. **Recognition never forces a class**, the one exception being the empty-export form, which carries no objections and takes `no action`. **These rules are stated once, in the classifier's prompt contract, and the references restate only the vocabulary and the dispatch** — the reference is the caller, the contract is the rule, and the budget depends on not writing them twice. A returned class outside the set, or a record missing a field, **stops the run** as a malformed classifier response rather than being mapped onto a class. Acceptance: FR-010, FR-007d. The tie-break is fixed rather than stylistic because FR-003 rules out any non-fixed tie-break. Depends on T040.
- [ ] T042 [US1] Document the recognized-export analyst payload in both phase-execution references: for a recognized comment the payload is the helper's export record — template id, kind, anchors — **plus the body with every line named in `matched_lines` replaced in place by `[registered export lead removed]`**, the export record standing beside the block rather than inside it. The remainder is **delimited and labelled as reviewer-supplied data**, never concatenated into the prompt as instruction. **The removal is performed by the redaction surface, not by the reference** (FR-007g step 3, T083): the reference hands the parse's `matched_lines` to the surface and forwards the block it returns, and the carriage-return corpus case is what pins that the normalized array is the one indexed. Acceptance: FR-007c, FR-007e, FR-007f. A registry entry that only tags the comment while the raw body still reaches the analyst does **not** satisfy FR-007c. **Delimiting is the strongest in-prompt layer; removal is defence in depth — if cost ever forces one out, removal goes and delimiting stays.** Both are model-layer controls, and nothing deterministic stands behind them on the forward path — the helper classifies deterministically and forwarding discipline is the orchestrator prose T040 documents, checked by FR-008b's second fixture — so neither is a reason to relax FR-005. FR-007g's span replacement ranks with removal, not with delimiting. Depends on T041.
- [ ] T083 [US1] Implement the analyst-payload leg of the redaction surface in `speckit-pro/speckit_pro_runner/helpers/read_only.py` and document the orchestrator half in both phase-execution references. **The surface is a second named surface of `sweep-pr-feedback`, under the discriminator T004 settles — not a second registered operation** — and this task lands its shared plumbing: the allowed-inputs entry for the surface's fields, its validation branch, and the leg dispatch, which T086's three outbound legs then extend. **The leg executes FR-007g's five steps in its one order**: normalize line endings; bound at 8192 bytes on a character boundary, a no-op on a conforming input and a cut otherwise, with `truncated` reported as the input flag or the surface's own cut; replace each `matched_lines` entry in place with `[registered export lead removed]`, returning `invalid_input` naming the comment id for an index past the last line; one left-to-right span scan — fence and HTML-comment openers, earliest by byte offset wins, closers as FR-007g defines them, no nesting, an unclosed opener running to the end of the body, a fence placeholder replacing opener line through closer line and a comment placeholder replacing exactly `<!--` through `-->`; then the frame — opening delimiter line with the comment id, the fixed statement line carrying the counts, the body, closing delimiter line with the id. Placeholder grammar and the 32-byte info echo as FR-007g fixes them; the report carries `budget_bytes`, `truncated`, `leads_removed`, `spans_withheld`, `spans_unclosed`, and a per-span list of kind, first line, line count, and unclosed. **The reference half is the call and the assembly, nothing more**: for every body forwarded to a consensus analyst, call the surface with the candidate's id, its capture-truncated body as captured, and the `truncated` and `matched_lines` the parse returned for it; hand the returned block to the analyst **unchanged**, beside the export record for a recognized comment and alone for an ordinary one; and carry `truncated` and `spans_withheld` from the report into the disposition cell and into the reply's fixed last line under FR-015 (T059). The reference restates no scan rule — those live in code and in the corpus — because the budget depends on it. Acceptance: FR-007g, FR-007e's payload, FR-007f's removal, FR-008b, SC-005; T082 goes green. Depends on T042 and T004.
- [ ] T043 [US1] Document the convergence invariant in both phase-execution references and add its fixture assertion to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: a run's **work set** is the comments that pass the trust filter, are absent from the log, and are not excluded as self-replies. **Every run either shrinks that set or leaves it unchanged; no run may grow it.** Any future rule that writes to either comment surface must be tested against this. Name the one path that does not shrink: a comment whose consensus round returns a human-review outcome takes no class and writes no row, so it is in the set again next run and stops that run too — bounded by a human rather than by a counter, and **no attempt counter is introduced** because a per-comment counter would need the state-file mirror FR-013 forbids. Acceptance: FR-006a, FR-006c, SC-013. Depends on T083.
- [ ] T098 [US1] Create `speckit-pro/agents/sweep-classifier.md`. Frontmatter: `name`, `description`, `tools: Read`, `disallowedTools: Skill, Agent, TeamCreate, SendMessage`, a positive `maxTurns`, and a non-empty `effort` — the last two because `test_session_shape_metadata` reads them on every `agents/*.md`. Body: the three capability-pointer markers `capability-discovery.md`, `grounding.md`, and `Capability path:`, which `tests/speckit-pro/layer1-structural/validate-capability-pointer.py` requires of every non-excluded agent on both runtimes. **The markers live in body prose and never in the returned record** — the validator is a substring search over the file's whole text (`:75-100`), so prose satisfies it, while the record stays exactly four fields and a fifth is malformed under FR-010a. An editor who "fixes" the subtest by adding a `Capability path:` field to the record breaks the contract instead. The agent holds `Read` and nothing else, so its capability path resolves to local repository reading every time, and a question it cannot ground that way is reported as ungrounded rather than guessed; the closed class vocabulary; the structured record T094 fixes; and the rule that it receives **one** sanitized, delimited body and returns the record and nothing else. No `mcp__` token in frontmatter or prose. **Then run the tools probe**: dispatch the agent once with `tools:` present and empty, record the runtime's response verbatim in the workflow file, and if an empty list resolves, tighten **both** this frontmatter and T095's pinned tuple to it inside this task; if the runtime refuses an agent resolving zero tools, `Read` stands and the refusal text is the recorded reason. `Read` is the narrowest tool a subagent can be given, so the fallback is already the floor. A subagent cannot see its own resolved tool surface, so the outcome is read off the runtime's response and never off the agent's account of itself. Acceptance: FR-010a, FR-008c; T095's classifier half goes green. The closed `AGENTS` tuples in `validate-agents.py` and `validate-codex-agents.py` are **not** edited — a new agent sits outside their scope, as `artifact-author` does. Depends on T094 and T095.
- [ ] T099 [US1] Create `speckit-pro/codex-agents/sweep-classifier.toml` and add `"sweep-classifier.toml"` to `REQUIRED_CODEX_AGENT_NAMES` in `speckit-pro/speckit_pro_runner/helpers/install.py` (line 31), the one-line edit `artifact-author` made at commit `1d58e5cbb`. Fields: `name` equal to the filename stem and `model = "gpt-5.5"`, the only two the loader reads and both rejected when wrong; `sandbox_mode = "read-only"`; a valid `model_reasoning_effort`; `description`; and `developer_instructions` carrying the same three capability-pointer markers the Claude body carries. **No `tools` or `disallowedTools` key** — Layer 1 rejects every Claude-only field in a TOML, so `sandbox_mode` is the only Codex lever and it bounds the filesystem, not the tool set. The residual that `read-only` does not sandbox MCP server processes is the spec's to state, not this file's. Acceptance: FR-008c, FR-003; the bundle inventory stays closed — a TOML without its name returns `incomplete_agent_bundle` under `unexpected_files` and a name without its TOML under `missing_files`, both asserted by `tests/speckit-pro/unit/test-speckit-pro-mutation-helpers.py`, which is why the pair lands in one task and neither red window opens. `install.py` is a production file this feature now modifies. Depends on T098; `install.py` is serial with T105.
- [ ] T100 [US1] Wire the classifier dispatch into the sweep sequence in both phase-execution references. **This replaces the read T040 documents**: the loop still iterates `candidates` and nothing else, but what it does per candidate is dispatch `sweep-classifier` with the block the redaction surface returned (T083), the closed class vocabulary, and nothing else, then consume the record — the enum, the target, and the bounded reason, the reason passed through the `log_row` leg before it reaches a cell or the report. **The orchestrator reads no comment body on any path**, so the control is that a body is never handed to a model carrying `Bash`, by construction rather than by a rule the orchestrator is asked to keep. **The dispatch lives in the sweep sequence, not in the routing table**: no category tag is emitted, no `Unresolved for consensus` item is produced, and `consensus-protocol.md`'s Category-Routed Dispatch table and its three phase-specific flows are never consulted, which is what leaves Clarify, Checklist, and Analyze unchanged — T058's `Type` value `Sweep` stays the only edit this feature makes to that file. A malformed record stops the run under FR-020 naming the comment id, because a classifier that cannot answer in its own vocabulary is a defect, not a fifth outcome. Acceptance: FR-010a, FR-010, plan Trust Boundary mechanism 2; T096 goes green. Depends on T043, T098, and T099; serial with every other task that edits both phase-execution references.
- [ ] T101 [US1] Document the piped observation in both phase-execution references: the two `gh` reads are piped straight into the runner — `gh … | python3 -m speckit_pro_runner`, the envelope wrapped around the read rather than around a file — so **no unredacted body is written to disk at any point**, and no comment text reaches a shell argument in either direction (FR-004b). Where a byproduct file is unavoidable it goes under `specs/<feature>/.process/feedback-sweep/` and nowhere else (FR-004d), which is where the reply bodies already live. Acceptance: FR-004, FR-004d, FR-010a; T097 goes green. **T089's byproduct list names the helper request as a file the runner reads on stdin, and under this task the request is never a file** — that sentence is superseded, and amending T089 is not this task's job. Depends on T100; serial with every other task that edits both phase-execution references.

**Checkpoint**: US1 is independently testable — the helper classifies a mixed
corpus deterministically and both references carry the read, the filter, and
the classification rules.

---

## Phase 4: User Story 2 — Amendments run through consensus, get recorded, and stop for re-review (Priority: P2)

**Goal**: Route only `amended` through consensus, apply and commit each edit,
write the durable records, reply once per handled comment, then stop or
proceed.

**Independent Test**: Give the sweep one trusted comment that clearly warrants
a plan change and one that does not. Confirm exactly one amendment commit
lands, both comments get a reply and a log row, only the amendment gets a
Consensus Resolution Log row, and the run stops with a re-review report. Re-run
with no new comments and confirm it proceeds into task work.

### Tests for User Story 2 ⚠️

- [ ] T044 [US2] Add the failure-path corpus cases to `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json`: a read that fails on the second surface and one that fails mid-pagination, both asserting **zero rows, zero replies, zero commits**; a push failure after an amendment commit, asserting **no row and no reply followed it**; a reply that fails on one surface, asserting the comment is owed a reply and that a re-run posts **exactly one**; a comment already carrying a sweep reply, asserting **no second one**; and a human-review consensus outcome, asserting a Consensus Resolution Log row, **no** Feedback Sweep Log row, and a stop. Acceptance: FR-008a's failure-path half. Each of these is a stop or a recovery whose whole value is behaving correctly when something else has already gone wrong, which is exactly the condition a hand-run check never reaches. Depends on T024.
- [ ] T045 [US2] Add the composed-interrupt case to the corpus: a run that records two amendments and fails to push a third, asserting **two rows written, zero replies posted, one local unpushed commit**, and that the next run posts both owed replies and re-enters consensus on the third. Acceptance: SC-013's composed case, FR-015c. Depends on T044.
- [ ] T046 [US2] Add the log-shape cases to the corpus: a disposition cell containing **a pipe and a newline**, asserting every later column including `CRL #` stays readable in its own position; and a comment whose author cannot be resolved, asserting a complete row with the `Author` cell saying so explicitly rather than sitting blank. Acceptance: FR-013, SC-010 — both are found-and-fixed defects, not hypotheticals. Depends on T045.
- [ ] T084 [US2] Add the redaction cases to `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json`. **One per hit class on the amendment leg**, six: a line matching each of `private_key_header` (header, three body lines, END line), `aws_secret_key`, `aws_access_key`, `bearer_token`, `assigned_token`, and one line over the 8192-byte bound, each seeding a distinctive string and asserting the span is replaced by `[redacted: <rule>]`, every byte outside the span is unchanged, the line count is unchanged, the report carries exactly one event naming that rule, **and the write proceeds** — the artifact is written with the placeholder, the one path is staged and committed, the row is written, the reply is posted, and the run stops after all of them under FR-012f's post-publication rule, the same stop and the same report as FR-017's. **One seeded string across all three legs**: the same bearer header on the `log_row` leg, asserting the Disposition cell carries the placeholder after FR-013's escaping with `CRL #` still readable in its column, the bookkeeping commit taken, the reply posted, and the run stopping after both under FR-012f with a report in FR-017's shape and a resume path of re-run; and on the `reply` leg, asserting line 1 of the reply file is the marker alone, byte-identical, and the placeholder stands on a later line. **The marker-line case**: `<!-- speckit-pro:feedback-sweep IC_kwDOKQ7tDs5vXkZ9 -->` as the sole line of a `reply` request, asserting zero events and byte-identical output — the id is a token-shaped run, so this case fails the moment any rule fires without its trigger. **The ordered bound case**: a line over 8192 bytes whose tail carries a bearer token, asserting one `over_bound_line` event and **no** `bearer_token` event, beside its sibling at 8188 bytes, the longest line whose bearer replacement still fits the bound, asserting the reverse. **The boundary case**: an 8189-byte line ending in `bearer`, a space, and a twenty-character token-shaped run, asserting the whole output line is `[redacted: over_bound_line]`, that the report carries the `bearer_token` event and then the `over_bound_line` event on that line, and that the placeholder fed back returns unchanged with zero events. **The key-span cases**: header and body with no END line, asserting placeholders to the end of the text; body with no header, asserting **no** redaction, which pins the residual as a known miss. **The negative cases**, each asserting zero events and byte-identical output: `bearer authentication_credentials`, `bearer credentials/authorization-header`, the phrase `bearer token`, a bare `GITHUB_TOKEN`, `RELEASE_PLEASE_TOKEN=${{ secrets.RELEASE_PLEASE_TOKEN }}`, `GH_TOKEN=<your-token>`, a `_TOKEN=` value of twenty `x`s, a bare GitHub node id such as `IC_kwDOKQ7tDs5vXkZ9`, which is a token-shaped run with no trigger before it, and the `aws_secret_key` bullet of FR-012f pasted as an amendment line, which names the rule and carries no value. **The multi-occurrence case**: one line carrying two full bearer tokens, asserting both runs replaced and two events on that line. **The idempotence case**: every positive case's output fed back through the surface and returned unchanged with zero events, the boundary case included. **The transport cases**: a 9 KB line and the same line cut to 8193 bytes, asserting byte-identical output and a byte-identical report; and a request carrying a 33 KiB line, asserting the runner's `invalid_input` naming the field, which pins the failure shape a skipped transport cut produces. **The closed-input cases**: a `leg` outside the closed set, an empty comment id, and a non-array `lines`, each returning `invalid_input`. **The re-run case**: after the log-row case's post-publication stop, a re-run with no new comments produces zero rows, zero replies, zero amendments, and zero events, and proceeds, which is what makes that stop convergent; after the amendment case's stop, a re-run excludes that comment as `already_logged`. Use the request shape T004 settles for the outbound legs. Acceptance: FR-012f, FR-008a's outbound paragraph, SC-003 and SC-013 under redaction, SC-014. Depends on T046.
- [ ] T047 [US2] Add the captured-command reply assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: exactly one reply per handled comment with no handled comment at zero and none at two; every reply's line 1 being the marker alone — opening at position 0, closing on that line, the disposition starting on line 2; the `amended` reply naming artifact, section, and commit while the other three name none of those; **every body passed by file path** with no comment or reply text in any command string; a conversation reply naming the comment it answers; and the truncation line — the reply for a corpus comment whose body the parse reported `truncated` and whose analyst-payload report counts one withheld span carries `Body truncated at 8192 bytes; N spans withheld.` as its last line with `N` equal to that report's `spans_withheld`, and the plain-body comment's reply carries no such line. Acceptance: FR-015 and its reviewer line, FR-015a, SC-002, SC-008, SC-009. Replies sit outside the runner's determinism guarantees, so they are proved against captured commands, not against a golden helper response. Depends on T046.
- [ ] T085 [US2] Add the no-echo and report-shape assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: every seeded string from T084 appears in **no** surface response, **no** committed artifact text, **no** log row, **no** reply file, and **no** run report anywhere in the corpus — the same negative shape FR-008b's first assertion uses for comment bodies, scoped to outputs because the seed is present in the surface's request by construction, and the clause that makes SC-014 checkable by search over captured output rather than by judgment. Assert that every run-report redaction entry carries exactly the comment id, the leg, the rule, and a count **and nothing else about the match** — no excerpt, no redacted or truncated copy, no encoded form, and no operator-action pair, which FR-020 reserves for the human-review stop. Assert that the report for any run carrying an event is a stop report whose what-landed part names every commit, row, and reply the run took and whose resume path is re-run, because the stop fires only after all of them landed. Assert the line-count invariant on every case, the idempotence case, and that the same request twice returns byte-identical responses. Assert the documentation rule too: grep both phase-execution references for `secret scanner`, case-insensitive, and fail unless every occurrence sits inside the sentence `It is not a secret scanner`. Acceptance: FR-012f, SC-014. Depends on T047 and T084.
- [ ] T090 [US2] Add the byproduct assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`, against the captured commands T024 stores and T047 asserts over: **every path a captured command names as a sweep byproduct** — the request file on stdin, every body file path, every capture file — resolves under `specs/<feature>/.process/feedback-sweep/`, tested with `Path.is_relative_to` after resolution, so a file written anywhere else is a red test; and the run report in every corpus case, stopping or proceeding, names that directory as removed. Acceptance: FR-004d's second and third fixtures. The first is T088; together the three make the ignore entry, the placement, and the removal each fail independently. Depends on T047 and T085.
- [ ] T091 [US2] Add the corpus-scan assertion to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: run every line of this feature's **eight** documents — `specs/art-008-feedback-sweep/spec.md`, `plan.md`, `tasks.md`, `data-model.md`, `contracts/sweep-pr-feedback.md`, **`contracts/sweep-classifier-output.md`**, `quickstart.md`, and `research.md` — through the redaction surface's `amendment` leg via the runner, one request per document, and assert zero events and byte-identical output for each. The documents describe the rules in prose and carry the negative examples, so this is the executable form of the claim that no rule fires on a name, a phrase, or a quoted header; a rule loosened back to a substring fails here before it fails on a reviewer's amendment. The longest line in the eight is under 5 KB today, so the bound has room; a document line that crosses 8192 bytes fails this scan by design, because an amendment rewriting it would land as a placeholder. Acceptance: FR-012f's deny-set, FR-008a's corpus-scan case, SC-014. Depends on T090.
- [ ] T092 [US2] Add the captured-call fixture to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`, with the captured surface invocations stored as a sibling top-level key inside `tests/speckit-pro/unit/fixtures/feedback-sweep/expected-envelopes.json` beside the captured commands T024 stores: every call the orchestrator makes to the redaction surface is captured as sent — `leg`, `comment_id`, request, response — the way SC-009 captures argv. Assert per-leg call counts **derived from the corpus expectations, never typed beside them**: one `amendment` call per expected amendment; one `log_row` call per prose cell, which is three for an amended item (Feedback Sweep Log `Disposition`, Consensus Resolution Log disposition, Consensus Resolution Log item cell), two for a human-review item, and one for any other class; one `reply` call per reply the run posts — every comment handled this run plus every owed reply FR-015b reconciles this run, both read from the case's expectations, so T044's re-run case derives one and T045's derives two; and one `analyst_payload` call per comment routed to consensus. Then assert the report identity: for every comment in every case, the disposition text the run report carries is byte-identical to the `log_row` response for that comment's `Disposition` cell, taken before FR-013's escaping. Then assert the sink, against the captured commands: no `gh` invocation and no commit argv in any case carries the run report or any line of it, so a report that leaked onto the pull request or into a commit fails here rather than passing the no-echo search, whose seeded strings the report never carried. Acceptance: FR-008a's captured-call paragraph, FR-012f's report rule, FR-018a's sink, SC-014's orchestrator half. A wired surface whose orchestrator forgets one call — the log-row leg is the likely one, because its cells are filled last — fails the count, and a report assembled from the orchestrator's pre-call copy of a disposition fails the identity check; neither failure is reachable from a corpus that only sends requests to the surface. Depends on T085 and T087.
- [ ] T093 [US2] Add the amendment-commit subject assertion to `tests/speckit-pro/unit/test-feedback-sweep-parse.py` against the captured commands T024 stores: every amendment commit's captured argv carries exactly one `-m` whose value is `docs(<feature-id>): amend <artifact> for <comment-id>` with `<artifact>` one of `spec.md`, `plan.md`, `tasks.md` and `<comment-id>` the id of the comment the case amends, no body, and no byte outside those slots; the same value matches `^(feat|fix|chore|docs|test|refactor)\([a-z0-9-]+\): .+`, the release-readiness title regex in `speckit-pro/speckit_pro_runner/gates/release.py`; and no seeded string from T084 appears in any commit argv. Acceptance: FR-012's subject rule, SC-009's capture extended to the commit. The subject is built from ids and enums alone, which is why it is not an FR-012f leg; this assertion is what keeps it that way when someone later adds a helpful summary. Depends on T047 and T050.
- [ ] T102 [US2] Add the sweep-analyst dispatch assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`, against the captured dispatches T096 stores: per **amended** item, exactly **three** `sweep-analyst` dispatches carrying the perspectives `codebase`, `spec-context`, and `domain` **in the prompt**, plus exactly **one** more `sweep-analyst` dispatch in the synthesis prompt, both counts derived from the case's expectations; **zero dispatches to `consensus-synthesizer` in every case**, which is the assertion that keeps synthesis off an agent inheriting `Bash`; and zero dispatches to the three shared analysts, so the shared roles and the routing table stay out of the sweep path. Assert each dispatch carries the surface's returned block and no other reviewer bytes, and that a human-review outcome (T049) produces its Consensus Resolution Log row with **no** structured edit captured. Acceptance: FR-011b, FR-008a's captured-call paragraph extended to the analyst. Depends on T092 and T093; serial with every other task that edits the parse test file.
- [ ] T103 [US2] Add the structured-edit assertions to `tests/speckit-pro/unit/test-feedback-sweep-parse.py`: every edit an analyst returns is the record T094 fixes — `file` a member of the three-name allowlist by exact equality over resolved paths, an `anchor` that resolves in that file **exactly once and sits inside the contract's 512-byte cap**, and a `replacement` inside the contract's 8192-byte cap — one red case per stop, the fourth being an anchor a single byte over its cap, which stops before any write the same way an over-cap replacement does — and **every replacement reaches the write only after the `amendment` leg returned it**, asserted as byte identity between the written text and that leg's response, so a write assembled from the analyst's own string fails. Assert a returned `file` outside the allowlist is refused at the write point under T051's rule 2 and reported, never coerced onto an allowed path. Acceptance: FR-011b, FR-012b, FR-012f. A structured edit is what makes an analyst's return reviewable rather than free text, and this is the assertion where that claim is checkable. Depends on T102 and T086.

### Implementation for User Story 2

- [ ] T048 [US2] Document consensus routing in both phase-execution references: **only `amended`** routes into consensus; `answered`, `deferred`, and `no action` never invoke it. **The sweep runs its own consensus and never consults the category-routed routing table.** Per amended item the sweep sequence dispatches **three `sweep-analyst`s, the perspective — codebase, spec-context, domain — given in the prompt rather than by role**, then **one more `sweep-analyst` in a synthesis prompt**. **Synthesis is not `consensus-synthesizer`**, which declares no `tools:` allowlist and so inherits `Bash`, `WebFetch`, `WebSearch`, and every installed MCP server: routing sanitized reviewer text to it would reopen F-1 one hop downstream. **The sweep emits no category-tagged `Unresolved for consensus` item**, so `consensus-protocol.md`'s routing table and its three phase-specific flows are never reached and Clarify, Checklist, and Analyze are unchanged. **Say `routing table untouched`, never `consensus-protocol.md untouched`** — T058 edits that file to add the `Sweep` row type. **The domain perspective runs without web access and the reference says so**, because `sweep-analyst` carries a closed read-only allowlist. Acceptance: FR-011. Depends on T043.
- [ ] T049 [US2] Document the human-review outcome in both phase-execution references. All three ways consensus fails to answer — all three analysts disagreeing after Round 2, a Round-1 escape whose Round 2 still cannot resolve, and an analyst that fails its single retry — land on one behavior; only the report names which occurred. **No edit, no class, no sweep row**: writing no row is what makes the comment a candidate again once a human has resolved it, because the skip key is the log's comment-id column and nothing else. It surfaces as one Consensus Resolution Log row instead, `Type` `Sweep`, its item cell naming the comment id, and it **COUNTS** toward the Round-2 escape-rate metric. **It stops the run whether or not anything was amended**, and when other items amended in the same run it is the same stop and one report, not two. **Other items in the batch still complete** — resolved items are edited, committed, recorded, and replied to normally. Acceptance: FR-011a. Depends on T048.
- [ ] T050 [US2] Document the amendment commit protocol in both phase-execution references: **one commit per amendment**, never a single run-wide blob, because a row names its commit, a reply names the amending commit, and the stop reports a commit range — none of which survive collapsing. Each amendment commit **stages exactly the one artifact path it amended, never a directory**, and takes the fixed-shape subject `docs(<feature-id>): amend <artifact> for <comment-id>` with no body — the scope the feature's roadmap id in lowercase, `<artifact>` one of the three, `<comment-id>` the observation's id — so every slot is an id or an enum, no byte from a comment or a resolution reaches `git log`, and the subject is not a redaction leg; it satisfies the release-readiness title regex, and T093 asserts the captured argv. Flag the specific hazard: the sweep is a **Phase 7 setup step, and Phase 7 is the one phase whose existing commit path uses `git add -A`**; an amendment commit that inherits that pattern would stage the entire worktree and defeat the edit-surface allowlist at the last step. Acceptance: FR-012, FR-012b's staging rule. Depends on T049.
- [ ] T051 [US2] Implement the FR-012b rule-2 edit-surface check in `speckit-pro/speckit_pro_runner/helpers/read_only.py` and document rule 1 in both phase-execution references. **Rule 1, at classification**: a comment whose requested change lies outside `spec.md`, `plan.md`, and `tasks.md` in the feature directory takes `deferred`, with the refused target named in the disposition and the reply; word it as recorded and not acted on, and **it must not imply future action**, because the request is declined, not scheduled. **Rule 2, at the write**: the resolved target path is checked in code before any write. **The comparison is exact membership over resolved paths, never containment** — resolve the candidate and all three allowed paths, then test for equality against that three-member set. A containment or prefix test would admit anything beneath the feature directory, its checklists and contracts included, and prefix comparison against an unresolved path is a recurring traversal defect in its own right. **Reject a target that is a symbolic link, and reject one any of whose parents up to the feature directory is** — reuse the *shape* of `validate_target_path()` at `speckit-pro/speckit_pro_runner/helpers/mutation.py:1179`, which checks repository boundary and traversal safety but **not** job-scoped file identity, so this reuses its shape rather than its predicate. **The feature directory arrives as the explicit `feature_dir` input, never inferred** — the one inference mechanism available keys off a branch-name pattern this feature's own branch does not match, so inference would resolve to the wrong specification or to nothing. Acceptance: FR-012b, FR-012c. Rule 1 alone would be prose a mis-routed item walks past; rule 2 alone would turn an ordinary out-of-scope request into a stopped run. **Rule 2 is the enforcement boundary and rule 1 is disposition.** Depends on T050 and T034.
- [ ] T052 [US2] Document the write-point stop in both phase-execution references: it reports under the FR-020 contract, naming the refused target path, the comment id it came from, and the resume path, which is to fix the classification and re-run. Reaching this check means classification already failed, so it is a defect report rather than a routine path — which is why it stops rather than downgrading quietly. Acceptance: FR-012d. Depends on T051.
- [ ] T086 [US2] Implement the three outbound legs of the FR-012f redaction surface in `speckit-pro/speckit_pro_runner/helpers/read_only.py`, extending the named surface and leg dispatch T083 lands, registering nothing new. Inputs: `leg` from the three outbound values `amendment`, `log_row`, `reply` (the fourth value, `analyst_payload`, is T083's); a non-empty `comment_id`; `lines` as an array of strings; anything else returns `invalid_input`. **The bound runs first**: a line over **8192 bytes** as UTF-8 — the figure `data-model.md` and the contract fix for a comment body — becomes `[redacted: over_bound_line]` whole and is never scanned, never truncated, never split. Then `private_key_header`, which fires only on a line that is exactly a PEM header — `-----BEGIN `, optionally a run of uppercase letters, digits, and spaces ending in a space, then `PRIVATE KEY-----` or `PRIVATE KEY BLOCK-----`, nothing else on the line but surrounding whitespace — whose span runs from that line through the first later line that is exactly the matching `-----END` form or to the end of the text, every line in the span becoming the placeholder. Then, in this order and for every non-overlapping occurrence left to right, each requiring a value and never firing on a name alone: `aws_secret_key` and `aws_access_key` (a name beginning `AWS_SECRET` or `AWS_ACCESS_KEY`, case-insensitive, through any run of letters, digits, and underscores; optional spaces or tabs; `=` or `:`; optional spaces or tabs; an optional `"` or `'`; then a token-shaped run; span the run), `bearer_token` (`bearer`, case-insensitive, one or more spaces or tabs, then a token-shaped run; span the run), and `assigned_token` (a name from `A-Z0-9_` ending `_TOKEN`, `=`, an optional `"` or `'`, then a token-shaped run; span the run). **Then the bound again, on the same pass**: a placeholder can be up to six bytes longer than the run it replaces, so the output line is measured against 8192 once more and a line that grew past it becomes `[redacted: over_bound_line]` whole, with the deny-set event and then the bound event reported on that line. **A token-shaped run is twenty or more consecutive characters from `A-Za-z0-9._~+/=-` to the first character outside the class or end of line, at least one of them a digit** — the digit is what keeps `bearer authentication_credentials` and a row of `x`s out, and the class is what keeps `${{ … }}` and `<…>` out. The placeholder is `[redacted: <rule>]` with nothing else in it, so it carries zero reviewer bytes, contains neither pipe nor newline, and matches no rule; replaced spans are never rescanned, and with the second bound check the first-pass output is a fixpoint: fed back it returns unchanged with zero events, at the boundary included. **One line in is one line out.** The report is a list of `{rule, line}` events and never carries the replaced bytes. Document the **amendment-leg call point** in both phase-execution references beside the commit protocol: after consensus resolves the edit and before the write, the text the edit **introduces** — the replacement or inserted lines, never the file around them and never a diff read back — goes through the surface, and the orchestrator writes what comes back, stages the one path, commits, and pushes; no staged path list is read back, and FR-012b's single-path staging plus the `git add -A` flag in T050 remain the controls on what a commit stages. Document the transport cut there too: for any line over 8192 bytes the surface returns the placeholder whatever lies past byte 8193, so the orchestrator cuts each line at the first character boundary at or past byte 8193 before transport, which is outcome-equivalent, keeps every string under the runner's 32 KiB limit, and never splits a line. Acceptance: FR-012f, SC-014; T084 and T085 go green for the amendment leg. Depends on T052 and T083.
- [ ] T053 [US2] Document the push-failure outcome in both phase-execution references: the push is part of the amendment step, not a step after it. A commit that succeeded whose push failed **stops the run immediately, before that amendment's bookkeeping commit**, naming the unpushed commit's sha and the comment id. **The local commit stands and is not unwound** — the edit is correct work consensus resolved. No row means the skip key does not see the comment, so it is a candidate again next run. **A bookkeeping commit whose push fails stops the run the same way**, differing in one consequence: its row is already in the local workflow file which the sweep reads locally, so the skip key **does** see the comment and the reply is what would otherwise be lost, recovered by the reconciliation rule. **No automatic retry** — retrying inside the run would multiply the window the per-amendment cadence exists to bound. Acceptance: FR-012e, T044's push-failure case. Depends on T086.
- [ ] T054 [US2] Document the bookkeeping commit in both phase-execution references: log writes ride a **separate** commit and are never folded into an amendment commit, because a row that names its commit cannot exist until that commit's sha does. It stages the **workflow file path alone, never the directory**, and takes a `chore:` subject. **The trigger is rows, not handled comments**: a run takes one when it wrote at least one row to **either** log and takes none when it wrote none. Three consequences follow — a run with zero amendments but at least one handled comment takes exactly one; a run that handles no comment but must write Consensus Resolution Log rows also takes exactly one; a run that wrote no row to either log takes none, and an empty commit there would record nothing. One bookkeeping commit per amendment, not per run, which bounds the pushed-but-unrecorded window to a single item. Acceptance: FR-012a. Depends on T053.
- [ ] T055 [US2] Add the Feedback Sweep Log entry to `speckit-pro/skills/speckit-autopilot/references/workflow-file-protocol.md`, modeled on the `Draft PR` entry at lines 62–120. Header: `| # | Comment ID | Surface | Author | Class | Disposition | Commit | CRL # |`, one row per **handled** comment — a comment assigned a class, which excludes one the trust filter or the self-reply rule dropped and one whose consensus round returned no answer. **The `Disposition` cell escapes any pipe as `\|` and any newline as a line break**, because the table readers in this codebase split rows on the bare pipe with no escape handling and one unescaped pipe would shift `CRL #` out of position. When the author cannot be resolved the `Author` cell records that explicitly rather than sitting blank. **The workflow file is the sole store; no state-file mirror may be written.** Acceptance: FR-013, SC-010, T046 goes green. Depends on T054.
- [ ] T056 [P] [US2] Mirror the Feedback Sweep Log entry into `speckit-pro/codex-skills/speckit-autopilot/references/workflow-file-protocol-codex.md`, compressed to that file's far tighter entry style — the entire Codex protocol file is 90 lines. Acceptance: FR-003, SC-007; the eight-column header and the escaping rule are identical. Different file from T055, so parallel-safe.
- [ ] T057 [US2] Document the log's creation and placement in both workflow-file-protocol files. **The sweep creates it** when the workflow file carries none, writing the heading and the header row itself — it cannot come from the workflow template, because FR-002 forbids changing that template. **Creation and the first rows are one write in one bookkeeping commit**, never a commit of their own ahead of it, because a commit carrying an empty table is read as "nothing has been handled", indistinguishable from a genuine clean first run. **Placement matches the anchor's level rather than assuming one**: match `Consensus Resolution Log` by heading text at any level and write `Feedback Sweep Log` at the **same** level so the two are siblings; when no anchor exists, append `## Feedback Sweep Log` at the end. Of 69 committed workflow files, 33 carry no such heading, 31 write it at `###`, and 5 at `##` — including this feature's own. **Rows number sequentially and continue across runs**, each new row taking one more than the highest already present. Acceptance: FR-013a, SC-012. Depends on T055 and T056.
- [ ] T058 [US2] Add the fourth `Type` value `Sweep` to the Consensus Resolution Log row schema at `speckit-pro/skills/speckit-autopilot/references/consensus-protocol.md` line 617, beside the shipped `Clarify`, `Gap`, and `Finding`, plus the sweep-row escape-rate note. The link is **bidirectional and costs no extra column**: the sweep row's `CRL #` names the consensus row, and that row's item cell names the **comment id**, keying the reverse direction on an immutable value. Sweep rows **COUNT** toward the Round-2 escape-rate metric, and the `Type` column is itself the source discriminator so a breach can be attributed without excluding anything. On the human-review path the link degrades to one direction, by design. Acceptance: FR-014. Depends on T057.
- [ ] T059 [US2] Document the reply templates in both phase-execution references: **exactly one reply per handled comment**, following a run whose bookkeeping commits all landed. Every reply names its class. **Only an `amended` reply names an artifact, a section, and a commit** — requiring those of all four would make three of the four templates unsatisfiable. One fixed template per class, plain public-readable English, and one more fixed-shape line as the **last** line of every template, present only when the parse reported the comment `truncated` or its analyst-payload report carries `spans_withheld` above zero: `Body truncated at 8192 bytes; N spans withheld.`, `N` being that count and nothing else in the line — the channel that tells a reviewer their fence or their tail was not read, because SC-008 fixes the pull request as where a reviewer learns what happened and the disposition cell is in a file they need not open. **Every template opens with the HTML comment whose prefix is the same fixed string in every reply**, `<!-- speckit-pro:feedback-sweep`, followed by the answered comment's id and the closing `-->` (FR-015b), which renders as nothing and is what the self-reply rule anchors on. **The marker is the whole of line 1, alone; the disposition starts on line 2** — nothing else shares that line, which is what keeps FR-012f's per-line rules off it. A marker rather than a visible sentence, because a visible sentence is exactly what a reviewer quotes when they disagree. Acceptance: FR-015, T047 goes green. Depends on T058.
- [ ] T060 [US2] Document the two write paths in both phase-execution references: a review-thread reply posts **into its thread**; the pull-request conversation has no threading, so a reply there is a **new top-level comment that names the comment it answers**. **Every body is passed by file path, never inline.** Acceptance: FR-015a, FR-004b, SC-009. Neither shipped reply-writer in this repository posts to the conversation surface, so that write is new work with no prior art to copy. Depends on T059.
- [ ] T061 [US2] Document reply reconciliation in both phase-execution references: **replies are reconciled against the pull request, not assumed from the log.** A comment is owed a reply when it is **present in this run's observation**, has a log row, and carries no sweep reply answering it. The observation qualifier is load-bearing — keying on log rows alone would post a second reply into a thread someone deliberately resolved, turning a recovery rule into a duplicate-reply generator. **The fixed marker carries the answered comment's id** after its unchanged fixed prefix, inside the same HTML comment, so a thread carrying more than one comment still says which one a reply answered. **A failed reply is reported and does not by itself stop the run** — it appears in the run report naming the comment id and the surface. Acceptance: FR-015b, SC-002. An observation that failed means the work never happened; a reply that failed means the work landed and only the notification did not. Depends on T060.
- [ ] T087 [US2] Document the **log-row and reply call points** and the report line in both phase-execution references. *Log row*: the cells the sweep fills with prose rather than an id, enum, sha, or count — the Feedback Sweep Log `Disposition` cell, the disposition text of the Consensus Resolution Log rows FR-014 and FR-011a write, and those rows' item cell, which names the comment id and then summarizes the item in prose the way every shipped row does — go through the surface with `leg` `log_row` **before** FR-013's pipe and newline escaping, **one call per cell**, so an amended item makes three calls on this leg, a human-review item two, and any other class one; the placeholder contains neither, so escaping never splits it and `CRL #` stays in its column. *Reply*: the filled reply body, marker included, goes through the surface with `leg` `reply` before it is written to the file FR-004b passes by path; the marker stands alone on line 1 (FR-015), no rule's trigger is on that line, and the two line-granularity rules never reach a line ahead of their own, so FR-006's anchor survives and an over-bound disposition costs line 2 onward, never the marker. *Report*: the run report names, per affected comment, the leg, the rule, and the count, on every path, and nothing else about the match — never the matched line, an excerpt, a redacted copy, or an encoded form, in the report, a row, a reply, or the surface's own response; the disposition it carries per comment is the `log_row` response for that comment's `Disposition` cell, before escaping, never the orchestrator's copy from before the call; and it goes to the operator's sink, the one the plan-stage stop report reaches, never to the pull request. **Redaction never refuses a write and never discards a row**, because FR-012a batches every row of a zero-amendment run into one bookkeeping commit and a refused commit would livelock on ordinary reviewer input. **A run with any event stops once every write has landed** — after the last push and the last reply — in the re-review report's shape with resume path re-run, a ninth FR-020 condition; the next run finds the rows and replies in place, fires no event, and proceeds. Acceptance: FR-012f's three legs and report rule, SC-014; T084 and T085 go green for the log-row and reply legs. Depends on T061 and T086.
- [ ] T062 [US2] Fix the reply timing in both phase-execution references: **replies post once, at the end of the run, after every bookkeeping commit this run takes has landed. No reply is posted before that point.** Two orders are defensible and FR-003 forbids two. Under the run-scoped rule the composed interrupt case is exact rather than ambiguous — two rows written, **zero** replies posted, one local unpushed commit. **Name which stops post replies**: the re-review stop, the human-review stop, and the post-publication redaction stop occur **after** the reply point so a run reaching any of them has already posted every reply it owes; **every other stop aborts before it and posts none** — an invalid authenticated account, a corroboration failure, a failed observation, an unreadable log row, a refused edit target, and a failed push. A blanket "a stopping run first posts what it owes" would contradict this directly. Acceptance: FR-015c, T045 goes green. Depends on T087.
- [ ] T063 [US2] Record in both phase-execution references that **the sweep never resolves a review thread**. Acceptance: FR-016; the no-thread-resolution non-goal is stated in the reference rather than left to be inferred. Depends on T062.
- [ ] T064 [US2] Document stop-or-proceed in both phase-execution references. **One or more `amended`**: stop for re-review before any task work, with a report shaped like the plan-stage stop report naming the comments swept, the amendments made, the commit range, and **stating that draft pages regenerate once slice 2 lands**. **No `amended` but at least one handled**: write the records, post the replies, proceed directly into task execution without stopping. **No comment handled at all**: no rows, no replies, **no bookkeeping commit**, proceed. The last two are separated so the first does not read as requiring an empty commit on a pull request that carried no comments. **Any redaction event, on any leg**: stop once every commit is pushed and every reply is posted, in the re-review report's shape with the per-comment leg, rule, and count, resume path re-run; where nothing was amended this replaces the proceed at that same point, and where the re-review or human-review stop also holds it is the same stop and one report. Acceptance: FR-017, FR-018, FR-012f's post-publication stop. The regeneration sentence is an interface slice 2 replaces — until it does, it is the only thing telling a reviewer why the pages they are looking at are older than the amendments. Depends on T063.
- [ ] T065 [US2] Document the universal run report in both phase-execution references: it exists on **every** path the sweep takes, not only the stopping one, because the proceed path is exactly where a run that swept nothing but untrusted comments lands. Every run reports **each observed comment's disposition, candidates and exclusions alike, with every exclusion naming its reason** — "not swept: untrusted author" for the trust filter, and every self-reply exclusion named the same way. A run that observed no comments at all reports that, which is a one-line report rather than an absent one. Acceptance: FR-005's reporting half, FR-018a, FR-006's visibility rule. Depends on T064.
- [ ] T089 [US2] Document the byproduct directory in both phase-execution references, beside the sweep sequence: **every file the sweep writes for its own transport** — the helper request the runner reads on stdin, each reply body file FR-004b passes by path, the captured commands, any scratch — is written under `specs/<feature>/.process/feedback-sweep/` and nowhere else; a committed root `.gitignore` entry ignores that directory, which is what keeps Phase 7's `git add -A` from staging the request with every observed body in it; and **the sweep removes the directory before it proceeds into task work or stops, on every path**, because the spec-index generator scans the worktree rather than the index and live byproducts would contaminate a regeneration. The run report names the directory as removed. State why the request cannot be redacted instead: the parse filters over those bodies, so the file must carry them. Acceptance: FR-004d; T090 goes green. Depends on T065.
- [ ] T104 [US2] Create `speckit-pro/agents/sweep-analyst.md`. Frontmatter: `name`, `description`, `tools: Read, Grep, Glob`, `disallowedTools: Skill, Agent, TeamCreate, SendMessage`, a positive `maxTurns`, a non-empty `effort`. Body: the three capability-pointer markers, in body prose and never in the returned edit, for the reason T098 records — the structured edit stays exactly `{file, anchor, replacement}` and a fifth field is malformed; the rule that the perspective — `codebase`, `spec-context`, or `domain` — arrives **in the prompt** rather than in the definition, which is what lets one agent serve all three and leaves the shared analysts and their routing table untouched; the synthesis prompt's shape; and the structured edit T094 fixes as the only return. **The domain perspective runs with no web access, and the body says so** — `Read`, `Grep`, and `Glob` reach this repository and nothing else, so a domain question it cannot ground is reported as ungrounded under the grounding contract the pointer already binds it to, never guessed. No `mcp__` token anywhere. Acceptance: FR-011b, FR-008c; T095's analyst half goes green, which closes the cross-phase red T095 names. Depends on T094 and T095.
- [ ] T105 [US2] Create `speckit-pro/codex-agents/sweep-analyst.toml` and add `"sweep-analyst.toml"` to `REQUIRED_CODEX_AGENT_NAMES` in `speckit-pro/speckit_pro_runner/helpers/install.py`, the same one-line edit as T099. Fields as T099 fixes them: `name` equal to the stem, `model = "gpt-5.5"`, `sandbox_mode = "read-only"`, a valid `model_reasoning_effort`, `description`, and `developer_instructions` carrying the three capability-pointer markers and the perspective-in-the-prompt rule. No `tools` or `disallowedTools` key. Acceptance: FR-008c, FR-003; `validate-codex-parity` finds a mirror for the Claude agent, and the bundle inventory stays closed in both directions. Depends on T104; `install.py` is serial with T099, so the two inventory lines are not parallel-safe with each other.
- [ ] T106 [US2] Wire the sweep's own consensus dispatch into the sweep sequence in both phase-execution references: per `amended` item, spawn `sweep-analyst` **three times** with the perspective in the prompt — codebase, spec-context, domain — await all three, then spawn `sweep-analyst` **once more in the synthesis prompt**, and apply the one structured edit it returns. **Synthesis is `sweep-analyst`, never `consensus-synthesizer`**: the synthesizer declares no `tools:` allowlist and so inherits `Bash`, the web tools, and every installed MCP server, and routing sweep text through it would reopen the boundary one hop downstream, where it is harder to see. **The `consensus-protocol.md` routing table is neither edited nor consulted** — the sweep emits no category tag and no `Unresolved for consensus` item, its dispatch is named here in its own Phase 7 setup block, and Clarify, Checklist, and Analyze keep the shared analysts and the three phase-specific flows unchanged; T058's `Type` value `Sweep` and the escape-rate note stay the only edits this feature makes to that file. The human-review outcome T049 documents is unchanged: all three disagreeing after Round 2, a Round-1 escape Round 2 cannot resolve, and an analyst failing its single retry still land on one behavior. Acceptance: FR-011b, FR-011a, FR-003; T102 goes green. Depends on T089, T104, and T105; serial with every other task that edits both phase-execution references.
- [ ] T107 [US2] Document the structured edit and its call order in both phase-execution references: an analyst returns `{file, anchor, replacement}` and nothing else — never a patch, never prose the orchestrator retypes — with `file` from the three-name allowlist and `replacement` inside the contract's cap; the orchestrator passes `replacement` through the redaction surface's `amendment` leg (T086), writes what comes back, checks the resolved target in code before the write (T051 rule 2), stages that one path, commits, and pushes. **The order is fixed — allowlist check, redaction, write** — because redacting after the write leaves reviewer bytes in a commit and checking the target after the write is not a check. An edit whose `file` is outside the allowlist, or whose shape is not the record, stops the run under FR-020 naming the comment id, the same way a malformed classifier record does. Acceptance: FR-011b, FR-012b, FR-012f; T103 goes green. Depends on T086 and T106; serial with every other task that edits both phase-execution references.

**Checkpoint**: US1 and US2 both work independently — the sweep amends,
records, replies, and stops or proceeds.

---

## Phase 5: User Story 3 — An unreadable draft pull request stops the stage (Priority: P3)

**Goal**: Every corroboration status that is not `match` or `no_record` stops
the stage before task work with a report naming the condition and a resume
path; `no_record` passes through.

**Independent Test**: Run the sweep once per unreadable condition and confirm
each stops before task work with a report naming that condition and a resume
path. Then run it with no draft pull request record and confirm it proceeds.

### Tests for User Story 3 ⚠️

- [ ] T066 [US3] Add one corroboration case per status to `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json`: `match` sweeps, `no_record` proceeds, and `skipped`, `pr_closed`, `pr_missing`, `identity_mismatch` each stop. Add a **seventh case carrying a value outside the six**, asserting a stop reported as a malformed record rather than mapped onto one of the six. Acceptance: FR-019, SC-006, FR-008a's "a corroboration value outside the six". Depends on T047.

### Implementation for User Story 3

- [ ] T067 [US3] Document the corroboration gate in both phase-execution references: **the six statuses are exhaustive and each maps to exactly one behavior.** Four stop by name — `skipped`, `pr_closed`, `pr_missing`, `identity_mismatch` — `no_record` proceeds, and `match` sweeps. **Each stopping status names its own resume path**, because the four have different fixes: `skipped`, fix the tool and re-run; `pr_closed`, reopen the pull request or clear the `Draft PR` row if the checkpoint is genuinely abandoned, then re-run; `pr_missing`, clear the row, the one status where its absence would match reality; `identity_mismatch`, correct the row to name the right pull request, then re-run. **The sweep never writes the `Draft PR` row on any path**, including these stops — a run that repaired the record it had just failed to corroborate would destroy the evidence of the discrepancy. **A value outside the six is a malformed record and stops**, because exactly one status proceeds and a default that proceeded would make a corrupted record the cheapest way past the checkpoint. Acceptance: FR-019, SC-006, T066 goes green. Depends on T065.
- [ ] T068 [US3] Document the `skipped`-versus-`no_record` distinction in both phase-execution references: `no_record` means the gate does not apply, because no checkpoint was ever opened; `skipped` means the gate applies and the observation failed. Treating "could not observe" as "observed nothing" would make the checkpoint silently optional exactly when the tool is unreliable, which is when unread feedback is most likely. Acceptance: FR-019a. Depends on T067.
- [ ] T069 [US3] Document the four-cause `skipped` report in both phase-execution references: it **must read differently from the three discrepancy stops** and **must name which cause occurred** — the tool was absent, unauthenticated, rate-limited, or returned output that could not be parsed. Behavior does not branch on the cause; only the report does. **Clearing the `Draft PR` row is not a resume path here** — that belongs to `pr_missing`, and reusing it would erase a probably-true record to manufacture a `no_record` reading. Acceptance: FR-019b, SC-006. Depends on T068.
- [ ] T070 [US3] Document the mid-read failure report in both phase-execution references, distinguishing it from the gate stop. Both draw on the same four causes, so the mid-read report **must also name that reading had begun, and which surface failed** — an operator who cannot tell a gate failure from a mid-read failure cannot tell whether the pull request was ever reachable. The resume path is the same: fix the tool and re-run, since the observation is retaken fresh on every invocation and needs no repair step. Acceptance: FR-004c's report half, SC-011. Depends on T069.

**Checkpoint**: All three user stories independently functional.

---

## Phase 6: Polish & Cross-Cutting Concerns

- [ ] T071 Consolidate every stop onto **one report builder** in both phase-execution references, with three parts: the **condition** that stopped the run, **what already landed** before it did, and the **resume path**. The what-landed part names the commits pushed, the log rows written, and the replies posted so far, and **is explicitly empty on stops that occur before any write** rather than left for the reader to infer. The complete set of stop conditions is: an invalid authenticated account, a corroboration status that is not `match` or `no_record` or one outside the six, a failed observation, an unreadable Feedback Sweep Log row, a consensus outcome requiring human review, a resolved edit target outside the three artifacts, a failed push, one or more amendments requiring re-review, and one or more redaction events reported once every write landed — the last two being the only ones that are not failures. **One stop needs more than the shared contract**: the human-review stop must name **both** operator actions, resolve the substance and re-run **or** resolve the thread, because it is the only stop whose resume path is not satisfied by re-running. **The report also carries every redaction event on every path**, stopping or proceeding — comment id, leg, rule, count, and nothing else about the match. **It also names the byproduct directory as removed, on every path** (FR-004d). **Two stops must not produce two reports** — a run where several conditions hold emits one report naming every condition, with the per-comment dispositions inside it, so every run produces exactly one report on every path. Acceptance: FR-020, FR-012f's report rule, SC-011. Depends on T070.
- [ ] T072 Verify FR-002 by inspection across both platform variants: no row was added to the Workflow Overview table, and the phase-coverage guard's governed phase-id list, the stage-to-phase map, and the workflow template are all unchanged. Acceptance: FR-002; `git diff --name-only` names no template or guard file. Depends on T071.
- [ ] T073 Verify cross-platform parity: `python3 tests/speckit-pro/run-all.py --layer 1`, with `validate-codex-skills` and `validate-codex-parity` passing. Both phase-execution references and both workflow-file-protocol files describe the same sequence. Acceptance: FR-003, SC-007. **Confirm neither `SKILL.md` was touched** — the Codex body's three-word headroom means any addition fails the 8000-word cap. Depends on T072.
- [ ] T074 Regenerate the payload: `python3 scripts/refresh-release-artifacts.py`. This covers the four `dist/` copies of `read_only.py` and `registry.py`, the runner `.sha256`, and the runner `.manifest.json`, and it regenerates the reference `.md` files into both distributions. Acceptance: the six generated paths are byte-identical copies of the source tree; CI's `artifact-consistency` job fails the pull request if this was skipped. **Regenerate — never hand-edit.** Depends on T073.
- [ ] T075 Regenerate the two installed-cache copies of `read_only.py` under `tests/speckit-pro/unit/fixtures/plugin-bash-confinement/installed-cache/claude/` and `.../codex/`. Adding a read-only helper restales them. Acceptance: the plugin-bash-confinement fixtures match the source. **Regenerate — never hand-edit.** Depends on T074.
- [ ] T076 Regenerate the docs-site reference pages: `pnpm --dir docs-site install --frozen-lockfile` once for this worktree, then `pnpm --dir docs-site reference:generate`. The test tree changed — one new test file and a `suite-manifest.json` entry — so this fires. Acceptance: `validate-docs` is not stale. Depends on T075.
- [ ] T077 Run the full gate: `python3 tests/speckit-pro/run-all.py`, zero failures, expecting Layers 1, 4, and 5. Acceptance: constitution IV. While iterating, gate on the changed test file instead — a concurrent `speckit-pro/` edit stales the generated payload and reds roughly six unrelated tests with an opaque `AssertionError: 1 != 0`, which is noise, not signal. Depends on T076.
- [ ] T108 Regenerate the installed-cache copies the four new agent definitions and the `install.py` edit restale, under `tests/speckit-pro/unit/fixtures/plugin-bash-confinement/installed-cache/claude/speckit-pro/` and `.../codex/speckit-pro/`: `agents/sweep-classifier.md` and `agents/sweep-analyst.md` on the Claude side, `codex-agents/sweep-classifier.toml` and `codex-agents/sweep-analyst.toml` on the Codex side, and `speckit_pro_runner/helpers/install.py` on both. `artifact-author` carries exactly this pair of mirrors, which is the precedent for the shape; T075 covers `read_only.py` alone and names no agent path. Acceptance: the plugin-bash-confinement fixtures match the source tree. **Regenerate — never hand-edit.** Depends on T075.
- [ ] T109 Verify the payload regeneration reached the new shipped bytes: the `dist/claude` and `dist/codex` copies of both agent definitions, both TOMLs, and `install.py`; the runner `.sha256` and `.manifest.json`, which the `install.py` edit restales; and `docs-site/src/content/docs/reference/agents.md`, which the reference generator rebuilds and which must list both new agents. T074 and T076 run the two commands that produce all of them; this task is the check that they did, because `agents/` and `codex-agents/` are a payload surface no earlier task in this feature touched. Acceptance: CI's `artifact-consistency` job passes, `validate-docs` is not stale, and `git status` shows no hand-edited generated path. **No task here edits `tests/speckit-pro/layer6-efficiency/fixtures-codex/`** — the corpus binds twelve governed roles through a sha256 chain with no regeneration script, `artifact-author` shipped outside it on both platforms with a green suite, and joining it is the Non-Goals entry deferred to a future G56R spec. Depends on T074, T076, and T108.
- [ ] T078 [P] Generate the PR review packet from `spec.md`'s PR Review Packet Requirements section and the traceability table in `quickstart.md`: what changed, why, non-goals, review order, scope budget, traceability, verification evidence, known gaps, and rollback notes. Traceability maps each major requirement or success criterion to changed files and verification evidence. **Deferred work names its follow-up spec** — slice 2 for artifact freshness, ART-010 for the ready flip, and the **six** items the Non-Goals section records as owned by no spec yet, the sixth being that **the two sweep agents join the Layer 6 governed corpus under a future G56R spec** — a security boundary nothing digest-binds is a gap, deferred because G56R qualification is its own workstream. Record T014's operator-accepted block, both its production-file and its reviewable-LOC half, with the PRSG-013 precedent, in the scope-budget section. Record the T014 budget decision in the scope-budget section.
- [ ] T079 Run the `quickstart.md` validation: all seven scenarios, plus the five-step "Before calling the work done" tail. Acceptance: every scenario's expectations hold; the traceability table's evidence column is true. Depends on T077.
- [ ] T080 Pin the clean-re-run convergence case in `tests/speckit-pro/unit/fixtures/feedback-sweep/comment-corpus.json` and its expected envelope: replay a corpus whose every comment already carries a Feedback Sweep Log row and a posted sweep reply, and assert the second run produces **zero new log rows, zero new replies, zero amendments, and proceeds into task work**. Then assert the qualifier holds in the other direction: replay the interrupted case, where a prior amendment was pushed but its bookkeeping commit never landed, and assert that run's handling of that one item is the documented edge case rather than a violation. Acceptance: SC-003. This is the convergence claim itself, and it was the only success criterion no task cited — the fixture corpus covers the no-second-reply half at T044, but nothing pinned the whole-run assertion or its qualifier. Depends on T044 and T063.

---

## Known Interface Gap

**Recorded rather than resolved, because resolving it is a design decision this
phase may not take.** FR-012b rule 2 requires the write-point path check to run
"in code rather than in judgment", and the plan's Declared File Operations
block lists exactly two Python production files — `read_only.py` and
`registry.py`. The contract as shipped in `contracts/sweep-pr-feedback.md`
carries neither a `feature_dir` input nor any target-validation surface, and
the helper is called once, before classification, when no resolved edit target
exists yet.

T004 adds the input to the contract and T051 implements the check in
`read_only.py`, which is the only declared code file that can host it. What
T004 settles is **the request and response shape of that check at the
write**: it is a named surface of the same registered operation, the form the
third paragraph below extends to the redaction surface. No task below invents
an unregistered second helper operation, because that would add an eighth
registration touch point and a second contract the plan does not budget for.

FR-012f's redaction surface has the same invocation problem — it is called at
three write points, after classification, with text the parse never saw — and
FR-007g's analyst-payload leg calls the same surface once more, at payload
assembly, with a body the parse validated but did not return. Both are bound
by the same rule. Whatever T004 settles carries **both** the pre-write path
test and the redaction surface as named surfaces of the one registered
operation, and pins the redaction surface's request and response shape per
leg — `leg` and `comment_id` on every leg; `lines` in and `lines` plus
`redactions` out on the three outbound legs; `text`, `truncated`, and
`matched_lines` in and `text` plus `report` out on the analyst-payload leg —
so T083 implements and T084 fixtures a shape rather than prose. Settling them
separately mints the eighth touch point this section refuses.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — corrects the design artifacts the rest reads from
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS all user stories
- **User Story 1 (Phase 3)**: Depends on Foundational. **MVP.**
- **User Story 2 (Phase 4)**: Depends on US1 — consensus routing consumes US1's classified candidate list
- **User Story 3 (Phase 5)**: Depends on Foundational; independently testable against US1 and US2, and sequenced after them only because the happy paths must work first
- **Polish (Phase 6)**: Depends on all three stories

### The one dependency that is not conventional

US2 depends on US1 rather than running beside it. The spec says so directly:
US2 "depends on Story 1 having produced a trustworthy classified list." Only
`amended` routes to consensus, and nothing assigns `amended` until US1's
classification exists.

### Within Each User Story

- Every fixture and failing test precedes its implementation task
- Helper branches before the reference prose that documents them
- Claude reference before its Codex mirror, so the mirror has a source

### Parallel Opportunities

- **T001 and T002** — contract and data-model, different files
- **T005, T006** — suite manifest and the new fixture directory
- **T009 and T012** — request fixture and registry entry, both independent of the `read_only.py` chain
- **T035 and T036**, **T055 and T056** — each Claude reference and its Codex mirror are different files
- **T078** — the PR packet needs no code
- **T098 and T099**, **T104 and T105** — each sweep agent's Claude definition and its Codex TOML are different files, so each pair runs in parallel; the two pairs are independent of each other
- **T095** — the Layer 5 carve-out edits `validate-tool-scoping.py` alone, which no other task touches

### What is NOT parallel-safe

`speckit-pro/speckit_pro_runner/helpers/read_only.py` hosts the parse cluster,
the registry cluster, the redaction surface, and three registration touch
points: **T010, T011, T025 through T034, T051, T083, and T086 are strictly
serial.** `speckit-pro/speckit_pro_runner/helpers/install.py` hosts the closed
Codex agent inventory: **T099 and T105 edit the same tuple and are serial with
each other**, and they sit in different phases deliberately, because the bundle
loader returns `incomplete_agent_bundle` for a name whose TOML has not landed,
so each name is added in the phase that ships its file. The corpus tasks T016
through T022, T081, T044 through T046, T084, T066, and T080 all edit
`comment-corpus.json` and are serial. **T015, T023, T024, T082, T088, T043,
T047, T085, T090, T091, T092, T093, T096, T097, T102, and T103 all edit
`tests/speckit-pro/unit/test-feedback-sweep-parse.py` and are serial.**
**T095 edits `tests/speckit-pro/layer5-tool-scoping/validate-tool-scoping.py`
alone**, so it is parallel-safe against every task above; it is written red
before T098, T099, T104, and T105, which turn it green. T037
through T043 and T083, T048 through T054 and T086, T059 through T065, T087,
and T089, T067 through T071, and T100, T101, T106, and T107 each edit **both**
phase-execution references and are serial with each other. The classifier and
analyst dispatch prose lands in T040, T041, T048, T100, T101, T106, and T107;
**`consensus-protocol.md`'s Category-Routed Dispatch routing table is edited by
no task**, T058's `Sweep` row type being that file's only change. T055 and T056 each edit one workflow-file-protocol
file and run in parallel; T057 edits both after them; T058 edits
`consensus-protocol.md` alone.

---

## Parallel Example: Phase 1

```bash
Task: "T001 Correct matched_line to matched_lines in contracts/sweep-pr-feedback.md"
Task: "T002 Correct export.matched_line in data-model.md"
Task: "T005 Register the new test in tests/speckit-pro/suite-manifest.json"
Task: "T006 Create the feedback-sweep fixture directory skeletons"
```

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Phase 1: Setup — correct the three design artifacts
2. Phase 2: Foundational — register the helper, **take the T014 budget decision**
3. Phase 3: User Story 1
4. **STOP and VALIDATE**: quickstart Scenarios 1 and 2 against the fixture corpus

At that point the sweep reads, filters, recognizes, and classifies. It does not
yet amend, so it is an inventory rather than a checkpoint — which is exactly the
slice split the plan **rejected** shipping on its own. MVP here means a
validation milestone, not a shippable increment.

### Incremental Delivery

1. Setup + Foundational → helper registered, budget decision recorded
2. US1 → deterministic classification, both references carry the read
3. US2 → amend, record, reply, stop or proceed
4. US3 → the integrity guard on every corroboration status
5. Polish → regeneration, parity, the full gate

---

## Notes

- `[P]` = different files, no dependencies
- Verify every test FAILS before implementing against it
- Each amendment stages **one path**, never a directory — Phase 7's existing `git add -A` pattern must not be inherited
- The sweep's own files live under the ignored `specs/<feature>/.process/feedback-sweep/` directory and are removed before the run ends (FR-004d); Phase 7's `git add -A` runs after the sweep and must find nothing of the sweep's.
- **Non-goals bound this list.** No task regenerates the draft page set, detects stale pages, or refreshes the pull-request description (all slice 2); resolves a review thread; adds a Workflow Overview phase row; edits a shipped gallery template or its payload copy; edits a Layer 6 corpus agent definition; or writes a state-file mirror. **No task crosses any of them**, and T023 is the one that comes closest — it reads the gallery templates and edits none, so it stays inside the boundary and triggers no payload regeneration.
- Neither `SKILL.md` may gain a line. Three words of headroom on the Codex cap.
