# Error-Handling Checklist: G56R-004 Policy Controls and Adaptive Comparators

**Purpose**: Validate that the requirements governing adaptive signal
resolution, route-bound movement, retry and cancellation outcomes,
platform-initiated reroutes, fail-closed integrity cases, and non-scorable
streak accounting are complete, unambiguous, consistent, and measurable before
task generation.
**Created**: 2026-07-29
**Feature**: [spec.md](../spec.md)
**Depth**: Standard | **Audience**: Reviewer (PR) | **Focus**: error-handling
requirements named in the domain prompt.

**Note**: This checklist is generated by the `/speckit-checklist` command based
on feature context and requirements. It tests the requirements, not the
implementation.

## Adaptive Signal Resolution

- [x] CHK001 Are adaptive signal sources limited to frozen observed members
      rather than prose, timestamps, route names, or dispatch intent?
      [Completeness, Spec §FR-012]
- [x] CHK002 Is the adaptive response set closed to exactly `escalate`, `hold`,
      and `non_scorable`? [Measurability, Spec §FR-013]
- [x] CHK003 Is row-resolution precedence specified as the exact ordered set
      `failure_code > failure_plane > retry_count > budget_threshold >
      terminal_state`? [Clarity, Spec §FR-013]
- [x] CHK004 Are values outside the frozen terminal-state, failure-plane,
      failure-code, retry-count, and resource-budget domains required to fail
      closed rather than default to a response? [Closed by Spec §FR-013]
      [Spec §FR-012, Spec §FR-013]
- [x] CHK005 Are failure-plane responses required to agree with failure-code
      responses under the frozen plane derivation? [Consistency, Spec §FR-013]
- [x] CHK006 Are terminal-state responses required to agree with paired
      candidate-plane failure codes? [Consistency, Spec §FR-013]
- [x] CHK007 Do success criteria require every adaptive signal value to resolve
      exactly once with precedence and consistency exercised by deterministic
      replay? [Acceptance Criteria, Spec §SC-006]

## Route Bounds, Movement, and Streaks

- [x] CHK008 Is the adaptive ladder required to contain only ordered, admitted
      G56R-003 successor-capability tuples from the bound freeze? [Completeness,
      Spec §FR-010]
- [x] CHK009 Are empty ladders, missing routes, duplicates, omitted admitted
      routes, unqualified entries, dynamic discovery, digest drift, and
      effort-order contradictions defined as closed failures? [Coverage,
      Spec §FR-010, Spec §FR-017]
- [x] CHK010 Is the ladder order declared and hash-relevant, with within-model
      effort order and cross-model rationales specified? [Clarity,
      Spec §FR-011]
- [x] CHK011 Are ceiling and floor movement outcomes specified without
      wrap-around? [Edge Case, Spec Acceptance Scenario 6, Spec Edge Cases,
      Spec §FR-014]
- [x] CHK012 Is the clean-pass counter disposition specified when de-escalation
      is evaluated at the floor or otherwise records no ladder step? [Closed by
      Spec §FR-014]
- [x] CHK013 Is "clean pass" defined with terminal state, failure code, retry
      count, and resource-budget trigger criteria? [Clarity, Spec §FR-014]
- [x] CHK014 Is an escalating objective barred from counting as clean?
      [Consistency, Spec §FR-014]
- [x] CHK015 Is a non-scorable objective required to neither advance nor reset
      the clean-pass streak, and does that disposition apply to
      platform-initiated reroutes? [Coverage, Spec §FR-014, Spec §FR-016]

## Retry, Cancellation, and Bound Outcomes

- [x] CHK016 Are retry and cancellation bounds required to declare both scope
      and breach outcome? [Completeness, Spec §FR-015]
- [x] CHK017 Is escalation forbidden from resetting retry or cancellation
      counters? [Consistency, Spec §FR-015]
- [x] CHK018 Is a retry-bound breach mapped to the frozen `failed` and
      `candidate_failed` pairing? [Clarity, Spec §FR-015]
- [x] CHK019 Is a cancellation-bound breach mapped to the frozen `cancelled`
      and `candidate_cancelled` pairing? [Clarity, Spec §FR-015]
- [x] CHK020 Are resource-budget triggers distinguished from retry and
      cancellation bound breaches so the requirements do not invent or omit a
      terminal-state/failure-code pairing? [Closed by Spec §FR-015]
      [Spec §FR-012, Spec §FR-015]
- [x] CHK021 Does replay have to exercise both respected and breached retry and
      cancellation paths? [Acceptance Criteria, Spec §FR-015, Spec §SC-007]

## Platform-Initiated Reroutes

- [x] CHK022 Is a platform-initiated route change identified by the frozen
      `service_reroute` failure code? [Clarity, Spec §FR-016]
- [x] CHK023 Is `service_reroute` required to resolve to `non_scorable` rather
      than a policy escalation? [Consistency, Spec §FR-016]
- [x] CHK024 Is a platform reroute barred from consuming escalation allowance or
      changing ladder position? [Coverage, Spec §FR-016]
- [x] CHK025 Does a platform reroute leave the clean-pass streak unchanged and
      make the whole parent-plus-children unit non-scorable? [Coverage,
      Spec §FR-016]

## Fail-Closed Integrity Cases

- [x] CHK026 Are missing routes and route omissions required to fail closed
      rather than becoming unreachable routes? [Coverage, Spec §FR-010,
      Spec §FR-017]
- [x] CHK027 Are unknown enum values, enum drift, numeric drift, and additional
      unsanctioned divergences required to fail closed? [Coverage, Spec §FR-006,
      Spec §FR-007]
- [x] CHK028 Is frozen binding digest drift required to fail closed without
      repairing the frozen artifact? [Consistency, Spec §FR-004, Spec §FR-005,
      Spec Edge Cases]
- [x] CHK029 Is reserved-objective consumption refused at replay admission,
      smoke planning, and smoke sealing? [Coverage, Spec §FR-032]
- [x] CHK030 Are reserved G56R-011 objectives, selection/cohort-lock objectives,
      and outcome-bearing scored objectives all excluded from G56R-004 evidence?
      [Measurability, Spec §FR-033]

## Smoke and Aggregated Failure Evidence

- [x] CHK031 Are smoke records required to be non-scored and to read
      exact-treatment observables from produced evidence rather than dispatch
      requests? [Completeness, Spec §FR-037]
- [x] CHK032 Are API-key, missing-auth, ambiguous-auth, reserved-objective,
      bound-breach, cache-isolation, and missing-evidence refusal outcomes
      specified for smoke records? [Coverage, Spec §FR-035, Data Model
      §SmokeRecord, Contract §Smoke Sealing]
- [x] CHK033 Is a missing terminal state in parent-plus-children aggregation
      defined as malformed and fail-closed? [Edge Case, Spec Edge Cases,
      Spec §FR-021]
- [x] CHK034 Is null acceptance allowed only as an evidence gap on an otherwise
      completed unit, with non-completed units floored to zero? [Consistency,
      Spec §FR-021]
- [x] CHK035 Are unobserved cache diagnostics preserved as unobserved rather
      than coerced to zero? [Consistency, Spec §FR-022, Spec Edge Cases]
