# LLM-Integration Checklist: CAR-004 Policy Controls and Adaptive Comparators

**Purpose**: Validate that the *requirements* governing how CAR-004 touches the
live model platform are complete, unambiguous, and machine-verifiable before
task generation: the exact treatment each bounded smoke must demonstrate, the
parent-plus-children token and cache accounting, the smoke bounds and their
non-scored labeling, the authentication path and the recorded mode, and cache
isolation between arms.
**Created**: 2026-07-28
**Feature**: [spec.md](../spec.md)
**Depth**: Standard | **Audience**: Reviewer (PR) | **Focus**: the five
llm-integration areas named in the request.

**Note**: This checklist is generated by the `/speckit-checklist` command based
on feature context and requirements. It tests the requirements, not the
implementation.

## Exact-Treatment Demonstration for the Three Smokes

- [x] CHK001 Is the obligation to declare each control's dispatch-time
      parameters, observable signals, bounds, and required evidence rows stated
      as a per-control requirement rather than left to fixture authoring?
      [Completeness, Spec §FR-003]
- [x] CHK002 Is the demonstration obligation stated separately for each of the
      three smokes rather than as one undifferentiated "the smokes work"?
      [Clarity, Spec §FR-031]
- [x] CHK003 Is the adaptive control's escalation target defined by an explicit
      declared ordered sequence rather than inferred from a model name or a
      capability array's serialization order? [Clarity, Spec §FR-011, §FR-011a]
- [x] CHK004 Is the unpinned control's binding to the already-pinned
      parent-session model and effort stated, together with the consequence of a
      different pin? [Completeness, Spec §FR-006, §FR-007]
- [x] CHK005 Is the orchestration unit's membership made decidable from the
      record — a recorded spawning identifier authored at dispatch, transitive
      closure, nested descendants included — rather than resting on the phrase
      "automatically spawned"? [Measurability, Spec §FR-016d]
- [x] CHK006 Is the dispatch-time parameter the adaptive control sets to effect
      an escalation named, given AC-2.17 requires the policy be exercised
      "through the documented dispatch-time model parameter" and FR-003 states
      only that *some* parameters are set? [Closed by §FR-031a.2]
      [Spec §FR-003, §FR-011, §FR-031, PRD AC-2.17]
- [x] CHK007 Is the observable that proves an escalation actually took effect
      named, and is it required to be read back from recorded run evidence
      rather than from the dispatch request that asked for it — the rule the
      frozen CAR-003 treatment runner already applies to the named agent and the
      effective model? [Closed by §FR-031a.1, §FR-031a.3] [Spec §FR-031]
- [x] CHK008 Is the observable that proves a *real* inherit resolution named —
      that the unpinned arm's agents resolved to the pinned parent-session model
      in fact, not merely that the arm was configured to inherit?
      [Closed by §FR-031a.4] [Spec §FR-006, §FR-031]
- [x] CHK009 Is the observable that proves a *real* parallel dispatch named, so
      that a serially dispatched fan-out cannot satisfy the orchestration smoke?
      [Closed by §FR-031a.5] [Spec §FR-031, §FR-016d]
- [x] CHK010 Does the spec state the condition under which a dispatch-time model
      parameter is even capable of deciding the served route, given the frozen
      environment contract carries a `claude_code_subagent_model_unset`
      observation precisely because a session-level subagent-model override
      supersedes it? [Closed by §FR-031a.6] [Spec §FR-031]
- [x] CHK011 Is a smoke whose demonstration cannot be evidenced given a defined
      disposition, rather than leaving "demonstrated" as an operator assertion?
      [Closed by §FR-031a.7] [Spec §FR-031, §SC-009]

## Parent-Plus-Children Token and Cache Accounting

- [x] CHK012 Is the set of additive dimensions that sum across the unit
      enumerated rather than described as "resources"? [Completeness,
      Spec §FR-016]
- [x] CHK013 Are children that failed, timed out, or were cancelled required to
      remain inside the additive sum? [Completeness, Spec §FR-016, §FR-016b]
- [x] CHK014 Are the two non-continuous dimensions given explicit combining
      rules rather than being left to a default? [Completeness, Spec §FR-016a,
      §FR-016b]
- [x] CHK015 Is replay coverage required to prove the additive dimensions equal
      the parent-plus-children sum? [Measurability, Spec §FR-029]
- [x] CHK016 Does "the complete raw token vector" resolve to a named member set,
      given the frozen raw-token vector is closed at four members while the
      frozen Pareto resource vector is eight and carries no reasoning-token
      member, so "complete" and "every dimension the frozen Pareto rule
      consumes" do not denote the same set? [Closed by §FR-016e.1]
      [Ambiguity, Spec §FR-016, §FR-029]
- [x] CHK017 Is the treatment of the reasoning-token member under aggregation
      stated, given the frozen contract requires it to be recorded on every
      attempt while keeping it non-decision-bearing? [Closed by §FR-016e.2]
      [Spec §FR-016]
- [x] CHK018 Is an aggregation rule defined for the cache-write-by-TTL-class and
      cache-read quantities, given `smoke_bounds` declares ceilings on both and
      the orchestration smoke spans a parent and every child — so without one, a
      unit can breach a declared ceiling while reporting compliance?
      [Closed by §FR-016e.3] [Spec §FR-016, Assumptions]
- [x] CHK019 Is it stated that aggregating the cache quantities does not promote
      them into the Pareto dimension set or into the FR-030a raw-token identity,
      so the aggregate cannot silently make a diagnostic quantity
      decision-bearing? [Closed by §FR-016e.4] [Spec §FR-016, §FR-030a]
- [x] CHK020 Is the per-TTL-class key space required to stay identical between
      the declared ceiling and the aggregated measurement, so a ceiling can never
      be keyed differently from what it bounds? [Closed by §FR-016e.3]
      [Consistency, Spec §FR-016, Assumptions]

## Smoke Bounds and Non-Scored Labeling

- [x] CHK021 Are all four smoke bounds stated with frozen values rather than
      ranges or "reasonable" limits? [Clarity, Spec §FR-030]
- [x] CHK022 Is every smoke-bound member required to ride an already-frozen
      budget member name rather than a newly coined one? [Consistency,
      Spec §FR-030, Assumptions]
- [x] CHK023 Is the raw-token ceiling required to exist as its own declared
      member with a machine-checked sum identity rather than as prose?
      [Measurability, Spec §FR-030a, §SC-017]
- [x] CHK024 Is the non-scored status of every smoke row stated as an explicit
      label rather than inferred from the partition's ineligibility?
      [Completeness, Spec §FR-030, §FR-027]
- [x] CHK025 Is the behavior of a run that reaches a bound mid-flight defined —
      stop at the bound, stay valid, never silently exceed? [Coverage,
      Edge Cases, Spec §FR-030]
- [x] CHK026 Is the scope each smoke bound is counted over stated for the
      orchestration-changing control, given FR-014a fixes unit scope for the
      retry and cancellation bounds but left the wall clock, attempt, and
      raw-token ceilings unscoped — so concurrent children could each consume a
      full bound? [Closed by §FR-030b.1, §FR-030b.2, §FR-030b.3]
      [Ambiguity, Spec §FR-030, §FR-014a]
- [x] CHK027 Is it stated whether a child dispatch consumes an attempt against
      `max_attempts`, given the same member is used to express "five
      non-reserved objectives"? [Closed by §FR-030b.4] [Spec §FR-030,
      Assumptions]

## Authentication Path and Recorded Mode

- [x] CHK028 Is the prohibition on requiring API-key authentication stated
      unconditionally rather than scoped to scored work? [Clarity, Spec §FR-030,
      Out of Scope]
- [x] CHK029 Is the reason smoke rows escape the scored-campaign evidence rules
      stated as their non-scored status rather than their authentication mode?
      [Consistency, Spec §FR-030, Out of Scope]
- [x] CHK030 Is the recorded `authentication_mode` *constraining* — does an
      observed `api_key` invalidate the smoke — or is it merely observable?
      FR-030 required the mode be "recorded" and SC-009 required the label be
      present, neither of which refused a run that was in fact API-key
      authenticated. [Closed by §FR-030c.3] [Spec §FR-030, §SC-009,
      PRD AC-2.19 amendment]
- [x] CHK031 Is the frozen contract that owns `authentication_mode` pinned by
      identifier and digest under FR-004, given the repository carries frozen
      fields of that name whose enumerations differ — `subscription | api_key`
      on the Claude side and `chatgpt_subscription | api_key` in the shared
      environment contract — so "the already-frozen field" does not by itself
      identify one? [Closed by §FR-030c.1] [Ambiguity, Spec §FR-030, §FR-004]
- [x] CHK032 Is the recorded mode required to be an observation of the run rather
      than a restatement of the operator's intent or of a product plan?
      [Closed by §FR-030c.2] [Measurability, Spec §FR-030]

## Cache Isolation Between Arms

- [x] CHK033 Is per-run smoke output required to stay out of version control
      while consolidated and contract artifacts are committed? [Completeness,
      Spec §FR-033]
- [x] CHK034 Is the observable that proves cache isolation named, given the
      frozen additive record already publishes an `observed_cache_isolation`
      object carrying both arms' cache-root digests and a disjointness flag — so
      FR-032 stated an obligation with no bound observable, the same defect
      FR-015a was written to remove for the reroute trigger?
      [Closed by §FR-032a.1] [Spec §FR-032]
- [x] CHK035 Is a confirmed isolation breach distinguished from an unobservable
      cache state, and is each given a disposition, given the frozen record
      already fixes distinct closed codes and planes for the two?
      [Closed by §FR-032a.3] [Coverage, Exception Flow, Spec §FR-032]
- [x] CHK036 Is the precommitment to per-arm ephemeral roots distinguished from
      the evidence that isolation actually held, so a declared policy cannot be
      mistaken for an observation? [Closed by §FR-032a.2] [Clarity,
      Spec §FR-032]
- [x] CHK037 Is the isolation obligation scoped to the whole orchestration unit
      rather than to the parent alone, so a child cannot warm another arm's
      cache? [Closed by §FR-032a.5] [Coverage, Spec §FR-032, §FR-016d]
- [x] CHK038 Is the arm pair the isolation claim is made against identified,
      given the acceptance scenario reasons over *consecutive smoke runs for
      different controls* while the frozen observable is shaped as one arm plus
      its paired arm? [Closed by §FR-032a.4] [Ambiguity, Spec §FR-032,
      Acceptance Scenario 21]
- [x] CHK039 Is a cache-root value required to be recorded in a form that cannot
      leak an operator filesystem path into committed evidence?
      [Closed by §FR-032a.6] [Spec §FR-032, §FR-033]

## Agreement With the Frozen Platform Trace Surface

- [x] CHK040 Is the no-new-telemetry constraint stated as an absolute covering
      both new fields and any reopening of the frozen profile? [Completeness,
      Spec §FR-009]
- [x] CHK041 Are the adaptive control's signals required to bind only members the
      frozen contracts already publish as stable? [Completeness, Spec §FR-008]
- [x] CHK042 Is FR-016d's justification accurate about the frozen trace surface?
      It asserted the frozen trace contract "publishes only a parent-session
      configuration string, never a spawn graph", but that contract publishes a
      required parent-child graph of execution-trace identifiers whose
      reciprocity, acyclicity, and root agreement the Claude-side validator
      already enforces — so the rationale a mirroring implementation would copy
      under FR-034 category 7 was wrong even though the dispatch-authored
      conclusion is right. [Closed by §FR-016d] [Conflict, Spec §FR-016d]
- [x] CHK043 Is the CAR-004 unit boundary required to agree with that frozen
      graph wherever a unit member's bound trace carries it, so two records of
      the same run cannot disagree about who spawned what?
      [Closed by §FR-016d] [Consistency, Spec §FR-016d]

## Notes

- Check items off as completed: `[x]`
- Every item above tests the *requirements*, not the delivered artifacts. An
  item reading "is X named" fails when the spec leaves X to Implement, even
  where a design artifact happens to record a value — a Phase 1 artifact cannot
  carry a policy decision the twin must mirror under FR-034 category 7. Two
  items here failed for exactly that reason: data-model.md already said the
  smoke script "refuses `api_key`" and that cache roots are "distinct per
  control", while spec.md required neither.

### Validation record (iteration 1, 2026-07-28)

**Counts**: 43 items — 19 met on first pass, 24 gaps, 24 remediated, 0
remaining.

Gaps clustered into six closures, all additive obligations. Nothing was
weakened to make a gap disappear, no frozen CAR-003 schema was touched, no new
telemetry field was introduced, and API-key authentication was not
reintroduced.

| Closure | Gaps closed | Requirement added or amended |
|---------|-------------|------------------------------|
| Exact-treatment observables and read-back source | CHK006–CHK011 | §FR-031a (new) |
| Cache and reasoning-token accounting through the unit | CHK016–CHK020 | §FR-016e (new) |
| Smoke-bound scope over the orchestration unit | CHK026, CHK027 | §FR-030b (new) |
| Authentication mode made constraining and its owner pinned | CHK030–CHK032 | §FR-030c (new) |
| Cache-isolation observable and breach disposition | CHK034–CHK039 | §FR-032a (new) |
| Frozen trace-surface agreement | CHK042, CHK043 | §FR-016d (amended) |

Each closure also landed its acceptance scenario (40–45), its success criterion
(SC-026 through SC-031), and its FR-034 category 7 handoff entry, so a mirroring
implementation inherits the semantics rather than only the schema shape. plan.md
records the same five as frozen live-integration decision semantics Implement
serializes rather than decides.

**Deliberately out of scope, recorded rather than required here.** Three
adjacent obligations were considered and left to the specs that own them, so a
later reader does not read their absence as an oversight:

| Adjacent obligation | Owner | Why not CAR-004 |
|---|---|---|
| A preflight that establishes route availability before dispatch | CAR-006 | FR-031a.6 records an already-frozen environment observation on CAR-004's own smoke record; it builds no manifest or preflight machinery. |
| Fallback behavior when a dispatched route is unavailable | CAR-005 | CAR-004's ladder is a policy fixture; resolver fallback simulation is CAR-005's surface. |
| Applying the comparison to real arms and the resulting release wording | CAR-011 | CAR-004 freezes the rule and the mapping only. |
