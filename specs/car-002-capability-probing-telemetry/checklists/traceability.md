# Traceability Checklist: CAR-002 Capability Probing, Telemetry Profile, and Exact-Treatment Contract

**Purpose**: Unit-test the *requirements* (spec.md, plan.md, data-model.md) for traceability quality — that every CAR-001 capability question (CAP-Q1..CAP-Q6) maps to a probe step and a snapshot field or an explicit open/deferred disposition; that every AC-2.2/2.3/2.4 clause and roadmap CAR-002 scope bullet maps to an FR; that every design-concept decision (Q1–Q8) is honored or explicitly revised with a note; that the telemetry profile's field classifications match the roadmap verbatim (stable_native / derived / derived_from_controlled_configuration; nulls preserved); and that each FR and task traces to exactly one of the three work packages so split-PR emission stays clean. Tests whether the traceability requirements are complete, unambiguous, consistent, and measurable — not whether code works.
**Created**: 2026-07-16
**Feature**: [spec.md](../spec.md)

## Capability-Question Traceability (CAP-Q1..CAP-Q6)

- [x] CHK001 Is every capability question CAP-Q1..CAP-Q6 mapped to a probe step in the requirements — per-alias canary + configuration-acceptance for CAP-Q1..Q4, dual-surface unavailable probe for CAP-Q5, route-change detection rule for CAP-Q6? [Completeness, Spec §FR-003/§FR-006/§FR-008/§FR-009; manifest `capability_questions`]
- [x] CHK002 Is every CAP-Q1..CAP-Q6 mapped to a snapshot field or an explicitly recorded open/deferred disposition, with the mapping distinguishing "answered with evidence" from "recorded open"? [Completeness, Spec §FR-007; data-model `capabilityAnswer` {status: answered|open}]
- [x] CHK003 Are the alias-binding halves of CAP-Q1 (opus→`claude-opus-4-8`), CAP-Q2 (sonnet→`claude-sonnet-5`), CAP-Q3 (haiku→`claude-haiku-4-5-20251001`), and CAP-Q4 (fable→`claude-fable-5`) each traced to the per-alias canary observation that records `resolved_dated_model_id`? [Completeness, Spec §FR-006; data-model `resolved_dated_model_id` (CAP-Q1..Q4)]
- [x] CHK004 Is the effort-acceptance half (manifest EFF-1: "available effort levels are model-dependent") of CAP-Q1..CAP-Q4 traced distinctly from alias binding, so alias resolution alone is not treated as certifying a (model, effort) tuple? [Clarity, Spec §FR-003 configuration-acceptance check + §FR-027; manifest CAP-Q1..Q4 EFF-1]
- [x] CHK005 Is CAP-Q5 traced to both dispatch surfaces the question spans — recognizing the manifest phrases CAP-Q5 as subagent-frontmatter dispatch while the requirements additionally probe the `-p --model` surface — with each surface recorded separately? [Consistency, Spec §FR-009 vs manifest CAP-Q5; data-model `unavailableObservation.surface`]
- [x] CHK006 Is CAP-Q6 traced as a detection-rule-only open disposition (route-change rule over observed-vs-resolved IDs, recorded open in the primary bounded matrix), matching the manifest's execution-time re-pointing question? [Consistency, Spec §FR-008; data-model `capabilityAnswer` CAP-Q6 open; manifest CAP-Q6]
- [x] CHK007 Is CAP-Q4's availability half (fable "available/accessible where your organization has access," entitlement-gated) traced to a probe observation or an explicit labeled-gap disposition, distinct from the binding question? [Coverage, Spec §FR-007 + Assumptions "Interference-surface set" org-restriction gap; manifest CAP-Q4]

## Acceptance-Criteria & Roadmap-Scope Traceability (AC-2.2/2.3/2.4 → FR)

- [x] CHK008 Does every AC-2.2 snapshot element (probed model IDs, alias-to-ID bindings, supported efforts, client version, retrieval/probe method, timestamp, raw evidence) trace to an FR? [Completeness, Spec §FR-003/§FR-006/§FR-011/§FR-012/§FR-014 + Key Entities "retrieval/probe method... per AC-2.2"; workflow AC-2.2]
- [x] CHK009 Is AC-2.2's "retrieval/probe method" recorded as an explicitly distinguishable element separating the bounded exact-invocation probe from the API-models-endpoint (API-key) path? [Clarity, Spec §FR-014 + Key Entities "runtime capability snapshot"; workflow AC-2.2]
- [x] CHK010 Does every AC-2.3 route_resolution/replay binding field (candidate_route_id, agent_contract_id, runtime_capability_snapshot_id, route_resolution_id, requested + effective model, configured effort, fallback index + reason, instruction hash, mutation contract, raw token categories incl. cache-writes-by-TTL and cache-reads, nulls preserved) trace to an FR or a record field? [Completeness, Spec §FR-021/§FR-022; data-model Record 3/Record 4]
- [x] CHK011 Is AC-2.3's "platform-initiated route change recorded separately from resolver fallback and makes the run non-scorable" traced to the misdelivery-vs-fallback precedence rule? [Consistency, Spec §FR-025 precedence rule; data-model misdelivery rule (R11)]
- [x] CHK012 Is AC-2.3's raw-token-category linkage (cache writes by TTL class, cache reads, nulls preserved) resolvable from the exact-treatment outcome to the telemetry-profile field set? [Coverage, Spec §FR-022 telemetry linkage + §FR-019; data-model `outcome.telemetry_ref`]
- [x] CHK013 Do AC-2.4 and the roadmap CAR-002 scope classify the effective model as `stable_native` while FR-019 and data-model classify the (modelUsage-extracted) effective model as `derived`, leaving the divergence from the roadmap-verbatim label unreconciled by any explicit revision note? [Resolved, Spec §FR-019 + data-model — remediated: effective model reclassified `stable_native` per AC-2.4/roadmap verbatim with an explicit revision note (modelUsage key-set extraction is a read of a platform-returned value, not a derivation)]
- [x] CHK014 Do the remaining AC-2.4 classifications — raw token vector `stable_native`, client-side cost estimates `derived`, effective reasoning effort `derived_from_controlled_configuration` (never a returned value), conditional/unavailable fields null — each match the roadmap verbatim and trace to FR-019/FR-020? [Consistency, Spec §FR-019/§FR-020; workflow AC-2.4]
- [x] CHK015 Does the roadmap CAR-002 scope (bullet 4) name "dispatch namespace" and "parent-session configuration" among the route_resolution/exact-treatment bindings while neither FR-021 nor the data-model route_resolution field set binds them and no note defers or revises them? [Resolved, Spec §FR-021 + Key Entities + data-model Record 3 — remediated: `dispatch_namespace` and `parent_session_configuration` added to the route_resolution binding per the roadmap bullet]
- [x] CHK016 Does each roadmap CAR-002 scope bullet (snapshot capture; unavailable-model behavior; telemetry profile; route_resolution/exact-treatment schemas; platform route-change detection; authentication mode per run; synthetic-replay validation of the four record classes) trace to at least one FR? [Completeness, Spec §FR-003–§FR-025; roadmap CAR-002 "Scope"]
- [x] CHK017 Is AC-2.2's "unresolved availability blocks that route's scored run" traceably assigned — CAR-002 records the open/gap disposition (FR-007) while the scoring-gate consequence is deferred to CAR-003 (out of scope) — rather than left unowned? [Clarity, Spec §FR-007 + §Out of scope + §SC-008; workflow AC-2.2]

## Design-Concept Decision Traceability (Q1–Q8)

- [x] CHK018 Is design-concept Q1 (operator-only live probing; deterministic repo/CI tests) honored by an FR? [Completeness, Spec §FR-001/§FR-002; design concept Q1]
- [x] CHK019 Is Q2 (one canonical snapshot at `docs/ai/research/…`, re-probe replaces in place, git preserves priors) honored by an FR? [Completeness, Spec §FR-011; design concept Q2]
- [x] CHK020 Is Q3 (dedupe 37 routes to unique (model, effort) tuples, ~20-invocation bound, every route cites its tuple's shared evidence) honored by an FR? [Completeness, Spec §FR-003/§FR-004/§SC-005; design concept Q3]
- [x] CHK021 Is Q4 (both `-p` and subagent-frontmatter unavailable-model surfaces; fallback/override unset-proof) honored by an FR? [Completeness, Spec §FR-009/§FR-010; design concept Q4]
- [x] CHK022 Is Q5 (one platform-neutral JSON Schema with four `$defs` + stdlib validator, CAR-001 style) honored by an FR? [Completeness, Spec §FR-015/§FR-016/§FR-017; design concept Q5]
- [x] CHK023 Is Q6 (fail-closed probe writer + deterministic CI test, defense in depth) honored by an FR? [Completeness, Spec §FR-023/§FR-024; design concept Q6]
- [x] CHK024 Is Q7 (full sanitized `--output-format json` payload + SHA-256, distinct from CAR-001's bounded extract) honored by an FR? [Completeness, Spec §FR-012/§FR-013 + Key Entities `rawEvidence`; design concept Q7]
- [x] CHK025 Is Q8 (accepted 3-vertical-slice split) honored, AND is the Plan-time WP1-consolidation refinement (schema-foundation surfaces that Q8's narrative split across WP1/WP2 moved into WP1 alone) recorded as an explicit note under design-concept Open Question 5 rather than a silent deviation? [Consistency, Spec §Reviewability Budget "Split decision" + Plan "WP1 sizing"; design concept Q8 + Open Question 5]

## Telemetry Profile Classification Traceability

- [x] CHK026 Does the requirement mandate exactly one classification label per telemetry field from the closed set {`stable_native`, `derived`, `derived_from_controlled_configuration`, `unavailable`}, enforced by validation? [Measurability, Spec §FR-019/§SC-006; data-model `classification` enum + §FR-024]
- [x] CHK027 Is null-preservation traced as a distinct, validated requirement (nulls recorded and classified, "unavailable" distinguishable from "absent"), not conflated with the label set? [Clarity, Spec §FR-020/§SC-006; workflow AC-2.4 "conditional/unavailable fields null"]
- [x] CHK028 Is the crosswalk from the CAR-001 manifest `source_class` taxonomy (official_documentation→`stable_native`; derived_from_controlled_configuration→same; undocumented/runtime_verification_needed→`unavailable`) to the profile labels documented, so each classification is traceable to manifest evidence? [Traceability, Spec Key Entities "Telemetry capability profile" crosswalk; manifest `telemetry_requirements[].source_class`]
- [x] CHK029 Is the pinned client version recorded as a profile field (not encoded in the ID) so the classification set is scoped to a specific client? [Completeness, Spec §FR-018 + Key Entities "Telemetry capability profile"; workflow AC-2.4 "for the pinned client"]

## Work-Package Traceability (each FR/task → exactly one WP)

- [x] CHK030 Does the plan assign every declared production/modified file to a work package via the per-WP file table — including showing the two recurring test files (`test-efficiency-claude-telemetry.py` NEW@WP1/MOD@WP2/MOD@WP3; `test-speckit-pro-runner.py` MOD@WP1/WP2) as incrementally extended — so split-PR emission can route each file change to exactly one WP? [Completeness, Plan §"Per-work-package file assignment"]
- [x] CHK031 Does the plan's per-WP table reference only ~14 representative FRs while roughly half the functional requirements (e.g., FR-002, FR-004–FR-008, FR-010, FR-012–FR-014, FR-022) carry no explicit single-WP assignment, so "each FR traces to exactly one WP" must be inferred from which file implements the FR rather than being stated? [Resolved, Plan — remediated: complete FR→WP table added (WP1: 20 FRs, WP2: 5, WP3: 3; every FR traces to exactly one primary WP, global invariants noted)]
- [x] CHK032 For records whose `$def` is authored in WP1 but whose fixtures land later (route_resolution fixture in WP2; four exact-treatment fixtures in WP3), is the schema-authoring-vs-fixture-authoring boundary explicitly assigned so the WP seam is unambiguous? [Consistency, Plan dependency-order note "WP2 consumes the schema WP1 published"; Spec §Reviewability Budget "Split decision"]
- [x] CHK033 Are the cross-cutting FRs that apply to all three work packages (FR-002 determinism; FR-026/FR-027 evidence authority) captured as global constraints rather than mis-forced into a single WP? [Consistency, Plan §Technical Context "Constraints"]
- [x] CHK034 Each task in the upcoming tasks.md MUST carry an explicit single-WP tag (WP1/WP2/WP3) matching the plan's per-WP file assignment, so split-PR emission routes every task to exactly one PR. [Traceability, tasks.md (Tasks phase) ← Plan §"Per-work-package file assignment"] [Satisfied at Tasks: all 40 tasks carry a single [WP1]/[WP2]/[WP3] tag; verify-tasks confirmed 0 untagged.]
- [x] CHK035 Each task in the upcoming tasks.md MUST cite the FR(s)/SC(s) and file(s) it implements, so the FR→file→WP chain is traceable per task and no task spans two work packages. [Traceability, tasks.md (Tasks phase) ← Plan §Declared File Operations] [Satisfied at Tasks: every task cites FR/SC markers + an exact file path; FR→Task table covers all 28 FRs; no task spans two WPs.]
- [x] CHK036 Does the plan preserve the Clarify-ratified 3-WP boundary (recording the WP1 over-ceiling sizing as a G5 escalation rather than silently re-slicing), so the WP seams that FRs/tasks trace to remain stable? [Consistency, Plan §"WP1 sizing — mechanical estimator vs. real reviewable LOC (G5 escalation)"; Spec §Reviewability Budget "Split decision"]

## Cross-Reference Identity & Join Traceability

- [x] CHK037 Is a consistent ID scheme established for all committed CAR-002 identities (`runtime_capability_snapshot_id` = `CAR-002-RCS-<YYYY-MM-DD>-V<n>`; `telemetry_profile_id` = `CAR-002-TP-<YYYY-MM-DD>-V<n>`; `route_resolution_id` pattern) and reused CAR-001 cross-references (`agent_contract_id` = `car.<name>.v<n>`; `candidate_route_id` = `CAR-001-CR-<NN>-<NN>`)? [Traceability, Spec §FR-021 + Assumptions "ID conventions"]
- [x] CHK038 Are cross-reference IDs pattern-constrained identically everywhere they appear across `$defs` (the same pattern for a given ID in the snapshot, route_resolution, and telemetry_profile `$defs`), so no `$def` accepts as free text an ID another `$def` enforces? [Consistency, Spec §FR-015 + §FR-021]
- [x] CHK039 Is referential integrity a validated requirement — every committed candidate_route_id/agent_contract_id resolves to an existing CAR-001 manifest entry, and every non-null telemetry_ref/runtime_capability_snapshot_id resolves to a committed record — failing closed on a dangling reference? [Measurability, Spec §FR-024 + §FR-022; data-model join (SC-005)]
- [x] CHK040 Is the 37-route→(model, effort)-tuple join specified as derived-not-persisted, with validation requiring every one of the 37 CAR-001 candidate routes to resolve to exactly one tuple (zero or more-than-one fails closed)? [Measurability, Spec §FR-004/§SC-005 + §FR-024; data-model "Relationships and derived join"]
- Orchestrator remediation 2026-07-16: CHK013 (effective-model label conflict — PRD verbatim prevails, revision note recorded), CHK015 (roadmap bindings dispatch_namespace + parent_session_configuration added to FR-021/Record 3), CHK031 (complete FR→WP table added to plan.md). CHK034/CHK035 remain unchecked as forward requirements on the upcoming tasks.md (satisfied at the Tasks phase, not gaps). Zero items routed to consensus.
