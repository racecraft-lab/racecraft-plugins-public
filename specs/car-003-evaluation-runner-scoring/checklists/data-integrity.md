# Data Integrity Checklist: Evaluation Runner, Fixtures, Scoring, and Statistical Analysis

**Purpose**: Validate that the CAR-003 requirements governing evidence immutability, single-source-of-truth references, digest/preimage pinning, invalidation scope, partition disjointness, and committed-evidence boundaries are complete, unambiguous, internally consistent, and objectively verifiable before implementation.
**Created**: 2026-07-24
**Feature**: [spec.md](../spec.md)

**Depth**: formal release gate. **Audience**: reviewer (PR). **Focus clusters**: archived-evidence immutability and non-reuse; reference graph and duplicated source of truth; digest preimage pinning; invalidation scope and blast radius; committed-evidence boundary.

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements. It tests the requirements, not the implementation.

## Archived CAR-002 Evidence: Immutability and Non-Reuse

- [x] CHK001 - Are the requirements explicit that immutability of the archived snapshot does **not** imply reusability of its tuples? [Clarity, Spec §FR-044, §Clarifications S1]
- [x] CHK002 - Is there a requirement making the non-reuse of archived tuples **machine-checkable**, rather than asserted only in prose? Every admitted tuple binds a `source_evidence_digest` and a `runtime_evidence_digest`, but no requirement constrains those digests to CAR-003's own `runtime_snapshot_binding`, so a record copied from the archived snapshot would satisfy every stated rule. [Gap, Spec §FR-044, §FR-003]
- [x] CHK003 - Do the requirements define how a **diagnostic-only** collection result is represented as a distinct artifact from an authoritative freeze? The freeze contract requires at least one admitted tuple unconditionally and carries no authoritative/diagnostic status field, so the fail-closed state named in FR-028 and FR-044 has no stated representable form. [Gap, Spec §FR-028, §FR-044]
- [x] CHK004 - Are the requirements specific about which CAR-002 surfaces are read-only inputs versus which are merely not-written? [Clarity, Spec §FR-001, §SC-001]
- [x] CHK005 - Is "CAR-002 artifacts remain unchanged" expressed as an objectively measurable property (paths and IDs) rather than an intent? [Measurability, Spec §SC-001]
- [x] CHK006 - Are requirements defined preventing any CAR-003 record from **shadowing** an archived identifier — that is, reusing a CAR-002 ID namespace for a new record? [Coverage, Spec §FR-001, §FR-010]
- [x] CHK007 - Do the requirements state that the frozen `exactTreatmentReplay.outcome` shape and the closed `record_class` enumeration are never extended, and name where the displaced information lives instead? [Completeness, Spec §FR-010, §FR-045]

## Reference Graph and Single Source of Truth

- [x] CHK008 - Is the "reference, never embed" rule stated once as a global invariant rather than restated per bundle? [Consistency, Spec §FR-010, §FR-032]
- [x] CHK009 - Are requirements defined for the case where the score bundle's `resource_vector` and the bound execution trace's telemetry **disagree**? The eight decision-bearing dimensions are recorded in the score bundle while the same observations exist in the trace, and no requirement states which is authoritative or that they must be derived from and equal to one another. [Gap, Spec §FR-018, §FR-032]
- [x] CHK010 - Are the requirements unambiguous that a score bundle references its trace by ID and digest and never copies trace content? [Clarity, Spec §FR-010]
- [x] CHK011 - Are requirements defined for detecting a **dangling** reference distinctly from a **mismatched** digest, and do both produce the same closed outcome? [Coverage, Spec §FR-032]
- [x] CHK012 - Is the failure vocabulary for reference-integrity failures consistent across the artifacts that can record it? `trace_reference_integrity_failure` is available as an assignment invalidation and a decision reason, but the score bundle's invalidation vocabulary is pinned verbatim to the twin's nine-member set and cannot express it. [Consistency, Spec §FR-032, §FR-034]
- [x] CHK013 - Do the requirements state that a reference-integrity failure blocks the decision bundle rather than repairing either artifact by rewrite? [Clarity, Spec §FR-032]
- [x] CHK014 - Are requirements defined for the ordering relationship between "trace exists for every assigned attempt" and "score bundle exists only for score-eligible attempts"? [Completeness, Spec §FR-010, §FR-030]

## Digest Preimage Pinning

- [x] CHK015 - Is the canonical JSON preimage stated precisely enough to be reproduced independently (sorted keys, minimal separators, UTF-8, no NaN)? [Measurability, Spec §FR-032, §FR-033]
- [x] CHK016 - Is the fixture/scorer preimage explicitly defined as the artifact record **excluding its own digest field**, closing the self-reference? [Clarity, Spec §FR-033]
- [x] CHK017 - Is the materialization content-hash preimage clearly distinguished from the canonical-JSON preimage, including that it is bytes read back from disk and never an in-memory buffer? [Clarity, Spec §FR-008]
- [x] CHK018 - Are the requirements explicit that the destination path is verified separately and is **not** folded into the materialization digest preimage? [Completeness, Spec §FR-008]
- [x] CHK019 - Do the requirements state that digest verification happens at **both** bundle acceptance and replay, not only at write time? [Completeness, Spec §FR-032, §FR-033]
- [x] CHK020 - Is a preimage defined for the partition **objective-set digest**? FR-033 enumerates fixture, oracle, corpus, rubric, scorer, and adjudicator digests, and the objective-set digest that carries FR-013's disjointness evidence is absent from that enumeration, leaving its preimage unpinned. [Gap, Spec §FR-013, §FR-033]
- [x] CHK021 - Are requirements defined for which digest algorithm and encoding format every digest field uses, consistently across all artifacts? [Consistency, Spec §FR-032, §FR-033]
- [x] CHK022 - Can "content-hash identical" be objectively distinguished from "parsed-field equivalent", with the drift classes that separate them enumerated? [Measurability, Spec §FR-008, §Clarifications S2]

## Freeze-Bound Identity Disambiguation

- [x] CHK023 - Are the three identically named identity sources — CAR-003's own successor freeze, the run-time route-resolution field, and the archived snapshot — explicitly disambiguated in the requirements? [Clarity, Spec §FR-039, §Clarifications S1]
- [x] CHK024 - Is the freeze-bound identity's provenance **verifiable** rather than asserted? The attribution record names its source with a fixed literal but carries no `{id, digest}` binding to the freeze it was read from, so at replay there is no way to confirm the identity came from CAR-003's successor freeze rather than from the archived snapshot. [Gap, Spec §FR-039, §FR-045]
- [x] CHK025 - Are all five detection observables individually required, including the pinned client version at both freeze time and run time? [Completeness, Spec §FR-039]
- [x] CHK026 - Are requirements defined distinguishing a behavioral divergence with no identity change from an alias re-point? [Coverage, Spec §FR-045]
- [x] CHK027 - Is the attribution claim explicitly recorded as **bounded** by its enumerated cause set rather than as proof of platform causation? [Clarity, Spec §FR-045]
- [x] CHK028 - Are requirements defined for the unresolved case, and does it block admission rather than degrade to a default? [Coverage, Spec §FR-039]
- [x] CHK029 - Is the detector's validation status required to be recorded honestly when the synthetic replay path cannot be built? [Measurability, Spec §FR-046]

## Invalidation Scope and Blast Radius

- [x] CHK030 - For each of the four refresh triggers, are both the invalidated set and the surviving set enumerated, rather than only the invalidated set? [Completeness, Spec §FR-041]
- [x] CHK031 - Is "marked invalidated rather than rebound" stated unambiguously for already-bound pairs? [Clarity, Spec §FR-041, §FR-037]
- [x] CHK032 - Are the requirements consistent that a source-ledger change alone cannot admit a tuple the pinned runtime never supported? [Consistency, Spec §FR-041, §FR-003]
- [x] CHK033 - Are the alias-re-point trigger's **additional** effects on in-flight attempts specified distinctly from the three shared effects? [Clarity, Spec §FR-041]
- [x] CHK034 - Are requirements defined bounding a fixture or scorer version bump to **exactly** the affected candidate results — neither over-invalidating unaffected bundles nor under-invalidating affected ones? [Coverage, Spec §FR-034]
- [x] CHK035 - Is every invalidation required to be additive, with prior bundles left unmutated? [Consistency, Spec §FR-034, §FR-041]
- [x] CHK036 - Are requirements defined for an unresolved availability blocking that route's scored run, distinct from excluding the route from the freeze? [Coverage, Spec §FR-029, §FR-039]

## Partition Disjointness and Binding Acyclicity

- [x] CHK037 - Is the unit of disjointness stated unambiguously as the **objective**, not the fixture, task, or partition? [Clarity, Spec §FR-013, §Clarifications S4]
- [x] CHK038 - Are `partition_type` and `qualification_eligible` required to be immutable after freeze, with calibration always ineligible? [Completeness, Spec §FR-013]
- [x] CHK039 - Does the requirement set avoid **reintroducing** the circular dependency that FR-037 resolved? The pair-level substitution replaces the analysis-plan binding with a calibration-protocol binding, but every assignment also binds an experiment policy, and the experiment policy binds the analysis plan unconditionally — so a calibration pair still transitively requires the plan that only freezes after calibration. [Gap, Spec §FR-037, §FR-038]
- [x] CHK040 - Are requirements defined for a pre-execution binding record on a partition that is neither calibration nor qualification-eligible? The stated substitution covers the calibration case and the qualification-eligible case, leaving a partition that is both non-calibration and ineligible bound to neither a plan nor a protocol. [Gap, Spec §FR-037, §FR-013]
- [x] CHK041 - Is the calibration protocol required to carry no margins, sample sizes, or terminal thresholds, so that it cannot silently become a shadow analysis plan? [Clarity, Spec §FR-037]
- [x] CHK042 - Is the direction of the plan-to-protocol reference stated, so the resolution is a substitution rather than a mutual dependency? [Consistency, Spec §FR-037]
- [x] CHK043 - Are the requirements consistent about which budget is decision-bearing, and is the equality condition scoped to qualification-eligible partitions only? [Consistency, Spec §FR-038]

## Committed-Evidence Boundary

- [x] CHK044 - Is the committed-evidence allowlist deny-by-default, with non-allowlisted fields **blocking publication** rather than being silently stripped? [Clarity, Spec §FR-027, §FR-036]
- [x] CHK045 - Are requirements defined governing which evaluation outputs are committed and which stay ignored? The design concept records that the current ignore rule is a blanket `results/` with no allow rule for consolidated baselines, and the plan schedules that file for modification, but no requirement states what a consolidated baseline may contain or that per-run raw outputs stay ignored. [Gap, §Design Concept, §Plan Declared File Operations]
- [x] CHK046 - Are requirements explicit that raw captures inherit the existing sanitization contract rather than defining a parallel one? [Consistency, Spec §FR-027]
- [x] CHK047 - Is the prohibition on committing raw model, CLI, prompt, and response bytes stated for scorer evidence as well as capability evidence? [Coverage, Spec §FR-027, §FR-036]
- [x] CHK048 - Are requirements defined for the operator-only retention store as the destination for denied fields, so denial has a specified alternative? [Completeness, Spec §FR-027]
- [x] CHK049 - Can the boundary condition be objectively verified on committed artifacts rather than assessed by judgment? [Measurability, Spec §SC-015]

## Vocabulary Reuse and Cross-Platform Parity

- [x] CHK050 - Are the requirements explicit that closed enumerations in the byte-identical shared contracts are reused verbatim and never unilaterally extended? [Clarity, Spec §FR-030, §FR-034]
- [x] CHK051 - Is the rationale for reuse stated as a correctness constraint (one-sided validation failure) rather than a style preference? [Clarity, Spec §FR-034]
- [x] CHK052 - Where a CAR-003 artifact adds fields the twin does not carry, are those additions individually justified against a named closed contract that cannot hold them? [Traceability, Spec §FR-018, §FR-045, §FR-049]
- [x] CHK053 - Are the key vocabularies for cache-write TTL classes pinned, so a budget ceiling and a diagnostic record can be matched? Both the ceiling map and the diagnostic breakdown are keyed by open-ended strings with no shared closed class set, so differing keys would leave the ceiling silently unenforced. [Gap, Spec §FR-018, §FR-022]
- [x] CHK054 - Is "logically identical to the twin" defined precisely enough to be checked — set-equality of enumerations and dimension sets rather than textual similarity? [Measurability, Spec §FR-014, §FR-018, §FR-034]

## Fail-Closed Completeness

- [x] CHK055 - Is "every required gate" defined, so the precondition on ballot collection is decidable? The hard-gate set is closed at seven members, but the requirements never state whether all seven are required for every role or whether a per-role subset applies. [Gap, Spec §FR-014]
- [x] CHK056 - Are requirements defined making `scorable` necessary but not sufficient, with the sufficient predicate enumerated separately? [Clarity, Spec §FR-030]
- [x] CHK057 - Is the co-occurrence rule explicit that all fired codes are unioned and no non-terminal cause is discarded? [Completeness, Spec §FR-031]
- [x] CHK058 - Is the terminal-disposition precedence total and stated in the requirements rather than left to implementation? [Measurability, Spec §FR-031]
- [x] CHK059 - Are requirements defined for unclassifiable attrition as an evidence-boundary failure rather than silently folding it into a known plane? [Coverage, Spec §FR-021]
- [x] CHK060 - Are requirements defined so that a non-executable role produces no score bundle **and** is never counted as attrition, keeping corpus completeness and statistical denominators independent? [Consistency, Spec §FR-012]

## Remediation Log

60 items assessed; 10 `[Gap]` findings, all remediated and re-verified.

| Item | Fix | Landed in |
|---|---|---|
| CHK002 | Admitted tuples' runtime evidence must resolve to this freeze's own snapshot binding; archived resolution rejected `availability_not_proven`; `historical_freeze_binding` is provenance-only | FR-044, data-model |
| CHK003 | Authority failures ⇒ **no freeze record emitted**; diagnostics ride the collection record, so a freeze's existence is the publication signal | FR-028/FR-044, data-model |
| CHK009 | Trace is sole source of truth; `resource_vector` and reasoning report are re-derived projections compared at acceptance and replay, mismatch ⇒ `binding_digest_mismatch` | FR-032, plan |
| CHK020 | Objective-set digest added to the FR-033 enumeration with its preimage (deduplicated, sorted `objective_ids`) | FR-033, `experiment-assignment` |
| CHK024 | `candidate_freeze_binding` `{id, digest}` added and required — provenance verifiable, not self-declared | FR-039, `car-003-additive-records`, data-model |
| CHK039 | Transitive cycle stated (an ineligible partition's policy binds the protocol); schema change deferred as a **joint cross-platform change** per the FR-049 rule | FR-037, plan Known Gaps |
| CHK040 | Binding branches re-keyed on `qualification_eligible` so they are exhaustive; the ineligible non-calibration hole is closed | FR-037, `experiment-assignment` |
| CHK045 | Consolidated baselines committed only after the deny-by-default scan; file-level allow only, never directory-wide; both halves verified | FR-027 |
| CHK053 | TTL key space closed to `ephemeral_5m` / `ephemeral_1h` — reusing CAR-002's already-observed `stable_native` cache-creation classes — across the diagnostic and both budgets | FR-022, three contracts, data-model |
| CHK055 | "Required gate" defined as all seven, unique per bundle; a missing result fails closed `required_evidence_missing` rather than reading as a pass | FR-014 |

## Notes

- Items marked `[Gap]` identify requirements that were missing, undecidable as written, or asserted in prose without a stated verifiable form. All ten are resolved above; the marker text is retained so the finding remains auditable.
- Items referencing `§Clarifications` check that a resolved Clarify decision is faithfully carried into the requirement text; they are not an invitation to re-litigate the decision.
- Repo-level shared contracts under the Layer 6 test tree are byte-identical across the Claude and Codex worktrees. Any item here that would be resolved by editing one of those three files is instead a cross-platform coordination item.
