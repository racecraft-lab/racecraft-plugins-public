# Error-Handling Checklist: Roadmap-MOC home note from PRD + coach the two-zone structure

**Purpose**: Validate that PRSG-004's error/failure-mode behaviors are completely, clearly, and consistently SPECIFIED in the requirements — specifically the home-note discovery error paths (absent / malformed-sentinel / missing-INDEX-pair), SPEC-MOC frontmatter degradation (`status` AND `spec_id`), render_index determinism + atomic-write-under-failure, and the byte-identical spec-MOC regression guard. Unit tests for the requirements, NOT the implementation or the generator's behavior.
**Created**: 2026-06-08
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements. It is the error-handling sibling of the existing `documentation-quality.md` checklist; it does NOT re-test Domain-1's Codex-parity items (FR-020/SC-008) and leaves that tightening intact.

## Home-Note Discovery — Absent / No-Op (clean exit, no error)

- [ ] CHK035 - Is the absent-home-note case (glob `docs/ai/specs/*-roadmap-MOC.md` matches 0) specified as a clean no-op success — no INDEX rows, no error, and the `specs/` scan proceeding unaffected? [Coverage, Spec §FR-017, §Edge Cases, Contract §"Empty / absent behavior"]
- [ ] CHK036 - Is the no-op behavior for the home-note path tied to the SAME no-`specs/`-directory no-op success precedent (so a reader knows a missing home note is not an error condition)? [Consistency, Spec §Edge Cases, Contract §"Empty / absent behavior"]
- [ ] CHK037 - Is a non-gated / legacy / fenceless home note (present but failing `moc_is_gated`) specified as SKIPPED (not an error), consistent with the spec-MOC gating? [Coverage, Spec §FR-017, Contract §"Empty / absent behavior"]
- [ ] CHK038 - Is the home-note-present-but-`specs/`-empty-or-all-legacy case specified to render an EMPTY (link-free) INDEX rather than failing? [Edge Case, Contract §"Empty / absent behavior"]

## Home-Note Discovery — Malformed / Unbalanced Sentinels (fail-safe exit 2, no partial write)

- [ ] CHK039 - Is the malformed/unbalanced GENERATED:INDEX sentinel pair in the HOME NOTE (e.g. orphan START, duplicated END, START-without-END) specified to fail safe with exit 2 and an actionable stderr line? [Coverage, Exception Flow, Spec §FR-002, Contract §"Empty / absent behavior", §"Zone framing & assembly"]
- [ ] CHK040 - Does the spec/contract state that an unbalanced home-note marker pair produces NO partial write (the poison-map batch abort happens in PASS 1 before any write)? [Exception Flow, Spec §FR-019, Contract §"Determinism & idempotence"]
- [ ] CHK041 - Is the exit-code contract for the home-note path stated unambiguously (0 = current/clean, 1 = `--check` stale, 2 = error) so a deliberate `--check` stale exit on a stale home note is never conflated with an error-2? [Clarity, Contract §"Scope", Spec §FR-019]
- [ ] CHK042 - Is the requirement that the home-note's emitted INDEX sentinel bytes MUST byte-match the generator's `INDEX_START`/`INDEX_END` constants stated, along with the failure mode if they drift (silent never-fills, not a crash)? [Edge Case, Spec §FR-002, Contract §"Zone framing & assembly", Research §"Decision 3"]

## Home-Note Discovery — Missing INDEX Pair (the all-three-absent contradiction)

- [x] CHK043 - Does the spec define the behavior when a DISCOVERED, GATED home note is MISSING its INDEX sentinel pair entirely (all three GENERATED pairs absent), given that the inject-if-missing path fires on all-three-absent and would inject ALL THREE zones — directly contradicting FR-002's "home note carries ONLY the INDEX pair"? [Resolved, Conflict, Spec §FR-002, §FR-017a] — was a GAP (the inject-if-missing default at generate-spec-index.sh:347 fires on all-three-absent and would inject all three zones for a home-note target). New **FR-017a** states the precondition (a template-emitted home note always carries exactly the INDEX pair) and overrides the inherited inject default: a discovered, gated home note lacking its INDEX pair is a malformed home note → fail-safe exit 2, no write. Contract "Empty / absent behavior" table gains the matching row.
- [ ] CHK044 - Is the assumption that a discovered home note ALWAYS carries the INDEX-present + PRS-absent + BACKLINKS-absent state (the `_rewrite_present_zones` whole-zone path) stated as a precondition, with the consequence specified if that precondition is violated? [Assumption, Spec §FR-002, Research §"Decision 3"]
- [x] CHK045 - If a home note reaches the all-three-absent inject path, is it specified what `spec_dir`-relative artifacts (PRS / BACKLINKS) would be rendered against the home note's non-spec directory (`docs/ai/specs/`), and whether that is an error, a skip, or prevented upstream? [Resolved, Exception Flow, Spec §FR-002, §FR-017a] — was a GAP (same root cause as CHK043). FR-017a now *prevents* the all-three-absent inject path for a home-note target entirely (fail-safe exit 2 before any inject), so the incoherent "render PRS/BACKLINKS against `docs/ai/specs/`" outcome can never occur; the spec names that outcome as the reason for the guard. Resolved by the same FR-017a + contract-row edit as CHK043.

## SPEC-MOC Frontmatter Degradation — `status` AND `spec_id`

- [ ] CHK046 - Is the empty/missing-`status` row form specified to EXACT bytes (separator present, status blank, row not dropped) so the degraded row is unambiguous and idempotent? [Edge Case, Spec §FR-015, Contract §"Empty / missing status"]
- [x] CHK047 - Does the spec define the behavior when a gated SPEC-MOC has an empty or MISSING `spec_id` — skip the row, fail safe, or emit a row with blank/placeholder link-text — given that `moc_is_gated` keys on `structureVersion` (NOT `spec_id`), so a gated MOC can legitimately reach the scan with no `spec_id`? [Resolved, Spec §FR-015a, Contract §"INDEX zone source rows", §"Empty / absent behavior"] — was a GAP (FR-015 defined only `status`; the row form was undefined for an empty/missing `spec_id`, and the originally-considered "blank link-text" form renders an unreachable zero-width `[]()` anchor). Discriminator checked: PRSG-002 `id-normalization-grammar.md` makes an absent/empty `spec_id` in a gated marker a LINT VIOLATION (no join key), and `spec_id` is BOTH link text and sort key. New **FR-015a** defines the row as **SKIPPED** (not fabricated), explicitly NOT symmetric with empty `status`, with the PRSG-002 lint left as the authority — no new validation added. Contract table gains the matching `spec_id` row. **Flagged for consensus**: skip-vs-exit-2 is a genuine fork — the closest precedent (`render_prs` fails the WHOLE render with exit 2 on a single malformed *record*, never skip-and-continue) cuts against "skip" and toward "fail the INDEX"; the applied fix is the lower-coupling recommendation, not high-confidence.
- [ ] CHK048 - Is the degraded-`spec_id` decision made CONSISTENT with the established total/safe precedent (`moc_frontmatter_field` returns absent vs present-empty; absent manifest = empty-but-valid, malformed record = fail-safe exit 2), rather than introducing a fourth, ad-hoc behavior? [Consistency, Spec §FR-012, §FR-015, Research §"Decision 4"]
- [ ] CHK049 - Is the reachability success criterion (SC-006, every in-scope spec reachable from the home note) reconciled with whatever the degraded-`spec_id` behavior is — i.e. if a no-`spec_id` row is dropped, is the SC-006 tension acknowledged? [Conflict, Spec §SC-006, §FR-012]
- [ ] CHK050 - Is "in-scope spec" defined precisely for the INDEX (gated-only; legacy/non-marked skipped) so the set of rows that can degrade is bounded and unambiguous? [Clarity, Spec §FR-016, Contract §"INDEX zone source rows"]

## render_index Determinism & Atomic-Write-Under-Failure

- [ ] CHK051 - Is the determinism requirement stated as a hard, measurable property (same committed inputs => byte-identical INDEX zone; second run = zero-byte diff)? [Measurability, Spec §FR-019, §SC-004, Contract §"Determinism & idempotence"]
- [ ] CHK052 - Is the atomic-write requirement for the home note specified (temp-file-then-rename, per-target atomicity) so a reader never sees a partial INDEX and a mid-batch failure on one map cannot half-write another? [Completeness, Exception Flow, Spec §FR-019, Contract §"Determinism & idempotence"]
- [ ] CHK053 - Is the "no partial INDEX on failure" guarantee tied explicitly to the batch-abort ordering (all validation/regeneration in PASS 1 BEFORE any PASS-2 write) so a poison input aborts with zero writes? [Exception Flow, Spec §FR-019, Contract §"Determinism & idempotence"]
- [ ] CHK054 - Are the determinism TRAPS for the home-note row enumerated and pinned (trailing-whitespace on the empty-status row; U+00B7 vs ASCII separator bytes; `LC_ALL=C` directory-scan ordering) so the byte-identical guarantee cannot silently regress? [Edge Case, Spec §SC-004, Contract §"Empty / missing status", §"Ordering"]
- [ ] CHK055 - Is the determinism guarantee scoped to committed-files-only inputs (no network, no `gh`, no nondeterministic enumeration) so "deterministic" is objectively bounded? [Measurability, Spec §FR-011, Contract header, Plan §"Constraints"]

## Spec-MOC Path Byte-Identical (PRSG-003 regression guard)

- [ ] CHK056 - Is the requirement that the per-spec-MOC code path stays byte-identical to pre-activation stated as a HARD requirement and tied to a success criterion (so the activation cannot change any existing spec-MOC error path)? [Consistency, Spec §FR-018, §SC-005]
- [ ] CHK057 - Does the spec state that `render_index` is ALREADY invoked against every spec-MOC's present INDEX zone today (so a naive activation would emit rows into every spec-MOC and break the pinned PRSG-003 byte contracts), making the context-scoping branch mandatory rather than optional? [Completeness, Spec §FR-018, Research §"Decision 1"]
- [ ] CHK058 - Is the context-scoping mechanism specified such that the spec-MOC INDEX continues to render EMPTY (the `is_home` default reproduces today's empty output) — i.e. the error/no-row behavior on the spec-MOC side is unchanged by the activation? [Clarity, Spec §FR-018, Contract §"Scope", Research §"Decision 1"]
- [ ] CHK059 - Is the pinned PRSG-003 byte-for-byte contract named as the regression guard (the spec-MOC sentinel-grammar / PRS-manifest contracts re-run unchanged) so "no regression" is auditable against a concrete artifact? [Traceability, Spec §FR-018, §SC-005, Plan §"Constraints"]
- [ ] CHK060 - Are the spec-MOC malformed-input error paths (unbalanced spec-MOC sentinels, malformed `prs.json`) confirmed to be untouched by this spec (no PRSG-004 requirement alters them), so the existing exit-2 fail-safe semantics are preserved? [Consistency, Spec §FR-018, §SC-005]

## Cross-Cutting Error-Requirement Quality

- [ ] CHK061 - Is each error/failure-mode behavior in this domain traceable to a numbered requirement, success criterion, or contract section (so no error path is specified only in prose narrative)? [Traceability, Spec §"Functional Requirements", Contract §"Empty / absent behavior"]
- [ ] CHK062 - Are the home-note error conditions enumerated as an exhaustive table (present+gated+specs, present+empty-specs, absent, non-gated, status-empty, spec-MOC-target) so a reviewer can confirm no failure mode is unaddressed? [Completeness, Contract §"Empty / absent behavior"]
- [ ] CHK063 - Is the rollback/recovery posture for a failed generator run documented (revert is file-local; no partial state persists; re-run is idempotent), so the failure-recovery story is complete? [Coverage, Recovery, Spec §FR-019, Plan §"Constitution Check" rollback notes]

## Notes

- This checklist tests the QUALITY of PRSG-004's error-handling requirements (completeness, clarity, consistency, measurability of the failure-mode specifications), not the behavior of the generator or skills.
- Many home-note error paths are covered-by-construction because the home note folds into the SAME PASS-1/PASS-2 + `rebuild_map` + `_atomic_write` machinery as the spec-MOCs (the existing exit-2 fail-safe, batch abort, and atomic write apply). Those items are expected to PASS by reference to that reused machinery; the discipline (per the existing `documentation-quality.md` CHK009 precedent) is to mark PASS-with-rationale rather than force a redundant edit.
- Three items were authored as gap candidates; all three are now remediated (re-marked Resolved) and no open gap markers remain:
  - **CHK047** (empty/missing `spec_id`): GENUINE gap. Resolved by new **FR-015a** (skip the row — `spec_id` is link text + sort key, and an absent/empty `spec_id` in a gated marker is a PRSG-002 lint violation) + a contract behavior-table row. Discriminator that decided skip-vs-fail-safe-vs-blank: PRSG-002 `id-normalization-grammar.md` already classifies the absent/empty join key as a violation.
  - **CHK043 / CHK045** (discovered, gated home note MISSING its INDEX pair → the all-three-absent inject path injects all three zones, contradicting FR-002): GENUINE gap (one root cause, two facets). Resolved by new **FR-017a** (override the inherited inject-if-missing default for a home-note target: fail-safe exit 2, no write) + a contract behavior-table row.
- Files edited for remediation: `spec.md` (FR-015a, FR-017a), `contracts/roadmap-moc-index.md` (two new "Empty / absent behavior" rows). FR-020/SC-008 (Domain-1's Codex-parity tightening) were left untouched.
- Flagged for consensus (two structurally identical skip-vs-exit-2 forks; the applied fixes are the recommended defaults, not high-confidence):
  - **CHK043 / CHK045** chose **fail-safe exit 2** for a gated home note missing its INDEX pair over the equally-valid **skip the malformed home note** reading.
  - **CHK047** chose **skip the row** for an empty/missing `spec_id` over **fail the whole INDEX (exit 2)**; the closest precedent (`render_prs` aborts the entire render on one malformed *record*) cuts toward exit-2, so this needs an analyst tie-break.
  - See the run summary's "Unresolved for consensus" for the routing tags.
