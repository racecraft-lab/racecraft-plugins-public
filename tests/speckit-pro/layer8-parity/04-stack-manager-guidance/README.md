# 04 - Stack Manager Guidance

Checks that Agent Teams and fallback paths describe the same supported,
fallback, blocked, recovery, registered runner-helper, and shared-contract
guidance. Dry-run validates the versioned contracts and the explicit
`## Required Invariants` evidence table. Live mode evaluates that table in
both captured outputs before comparing them.
