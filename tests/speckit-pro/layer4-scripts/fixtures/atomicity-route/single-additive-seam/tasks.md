# Tasks: Add an export button

One indivisible additive capability on a single production surface. Additive
(a new button + its new handler), but there is only one seam — nothing to split.

- [ ] T001 Add a new "Export CSV" button to the existing report toolbar in
  `src/components/ReportToolbar.tsx` and wire its new click handler.
