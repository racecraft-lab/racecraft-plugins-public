interface:
  display_name: "SpecKit Autopilot"
  short_description: "Archive-aware autonomous SDD workflow executor"
  default_prompt: "Run a SpecKit autopilot workflow, starting with Archive Sweep when available"

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: mcp
      value: tavily
      description: "Web search for consensus research"
    - type: mcp
      value: context7
      description: "Library documentation lookup"
