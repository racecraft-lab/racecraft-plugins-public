# Tasks

- [ ] T001 Execute the release-held billing cutover during the weekly release train in `src/app/api/billing/route.ts`.
- [ ] T002 Hold the release window until operations confirms the cutover checklist in `docs/runbooks/billing-release.md`.
- [ ] T003 Add release-window verification for the cutover in `tests/billing/release-held-cutover.test.ts`.

```text
Historical notes mention feature flags, but this code-fence-only text is not
repo-local flag-system evidence and must not suppress the release-held route.
```
