# PRSG-918 spec (fixture stub)

A stub artifact so the symlinked-MOC spec dir has a real sibling file. Irrelevant to
the symlink rejection itself; present only to make the fixture a realistic spec tree.
