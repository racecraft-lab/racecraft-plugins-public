---
up: "[parent](../../docs/ai/specs/myproject-roadmap-MOC.md)"
related: []
status: "complete"
rank:
spec_id: "LEGACY-THING"
---

# legacy-thing — Map of Content (legacy, NOT version-marked)

This note carries NO `structureVersion`, so `moc_is_gated` returns false and the
home-note INDEX skips it (FR-016). It is left byte-for-byte unmodified by any run.
