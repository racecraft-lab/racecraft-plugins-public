# gate-no-marker — SKIP

This spec directory deliberately contains NO `SPEC-MOC.md`. A directory with no
marker is exempt and silently skipped (the scan globs `SPEC-MOC.md` only). This
placeholder README keeps the empty fixture directory tracked in git.
