# Unit native migration audit

## Scaffold diagnostic correction

Five current methods and their exact body hashes are recorded in
`scaffold_diagnostic_refresh`, replacing the earlier four-method nested-workflow
draft. Original Codex scaffold eval 7 asks why an existing non-interactive setup
failed, what state remains, and how to retry; it does not request a new scaffold
execution. The ordinary diagnostic stages a real registered child worktree and
requires exact state and retry reporting, retained inspection evidence, no
fabricated artifacts at either root, and parent/child preservation.

Root independently reviewed the original four expectations, prompt, checks and
negative controls. Tests pass 17/17; fabricated root or child artifacts and
wrong report fields fail on each client even with positive mock semantic
verdicts. The mock does not qualify semantic reliability. Fixture exclusions
name only the actual child, not the entire worktree directory. Real provider-free
preparation succeeded on both hosts at
`.native-eval-output/scaffold-diagnostic-preflight-6y1dpg4h`; this is not live
model qualification or evidence of successful interactive setup.

## Worktree adoption and redundant parity counts

Autopilot case 107 now has an exact catalog/factory and original-expectation
mapping regression. Root verified the five retained legacy expectations and
the explicitly added Claude arm. Functional controls pass 36/36.

Removed two unrelated assertions from the parity-owner test: a hard-coded total
catalog size and hard-coded counts for every other layer. Adding a legitimate
functional case made those assertions fail without changing parity behavior.
The same test still proves the exact two parity IDs, uniqueness in the active
catalog, both native arm mappings, artifact contracts, and pair compilation.
Functional coverage totals and legacy membership stay in their owner tests.
No behavioral case or test method was removed; parity controls pass 29/29.

## Git and request integrity refresh

Eight reviewed methods are recorded with exact body hashes in
`git_and_request_integrity_refresh`. The runner-result correction retains exact
raw request bytes and independently verifies raw and canonical identities;
formatting differences remain legitimate, while altered hashes, byte counts,
or request text are invalid. Root independently reran 24/24 runner controls.

Git observation now rejects indexed gitlinks before status can follow external
metadata. A real external fsmonitor marker remains absent. Topology baselines
must agree with their root and attached branch heads, without rejecting valid
changes to the current root. Root reviewed the repairs and independently reran
52/52 observer controls. Dynamic topology still needs controller-retained
baseline and final-report integration before scaffold-case adoption.

Declared registered children now travel in the same canonical, write-once Git
record as the parent. Fresh capture and replay bind their initial rows to the
prepared fixture. Both clients fail dirty or changed children; missing,
duplicated, empty, or substituted evidence is invalid. Independent review
accepted this integration: grading 36/36, execution 120/120, and the real
worktree fixture 3/3. Regrading preserves valid failures without subject
relaunches. These are local adapter/parser/grader controls, not live passes.

## Atomicity routing refresh

Ten reviewed methods cover the real additive split, single/dependent/shared
seams, static cross-seam imports, unsupported dynamic imports, cutover priority,
modified and destructive changes, malformed topology, and the adopted Post
fixture. The JSON ledger records their exact bodies and distinct boundaries.
Root reproduced the previously missed aliased `builtins.__import__` form as a
false split, then verified the one-line guard correction. Routing checks pass
16/16 and parity checks pass 29/29.

The tiny fixture-source assertions supply topology; they are not presented as
meaningful application tests or live behavioral evidence. The route's evidence
is bounded static Python import analysis, not general semantic independence.

## Cross-layer reuse and scaffold refusal refresh

The four scaffold-method hashes below describe the earlier nested-workflow
candidate, now superseded pending diagnostic-scope replacement and review.
They are historical audit evidence, not current acceptance of the revised case.

Five reviewed methods are recorded in `cross_layer_and_scaffold_refresh`:
one exact legacy-to-parity coverage mapping and four scaffold candidate controls.
The mapping covers both legacy clients without adding a duplicate launch pair.
The candidate controls cover schema validity, real Git fixture materialization,
independent fabricated design/workflow negatives, and a limited fixture-leakage
smoke check. Root removed a redundant exact prompt-literal assertion; the schema
test still exercises the real validator.

Functional controls pass 35/35, parity 29/29, and scaffold candidate controls
5/5 (four methods with parameterized artifact negatives). The scaffold candidate
is held pending controller-observed dynamic topology and final-report binding.
These controls neither prove semantic-judge reliability nor live qualification.
Scoped quality findings still include pre-existing Git-helper clone groups and
new fixture/test shapes; dynamic unittest discovery explains indexed
`dead-code` labels. No findings were blanket-acknowledged.

## Verification binding refresh

The JSON ledger's `verification_binding_refresh` records 13 individually
reviewed methods: twelve verification adapter/grader tests and one both-host
persistence race regression. All are retained for distinct declared-artifact,
authority, identity, strict JSON/type, workflow, reuse, replay, or post-binding
mutation boundaries. Body hashes identify the reviewed methods; parameterized
checks are not counted as additional independent methods.

Root reruns passed verification 27/27, execution 107/107, and adapters 94/94.
These are deterministic harness checks, not live subject qualification. The
race control proves both mutation and deletion after authentication preserve
the original saved bytes and allow regrading without a subject launch.

The scoped quality scan still reports three duplication gates involving
strict-JSON callbacks (including cross-file clone groups) and 79 new-symbol
findings across the large execution/verification slice. No blanket quality
acknowledgment was applied and no repository-wide clean result is claimed.
The test map found no untested indexed impact for the verification module;
its named verification and adapter suites passed, along with execution tests.

## Scope and accounting

- Frozen Layer 4 families remain **1,615** and are not modified by this audit.
- The bounded frozen-remainder lane contains **37** methods: **36 keep, 1 replace**.
- The accepted-surface checkpoint now contains **199** post-freeze native-eval methods: the prior **181** plus **18** reviewed additions, with **0 unreviewed** in this bounded slice. The additions are **16 exact runtime methods**, **1 trigger method**, and **1 execution method**. Runtime 19/19 and trigger 31/31 are counted checks, not exact method totals.
- Adapter source and tests were actively changing during this refresh. Their prior reviewed checkpoint is preserved, but this report does not claim their current bytes or methods were resnapshotted.
- Three adjacent functional-grounding methods added by their owner were also reviewed and hash-bound separately; they are not folded into the native-eval exact-name count.
- The historical 37-method remainder, original skill-injection cohort, and exact current-name diff remain separate accounting units. Exact symbols, lines, body hashes, claims, dispositions, and sensitivity evidence are in the JSON companion.

## Exhaustive contract review

- `adapter_fixture_runtime`: 6 methods, all keep
- `adapter_isolation_identity`: 4 methods, all keep
- `capture_claude_causality`: 1 methods, all keep
- `catalog_contract`: 1 method, all keep
- `codex_parent_delivery`: 4 methods, all keep
- `codex_rollout_collection`: 4 methods, all keep
- `codex_rollout_parsing`: 16 methods, all keep
- `execution_causal_return`: 5 methods, all keep
- `execution_nested_merge`: 5 methods, all keep
- `execution_pair_orchestration`: 4 methods, all keep
- `execution_publication`: 4 methods, all keep
- `execution_resume`: 8 methods, all keep
- `execution_semantic_grading`: 5 methods, all keep
- `execution_trigger_evidence`: 5 methods, all keep
- `execution_trigger_grader_identity`: 1 method, keep
- `fixture_materialization`: 12 methods, all keep
- `functional_grounding`: 3 methods, all keep
- `judge_projection`: 6 methods, all keep
- `pairing_deterministic`: 10 methods, all keep
- `pairing_fingerprints`: 1 method, all keep
- `pairing_semantic`: 4 methods, all keep
- `skill_read_qualification`: 12 methods, all keep
- `runtime_import_isolation`: 2 methods, all keep
- `runtime_payload_and_identity`: 5 methods, all keep
- `runtime_preflight_and_source_confinement`: 6 methods, all keep
- `runtime_roster_validation`: 1 method, keep
- `trigger_codex_exact_command_grammar`: 1 method, keep
- `trigger_qualification`: 11 methods, all keep
- `trigger_staging`: 4 methods, all keep

Every accepted method was inspected through its body, actual callee boundary, and assertions. Fake provider callbacks are legitimate transport adapters in execution tests: those methods still enter the real orchestration, capture, store, grading, pairing, and resume paths. No empty parameter sets or assertion-only tautologies were found. The parent-action-before-delivery check is narrowly classified as event-order preservation, not proof that a downstream actor consumed the delivery.

## Accepted post-checkpoint refresh

- Runtime source/test: `cd2677562c930150b4ff6dcfff8607697029133d01e3a90de31fbec71ae8f79e` / `ac241929938b823c013ea30cc31d79df05cb57b413466ec867f34a7daa2de743`
- Trigger source/test: `0bdbc77d5ee1900847f005a97593b88f51a82ac89e1c71cd563e6d58c28208bf` / `e9690e5a3118ae18ab9a41c8bf5b98247ed12ffd3c7c6fb0cb4ff5c2c5c79e0e`
- Execution source/test: `aa1c117f2c743b330424dc47bf74266b4e2c93ecf1809c7138304a4da7fe6ebf` / `0d3796d15db5a9018f2b94ce8615335cf7a1fc48361ffb5952a733cb18e2e41a`
- All 16 exact additions remain `keep`. The JSON companion binds each test body to its exact source symbol, line range, body hash, asserted boundary, failure class, and negative control.

Four properties remain narrower than their test names or surrounding prose: the nonisolated-root test does not spy on the product builder; the valid sibling-output test does not inspect Git ignore state; the source-safety test does not exercise the dedicated `LICENSE` branch; and the dist snapshot omits empty directories and metadata. Those properties are not counted as proven.

## Corrected findings

1. The old failed-command check used unsupported compound shell grammar. Flipping only its status left read evidence empty in both cases, so it was vacuous for success gating. The replacement uses the same supported `cat` operation: failure yields no read evidence and success yields one exact `read_file` observation. The compound fixture remains a provenance-only check.
2. Root `session_id` and `thread_source` have direct single-field rejection vectors. Both mutations fail closed while the unchanged frozen rollout qualifies.
3. The skill-injection collector directly rejects duplicate/symlink exact roots and existing/symlink destinations. All four faults fail closed; its adjacent happy path proves exact copied bytes and hash references.

## Prior adapter checkpoint

The earlier adapter audit remains accepted historical evidence. Active adapter integration was deliberately not inspected, rerun, or rehashed in this bounded refresh.

## Verification

- Native adapters: 55/55 passed
- Native capture: 82/82 passed
- Native contracts: 43/43 passed
- Native entrypoint: 8/8 passed
- Native execution: 60/60 passed
- Native judge: 13/13 passed
- Native pairing: 24/24 passed
- Native skill reads: 21/21 passed
- Native store: 30/30 passed
- Native runtime: 19/19 passed across 14 exact methods
- Native trigger: 31/31 passed
- Native functional catalog: 15/15 passed
- Codex native rollouts: 92/92 passed
- Affected trigger comparison: 35/35 passed
- Affected trigger eval runners: 231/231 passed
- Affected trigger campaign execution: 76/76 passed
- No provider, full-campaign, full-suite, corpus-generation, or generated-artifact action was performed.

## Limitations

The installed Claude replay method is supplemental because it can skip when its external replay fixture is unavailable; mandatory parser claims rely on frozen fixture tests. This is a dirty shared-worktree accepted-surface checkpoint; adapter integration remains pending rather than silently accepted. Ripwire reported 25 gating major findings on preexisting symbols worsened relative to git HEAD (79 `preexisting-worse` findings total, including 54 minor); this lane did not adjudicate them because root separately completed clone review. The shared-worktree test gate listed 145 test obligations and 85 untested impacted symbols; this bounded lane ran only the three refreshed suites above and preserves prior focused results as historical checkpoints.

## Search and final Git-state refresh

The JSON companion's `search_git_final_state_refresh` reviews 20 current test
methods with exact body hashes, failure boundaries, and keep dispositions:
11 search methods, six Git-grading methods, and three execution/resume methods.
They test parser, grader, and controller-binding behavior—not live skill success.
Search controls pass 44/44, Git grading 20/20, and execution 102/102; the execution
count includes existing methods and must not be added to unique behavioral cases.

Targeted negative controls reproduced child searches falsely establishing parent
absence, malformed root/NUL paths being treated as behavioral failures, and
unchanged grading identities after dependency changes. Each was corrected and
retested. A real recursive filesystem search exercises hidden files; execution
tests retain and replay controller-bound Git observations on both host adapters
without replacement subject launches. No paid provider calls were made.

Native search scope remains unqualified for the held absence cases. Git final
state is not a proof about transient actions. Runtime-exclusion corrections have
now passed independent review. This refresh does not clear the
whole-tree quality findings, remaining audit work, or native release gates.

## Registered-worktree fixture refresh

The JSON companion records three reviewed test methods with body hashes and
keep dispositions. They cover schema acceptance, real public-runner ambiguity
with negative controls, and rejection of unsupported external/temporal fixture
shapes. The public-runner control initially failed because the fixture lacked a
project marker; direct helper calls had bypassed that prerequisite. The fixture
now includes the neutral project marker and a real input request, never a
prewritten helper answer. Removing either workflow changes the actual result
from ambiguous to same or descendant.

Focused migration controls pass 3/3; the existing workflow-binding suite passes
4/4. Both native clients prepare the candidate, and Codex's real nine-probe
isolation preflight passes. No provider was launched. The candidate remains
held pending deterministic native-response binding, and the other external-root
and temporal-revalidation requirements remain incomplete. These test methods,
scenarios, and preparation results are not new live behavioral passes.
