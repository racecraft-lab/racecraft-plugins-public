# PARITY-01 Plan

1. Preserve the two independent additive slices: Alpha owns
   `src/capability_alpha.py` and `tests/test_capability_alpha.py`; Beta owns
   `src/capability_beta.py` and `tests/test_capability_beta.py`. Neither slice
   modifies the other's paths or a shared runtime contract.
2. Materialize the complete host-native Post list in `workflow.md` and
   `autopilot-state.json`.
3. Dispatch the three canonical parallel tracks with exclusive ownership.
4. Consume and attribute every result before the parent serial tail.
5. Use real current reviewability evidence, create the required local
   clean-worktree checkpoints, generate the missing packet/body, and run current
   packet and workflow validation in canonical serial order.
6. Write and locally checkpoint the bounded report, retain PR Creation and later
   canonical items as incomplete, and stop before the external PR command.
