# Troubleshooting/security workload kickoff

Apply the identical `../COMMON-TASKS-INPUT.md` supplement to the actual Tasks
production prompt in both arms; preserve the original workflow bytes separately.

Run the full prepared DOC-008 workflow through the installed native autopilot,
under the separately approved performance campaign. Use the scaffold workload
commit, whose troubleshooting/security pages are still route shells and whose
update-and-rollback page does not yet exist. The final frozen approved
specification is the scope reference. Generate new planning and run the actual
Tasks phase; final historical plan/tasks are evaluator evidence, not generated
artifacts to resume.

Preserve all **40** reference obligations: shared docs foundations; independent
troubleshooting, security/trust, and update/rollback story work after foundation;
then integration, links, accessibility, citation review, docs validation, scope
review, and PR-packet preparation. Retain read-only diagnostic boundaries,
manual mutation labels, last-resort cache handling, and official-source versus
repository-fact attribution. DOC-009 contributor workflow and DOC-010 CI
hardening remain out of scope.

Historical vendor/library references and commands must be prerequisite-qualified
identically across arms; do not claim their current validity from a frozen
document. The complete installed-arm automated Post checks still apply even
though this historical workflow predates the current Post table. Human UAT is
not established by automated page or link checks.

Follow `../PROTOCOL.md`. This kickoff is scope input, not launch authorization.
