# Implementation-notes workload kickoff

Apply the identical `../COMMON-TASKS-INPUT.md` supplement to the actual Tasks
production prompt in both arms; preserve the original workflow bytes separately.

Run the full prepared ART-012 workflow through the installed native autopilot,
under the separately approved performance campaign. Workload source is the
scaffold commit in the manifest, not the installed harness and not current
already-implemented source. Read the original workflow and design concept plus
the frozen approved specification as requirements. Generate fresh planning,
including the actual Tasks phase; the final reference plan and task list are
coverage evidence, not pre-generated work to resume.

## Approved scope amendment, effective before kickoff

The historical initial 13-task scope is superseded. Preserve the final **14**
reference obligations and all six functional requirements. In particular,
**FR-006** requires each dispatched attempt's complete Task Result summary to
reach the orchestrator; independent teammate sessions must be instructed to
send it upon completion. Append on per-attempt summary arrival, not on a bare
idle signal and not after all workers finish. **T014** corrects stale batched
delivery claims in the agent-teams integration reference.

This operator-approved amendment increased the projection to **190**
modify-weighted reviewable LOC, **six** production files, and nine total files;
the settled split decision remains one slice. The frozen final specification
contains the amendment history and exact acceptance requirements. Its scope
takes precedence over the original workflow's superseded 115-LOC/three-file
estimate. Keep all reporting-field touchpoints, Claude/Codex record parity,
append-only behavior, interruption durability, fail-open append errors,
regeneration, final verification, and PR-packet obligations.

Human UAT remains distinct from automated coverage. Follow `../PROTOCOL.md`;
this kickoff does not authorize provider launches or external writes.
