# Draft-PR workload kickoff

Run the full prepared ART-007 workflow through the installed native autopilot,
under the separately approved performance campaign. Use the manifest's scaffold
workload source. The final frozen approved specification governs scope,
including its draft-packet contract amendments. Run fresh Specify through
Post, including actual Tasks generation; never seed the final reference plan
or task list into the active feature directory to bypass generation.

Preserve all **54** reference obligations. The source task list records the
historical preflight T001–T005 as complete, but this campaign must perform those
obligations again; only the separate prepared acceptance reference resets their
checkboxes. Preserve the dependent story order and shared-file ownership.
The exact draft-packet mode contract, paired fixture validation, unchanged
non-draft behavior, artifact-agent fail-open behavior, stage corroboration,
resume/stop outcomes, final checks, and PR review-packet obligations remain
required. Historical planning contradictions are resolved against the frozen
authoritative contract amendment, not by weakening coverage.

**T052 remains operator-gated and not run:** quickstart scenarios 5–7 require
installed/authenticated GitHub tooling and an approved disposable fork with
permission to open/close draft PRs. Quickstart scenarios 1–4 and the other
automated obligations remain required. Do not mark T052 complete because an
automated fixture passes. Automated PR behavior elsewhere also requires the
campaign's explicit external-write scope; missing authority is a blocker, not
a skipped success. Human UAT remains separately pending.

Follow `../PROTOCOL.md`. This kickoff is scope input, not launch authorization.
