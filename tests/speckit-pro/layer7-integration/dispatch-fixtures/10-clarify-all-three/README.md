# Fixture 10 — Clarify, all-three explicit fan-out

Verifies that an explicit `[codebase, domain, spec]` tag dispatches
all three analysts. This is the "naturally cross-cutting" case where
clarify-executor judges the question genuinely needs all three
perspectives without invoking the security or ambiguous safety nets.
