# Fixture 08 — Clarify, codebase + spec multi-category

Verifies multi-category routing for the [codebase, spec] combination.
Mirrors fixture 02 ([codebase, domain]) but rotates which analyst
pair is exercised. domain-researcher must NOT be dispatched.
