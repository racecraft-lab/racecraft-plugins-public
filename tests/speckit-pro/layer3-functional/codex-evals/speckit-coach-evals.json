{
  "skill_name": "speckit-coach",
  "evals": [
    {
      "id": 1,
      "prompt": "I'm starting a brand new TypeScript project and want to use Spec-Driven Development from the beginning. I have nothing set up yet — no speckit, no specs directory, nothing. Walk me through getting started from zero.",
      "expected_output": "Routes first-time setup to `$speckit-install`, then explains constitution creation and the post-install SDD flow.",
      "expectations": [
        "Routes installation to `$speckit-install`",
        "Does not prescribe a Git, PyPI, or package-source installation command",
        "Explains constitution creation and its purpose",
        "Lists or references the 7 SDD phases in order: specify, clarify, plan, checklist, tasks, analyze, implement"
      ]
    },
    {
      "id": 2,
      "prompt": "I'm writing a spec for a REST API that handles webhook delivery with retry logic and payload signature verification. Which checklist domains should I select when I run specify checklist? I want to make sure I don't miss anything important.",
      "expected_output": "Recommends 2-4 relevant checklist domains from the available set, with justification for each based on the webhook delivery use case.",
      "expectations": [
        "Recommends at least 2 and no more than 4 checklist domains",
        "Includes 'api' or 'http' related domain for the REST API surface",
        "Includes 'resilience', 'reliability', or 'error-handling' domain for retry logic",
        "Includes 'security' domain for signature verification",
        "Explains why each recommended domain is relevant to the webhook delivery feature"
      ]
    },
    {
      "id": 3,
      "prompt": "I keep hearing about 'gates' G1 through G7 in the SpecKit autopilot. What does each gate specifically validate? I want to understand what artifacts need to exist and what markers need to be absent before each phase transition succeeds.",
      "expected_output": "Explains that Coach reads the live autopilot skill as a reference and describes the current gate contract without executing or invoking autopilot.",
      "expectations": [
        "Uses the live autopilot skill or its authoritative references instead of a copied gate catalog",
        "Does not claim `[TBD]` is the universal G1 marker contract",
        "Does not execute or invoke autopilot while answering",
        "States that the current phase artifacts and validation evidence determine whether a gate advances"
      ]
    },
    {
      "id": 4,
      "prompt": "How does the consensus protocol work in the autopilot workflow? I heard there are always 3 agents that vote, but I want the current contract.",
      "expected_output": "Explains the current category-routed protocol from the live autopilot references: unresolved consensus items use one to three relevant analysts, and the Round 2 majority rule applies only when its current escalation conditions are met.",
      "expectations": [
        "Uses the current live autopilot skill or its consensus-protocol reference as the authority",
        "States that routing selects one to three analysts by unresolved-item category rather than always dispatching three",
        "Explains that Round 2 and a 2-of-3 majority are conditional escalation behavior, not the default",
        "Explains that unresolved Round 2 disagreement is surfaced for human review and stops advancement"
      ]
    },
    {
      "id": 5,
      "prompt": "I'm building a large e-commerce platform feature that covers product catalog, inventory management, order processing, and payment integration. This is way too big for a single spec. Help me decompose it into a technical roadmap with proper dependency ordering.",
      "expected_output": "Breaks the feature into independently valuable vertical slices with observable outcomes, real dependencies, and an implementation order; it does not reserve a catch-all integration-only final spec.",
      "expectations": [
        "Decomposes into at least 3 independently valuable vertical slices with observable outcomes",
        "Identifies dependency relationships between specs (e.g., catalog before inventory, inventory before orders)",
        "Suggests a sequential or partially-parallel implementation order based on real dependencies",
        "Does not propose a catch-all integration-only final spec",
        "Recommends using the technical roadmap template to track cross-spec progress"
      ]
    },
    {
      "id": 6,
      "prompt": "What MCP servers does speckit-pro need? I'm setting up a new machine and want to know what's required vs optional for the full autopilot experience.",
      "expected_output": "Answers vendor-neutrally. ABSENCE arm: the answer does NOT prescribe a specific preferred MCP set as required (no Tavily/Context7/RepoPrompt ranking, no claim that any named server is mandatory). AFFIRMATIVE arm: explains that NO MCP server is required — speckit-pro works with the platform's built-in capabilities — and frames optional servers by the CAPABILITY CATEGORY they can serve (codebase context, library documentation, web/domain research, source extraction), with the coach recommending the best installed capability for a given need rather than a fixed named set. When no installed capability covers a need, the work continues with local files, native platform context, or repo-local helpers at reduced confidence.",
      "expectations": [
        "ABSENCE: does NOT present a specific named MCP set as required or as the single recommended default (no mandatory Tavily/Context7/RepoPrompt list)",
        "AFFIRMATIVE: states clearly that NONE are required — speckit-pro works with the platform's built-in capabilities",
        "AFFIRMATIVE: frames optional servers by the capability category they serve (codebase context, library documentation, web/domain research, source extraction) rather than a fixed named set",
        "AFFIRMATIVE: explains that the best INSTALLED capability is chosen for a given need, and local files / native context / repo-local helpers are the fallback when none is installed"
      ]
    },
    {
      "id": 7,
      "prompt": "Can you just run the full autopilot for me right now on SPEC-025? I have the workflow file ready at docs/ai/specs/.process/SPEC-025-workflow.md.",
      "expected_output": "Redirects to $speckit-autopilot as the correct Codex entrypoint. The coach does not execute the autopilot itself.",
      "expectations": [
        "Does NOT attempt to execute the autopilot workflow itself",
        "Redirects the user to $speckit-autopilot as the correct entrypoint",
        "Mentions $speckit-scaffold-spec OR correctly assumes the workflow file exists since the user stated they have it",
        "Provides brief guidance on what to expect when running the autopilot"
      ]
    },
    {
      "id": 8,
      "prompt": "What are SpecKit extensions? I see references to 'verify', 'review', 'cleanup', and 'retrospective' extensions in the autopilot workflow. How do I install them and what do they do?",
      "expected_output": "Explains extensions as optional workflow additions, then inspects the active project, host, installed CLI help, manifests, and configuration before describing which user-named examples are actually available or configured.",
      "expectations": [
        "Explains that extensions are optional workflow additions rather than mandatory built-in phases",
        "Inspects the active project, host, installed CLI help, manifests, and configuration before claiming availability or behavior",
        "Treats verify, review, cleanup, and retrospective as user-named investigation targets; reports each as observed, unavailable, or unverified without inventing behavior, and states that the list is non-exhaustive",
        "Names .specify/extensions.yml or another configuration path only when the active installed evidence confirms that path"
      ]
    },
    {
      "id": 9,
      "prompt": "I want to understand the workflow file that $speckit-scaffold-spec creates. What sections does it have? What gets populated automatically vs what do I need to fill in?",
      "expected_output": "Describes the workflow overview rows: Specify, Clarify, Plan, Checklist, Tasks, Analyze, Confidence Gate, Implement, and Post; plus status tracking, SPEC-ID, feature branch, and configuration.",
      "expectations": [
        "Describes the workflow overview rows: Specify, Clarify, Plan, Checklist, Tasks, Analyze, Confidence Gate, Implement, and Post",
        "Mentions the SPEC-ID and feature branch fields",
        "Explains that $speckit-scaffold-spec auto-populates the file from the technical roadmap and the Grill Me design concept doc",
        "References the workflow file location pattern (docs/ai/specs/.process/SPEC-NNN-workflow.md)",
        "Mentions that $speckit-scaffold-spec also produces a Design Concept doc at docs/ai/specs/.process/SPEC-NNN-design-concept.md from a Grill Me interview"
      ]
    },
    {
      "id": 10,
      "prompt": "I want to see the current roadmap progress and which spec is unblocked next. Which SpecKit entrypoint should I use in Codex and what will it show me?",
      "expected_output": "Redirects to $speckit-status as the correct Codex skill and explains that it summarizes roadmap progress, active workflows, blockers, and the next recommendation.",
      "expectations": [
        "Redirects the user to $speckit-status",
        "Mentions roadmap progress, active workflows, or phase status",
        "Mentions blockers or dependency status",
        "Mentions that the skill recommends the next unblocked spec"
      ]
    },
    {
      "id": 11,
      "prompt": "I need to address review comments on PR #42 and resolve the open threads after the fixes are pushed. Which SpecKit entrypoint should I use in Codex?",
      "expected_output": "Redirects to $speckit-resolve-pr as the correct Codex skill and explains that it fixes code, verifies the branch, replies to review comments, and resolves threads.",
      "expectations": [
        "Redirects the user to $speckit-resolve-pr",
        "Mentions that the skill addresses review comments on a PR",
        "Mentions verification such as tests, typecheck, or build",
        "Mentions resolving review threads after the fixes"
      ]
    },
    {
      "id": 12,
      "prompt": "Where does Grill Me fit into the SpecKit workflow? I see $grill-me in the plugin and I'm not sure when to use it vs $speckit-clarify inside autopilot.",
      "expected_output": "Explains that $grill-me is a pre-workflow, human-in-the-loop scoping interview used standalone or inside $speckit-scaffold-spec before any spec or workflow file is written, while $speckit-clarify (inside $speckit-autopilot) is the autonomous, agent-driven clarification mechanism that runs during the workflow with the multi-agent consensus protocol. Notes that grill-me MUST NEVER be invoked from autopilot or any of its phase subagents.",
      "expectations": [
        "Mentions that $grill-me is human-in-the-loop and runs before any spec/workflow file is written",
        "Mentions that $speckit-scaffold-spec always runs $grill-me before generating the workflow file",
        "Mentions that $speckit-clarify (inside autopilot) is autonomous and uses the consensus protocol",
        "States that $grill-me must never be invoked from autopilot or any of its phase subagents",
        "Distinguishes pre-workflow scoping (grill-me) from in-workflow disambiguation (clarify)"
      ]
    },
    {
      "id": 13,
      "prompt": "What community extensions are available that help with code review? I want to see what the actual SpecKit community catalog has — not a stale list from documentation, the real current state. Find me 2-3 candidates and tell me which one looks most active.",
      "expected_output": "Coach uses the best observed installed catalog/search capability or a disclosed current official source, then reports evidence-bearing candidates and an explicit activity metric; if freshness cannot be established, it reports the request as blocked instead of fabricating a current result.",
      "expectations": [
        "Uses the best observed installed catalog/search capability first, or a current official source with the source and observation time disclosed; does not require a particular transport or tool",
        "When freshness is established, lists 2-3 observed candidates by bare id with their observed categories or tags; otherwise explicitly reports that current candidates are unavailable and does not fabricate them",
        "Defines 'most active' using an available observed field and its timestamp, or states that the comparison is blocked when no defensible activity metric is available",
        "Does not recite a fixed catalog count or stale snapshot as current evidence, and does not mark the request complete when freshness could not be established"
      ]
    },
    {
      "id": 14,
      "prompt": "I want to install the archive extension into this project and have it fire automatically after each implement phase so merged specs get archived without me thinking about it. Walk me through the install + the hook wiring step by step.",
      "expected_output": "Coach inspects the active project, installed CLI and integration, catalog source, and archive manifest; it then proposes the exact evidence-backed operation and targets and asks for confirmation without mutating anything on this first turn.",
      "expectations": [
        "Inspects the active project, installed CLI version/help and integration, catalog source, archive URL or manifest, and pre-state before proposing an operation",
        "Shows the exact evidence-backed operation and target paths, asks for confirmation, and performs no install, configuration, or hook mutation on this first turn",
        "Does not attribute a hook, command, or configuration filename to archive unless the observed archive manifest declares it; explicitly discloses zero hooks when that is what the manifest shows",
        "Keeps any generic after_implement explanation separate from archive claims and explains that optional: false is automatic while optional: true is prompted or skippable",
        "States reload, restart, and path behavior only when supported by the active Codex host and installed-runtime evidence"
      ]
    },
    {
      "id": 101,
      "prompt": "When SpecKit Pro agents need codebase context to answer a question — say, matching a new module to the repo's existing error-handling pattern — how do they decide which capability to use? Is there a fixed tool they always reach for first?",
      "expected_output": "Explains installed-capability discovery: the agent first identifies the needed capability category (codebase context), then selects the best INSTALLED capability for that need by task fit, source directness, and expected evidence quality — not by a fixed named-tool order or a preferred vendor set. Capability categories are a taxonomy, not an ordered fallback chain. A formerly named tool is used only when discovery selects it as the best available capability for that need.",
      "expectations": [
        "States the needed capability category is identified first (codebase context) before any tool is chosen",
        "Selects the best INSTALLED capability for that need by task fit and evidence quality",
        "ABSENCE: does not name a fixed first tool or a preferred named-vendor set; capability categories are a taxonomy, not an ordered fallback chain",
        "States a formerly named tool is used only when discovery selects it as the best available capability for the need"
      ]
    },
    {
      "id": 102,
      "prompt": "What happens when a SpecKit Pro agent needs external library documentation but no capability that could supply library docs is installed or reachable in the environment? What does it do, and what does it avoid claiming?",
      "expected_output": "Explains the vendor-neutral fallback: when no installed capability is available, reachable, or usable for the library-documentation need, the agent continues with local files, native platform context, or repo-local helpers when they can support the work. It does NOT invent or require a specific named server. It emits the fallback disclosure 'No installed <capability> was available/usable; used <local/native/repo-local fallback>; confidence is <medium|low> because <reason>.' and reports the result at medium or low confidence, never high.",
      "expectations": [
        "Continues with local files, native platform context, or repo-local helpers when no installed capability is available, reachable, or usable",
        "ABSENCE: does not require or invent a specific named server/tool to do the work",
        "Emits the fallback disclosure line naming the missing capability, the fallback used, and the reason",
        "Reports the fallback result at medium or low confidence, never high"
      ]
    },
    {
      "id": 103,
      "prompt": "You mentioned SpecKit Pro answers are 'discovery-informed.' What exact evidence note is attached to such an answer, and what fields does it carry?",
      "expected_output": "Explains the compact capability-path evidence note in the form 'Capability path: <need> -> <selected capability/source>; Evidence: <citations or local file refs>; Confidence: <high|medium|low> (<brief reason>)'. The note records the need, the selected capability or source it was routed to, the evidence backing the answer, and a calibrated confidence with a brief reason — so the capability path is observable without naming a preferred vendor set.",
      "expectations": [
        "Describes the compact evidence note in the form 'Capability path: <need> -> <selected capability/source>; Evidence: ...; Confidence: ...'",
        "The note names the need and the selected capability/source it was routed to",
        "The note records the evidence and a calibrated confidence with a brief reason",
        "ABSENCE: the evidence note does not assert a preferred named-vendor set — only the capability/source actually selected for the need"
      ]
    },
    {
      "id": 104,
      "prompt": "If one SpecKit Pro answer is backed by an external standards source that supports citation and another is backed only by files in the repository, how does each present its Evidence field?",
      "expected_output": "Explains that Evidence presentation depends on the source. When the selected source supports citations, the Evidence field uses citations. When the evidence comes from repository files or local artifacts, the Evidence field uses local file references (repo-relative paths). The form stays 'Capability path: <need> -> <selected capability/source>; Evidence: <citations or local file refs>; Confidence: ...' in both cases; only the Evidence contents differ by source type — citations for the citable external source, local file references for the repo-sourced finding.",
      "expectations": [
        "Uses citations in the Evidence field when the selected source supports citations",
        "Uses local file references (repo-relative paths) in the Evidence field when the evidence comes from repository files or local artifacts",
        "Keeps the same capability-path evidence-note form for both, differing only in Evidence contents",
        "ABSENCE: cites the actual selected source, not a preferred named-vendor set"
      ]
    },
    {
      "id": 105,
      "prompt": "If one SpecKit Pro finding used a high-fit installed capability with a direct authoritative source and another fell back to local files because nothing was installed for that need, how should the Confidence on each compare, and why?",
      "expected_output": "Explains that Confidence is calibrated to evidence quality, so it is lower for the fallback finding. The first finding may carry high confidence because a high-fit installed capability supplied a direct, authoritative source. The fallback finding, resolved because no installed capability covered the need, MUST be reported at medium or low confidence — fallback evidence is never reported as high confidence — and carries the fallback disclosure line stating the missing capability, the local/native/repo-local fallback used, and the reason. Lower fallback quality yields a lower, explicitly calibrated confidence.",
      "expectations": [
        "States the fallback finding is reported at medium or low confidence, never high",
        "States the high-fit finding may carry high confidence because a direct authoritative source backed it",
        "Ties the confidence difference to evidence quality / source directness rather than to which named tool was used",
        "Requires the fallback finding to carry the fallback disclosure naming the missing capability, the fallback used, and the reason"
      ]
    }
  ]
}
