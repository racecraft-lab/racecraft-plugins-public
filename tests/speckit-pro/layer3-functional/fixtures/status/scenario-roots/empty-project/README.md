<!-- fixture-kind: deterministic-synthetic-testdata; setup input only, not a live project snapshot. -->

# Empty Project Scenario

This root intentionally contains no technical roadmap or workflow files. The
status skill must report that no SpecKit tracking artifacts were found and
direct the user to coach/setup guidance.
