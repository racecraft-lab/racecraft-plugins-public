<!-- fixture-kind: deterministic-synthetic-testdata; setup input only, not a live project snapshot. -->

# All Blocked Scenario

Every remaining roadmap entry is blocked by an incomplete dependency. Status
must list those blockers and must not recommend a blocked spec.
