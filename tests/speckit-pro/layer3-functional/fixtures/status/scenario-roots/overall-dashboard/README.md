<!-- fixture-kind: deterministic-synthetic-testdata; setup input only, not a live project snapshot. -->

# Overall Dashboard Scenario

Materialize this root as a disposable project and attach the declared worktree
before execution. The roadmap contains complete, active, pending, and blocked
rows; the workflow supplies phase detail for the active SPEC-021 entry.
