<!-- fixture-kind: deterministic-synthetic-testdata; setup input only, not a live project snapshot. -->

# Multiple Roadmaps Scenario

Both roadmap files are present intentionally. The current-quarter file is the
relevant source; the older file must not be silently merged into it.
